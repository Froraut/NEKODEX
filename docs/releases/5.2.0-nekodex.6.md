# NEKODEX 5.2.0-nekodex.6

The sidebar footer now shows only the NEKODEX cat and the complete version number. The GitHub icon, Source & credits label and footer link are removed. Repository attribution and license files remain unchanged.

## Development evidence

Vite renderer build and version/download-link consistency passed. The real isolated Electron DEV renderer showed the cat with `v5.2.0-nekodex.6`, without a link or the old credits label and without horizontal overflow. The temporary DEV process and profile were removed. No broad or repeated passing tests ran.

Includes the in-app Updates screen and authenticated GitHub feed fallback from .5. Existing accounts, settings and connector identities are preserved. macOS packages are released for Apple Silicon and Intel through the existing signature, notarization and integrity gates.
