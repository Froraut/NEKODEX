# NEKODEX 5.6.0-nekodex.3 — hidden-browser startup fix

Includes Native6 and corrects the installed startup failure when account capability inspection runs with the embedded ChatGPT browser hidden.

- The primary automatic browser now receives the same explicit background viewport as running task tabs. It is reapplied after navigation/renderer replacement and removed when shown or when Manual mode is selected.
- If a viewport transition rejects a physical reasoning-menu click, only that specific error falls back to the existing primary-pointer activation. Other errors still propagate.
- No account capabilities are fabricated; the exact reasoning range must still be observed.

A focused fallback case and an actual hidden Electron window scenario passed. The hidden browser reported a usable 1000x728 renderer viewport, and revealing its surface released emulation. Earlier Native6 and route checks remain valid; no full suite was run.
