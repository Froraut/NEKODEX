# NEKODEX 5.2.0-nekodex.3

Fix Browser tab alignment: the cat icon, title, and status indicator now retain their own sizing and align on one row.

The README now includes the updated NEKODEX image without a caption.

## Development evidence

Source 682531c was opened in the isolated Electron DEV profile with the source renderer at 127.0.0.1:4178. The Browser tab showed the cat icon, title, and status dot aligned on the same row. The DEV app and Vite server were closed afterward. Launcher TypeScript verification had already passed for this change.

The production Native4 connector and NEKODEX tunnel were configured separately on the local installation. That account-specific configuration is not bundled in this release. Connector discovery was verified; execution of a command through Native4 was not part of this focused check.
