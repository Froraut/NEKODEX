# Four-wave implementation wave 2, lane 12

Baseline `3a66157` plus uncommitted wave 1. Exclusive source ownership: `launcher/src/App.tsx`. No source change was needed in this lane.

## D06 and D13 disposition

- **D06:** Wave 1 already replaces the displayed smoke value from `browserSmokePassed` and the current app version during initial hydration, state events, explicit state updates, and metadata refresh. The backend's separate session latch is removed; migration clears persisted smoke fields and publishes a state event. No remaining monotonic OR merge or independent smoke-positive renderer path was found.
- **D13:** Setup and MCP surfaces use current `mcpSetupComplete`, with a failed Automatic production `runtime-start` operation suppressing a stale positive while state publication arrives. The old verification timestamp is presented as a last verification rather than a current ready flag. Backend/state owners clear current MCP evidence on failed or external production startup. Manual local verification retains its separate meaning.

Manual review covered the current `App.tsx` diff, its snapshot/event/direct-action update paths, the relevant backend and state changes, and `Overview.tsx`'s adjacent connection display. No additional concrete D06/D13 correction is justified. No tests, typecheck, runtime, benchmark, release, commit, or publication were run; the parent owns the final focused cases. A live same-process migration and failed-startup UI observation remain unproven until that pass.

Changed path in wave 2: `docs/reviews/2026-09-15-four-wave-fix2-12.md` only. No outside-file change requested.
