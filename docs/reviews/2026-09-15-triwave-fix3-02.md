# NEKODEX implementation wave 3 of 3, lane 2 — final service handoff

Baseline: `3740505b0107af9a83053fd523ae1ad30be36465` (`3740505`) plus the current uncommitted correction waves. Exclusive source scope: `src/service.ts`. This was the parent final focused adversarial pass for the accepted triwave service paths. No source change was made in this wave.

## Final disposition

No additional concrete bug survived the final review. The current `src/service.ts` diff already fixes the accepted **T2-2-1 / P2** path:

- `installService()` obtains the current launchd/file observation and renders the next plist before filesystem mutation.
- A loaded service with a missing or different saved definition is rejected before directory creation, plist replacement, the definition-written callback, or bootstrap.
- A loaded service with the same definition remains a no-op.
- An unloaded service retains the existing write, callback, bootstrap, and fresh-status behavior.

The source diff was left unchanged.

## Adversarial paths reviewed

- Direct CLI `service install` and its unchanged `installService(config)` caller.
- Setup's service definition snapshot, ownership callback, loaded-state observation, restart handoff, and rollback path.
- `restartService()`, `stopService()`, and `uninstallService()` drain/bootout behavior after a service install rejection.
- The missing-configuration uninstall boundary in `src/cli.ts`.
- The analogous `installTunnelService()` loaded-definition guard and the tunnel setup handoff, to check that the service fix preserves the established lifecycle pattern.
- The current diff's use of the pre-mutation `current.loaded` and `current.installed` observation, including the loaded/missing-definition case and the loaded/changed-definition case.

No new service lifecycle, setup ownership, or ABI interaction defect was found. The setup callback contract and `ServiceStatus` shape remain unchanged.

## Cross-scope handoffs checked

- `src/cli.ts` continues to own direct terminal service actions and requires the existing configuration for mutating actions.
- `src/setup.ts` continues to own configuration checkpoints, explicit restart/drain decisions, and compensation. It can continue calling `installService(config, onServiceDefinitionWritten)` without an API change.
- `src/tunnel-service.ts` retains its separate loaded-definition guard and tunnel lifecycle ownership.
- No source outside `src/service.ts` was edited by this lane.

`Native4`, `Native4DEV`, and `ZeroRisk4` identities, connector names, public schemas, and generation-4 ABI were not changed.

## Exact residual limits

1. **External status/filesystem interleaving remains.** The installer observes launchd and the plist, then later reads or writes them. An unrelated actor can change launchd state or the plist between those operations. Closing that window requires a cross-process lock or launchd definition-generation/transaction protocol outside this narrow source fix.
2. **`launchctl print` status semantics remain bounded.** An ordinary nonzero `launchctl print` result is still treated as `loaded: false`; this code has no live evidence that distinguishes a missing target from every actionable print failure.
3. **Launchd transition failures remain explicit failures.** Bootstrap, bootout, unload polling, or later status observation can still fail and require the existing caller/manual recovery path. This pass did not add implicit drain/restart or recovery redesign.
4. **No live runtime claim.** This is source-level evidence only. No macOS launchd, daemon, browser/account, installed-app, production, or end-to-end behavior was exercised.

## Verification boundary

Manual focused source review only. No tests, typecheck, scripts, runtime/account actions, production actions, release work, or commit were performed. The current source diff remains limited to the accepted service-install guard, and no additional source change is warranted.

## Counts

- New concrete bugs: **0**
- Additional source fixes: **0**
- Confirmed accepted finding retained: **T2-2-1 / P2**
- Cross-scope handoffs checked: **4**
- Exact residual limits recorded: **4**
- Source paths changed in this wave: **none**
