# Implementation wave 2, lane 02 — service helper

Baseline `3a66157` plus uncommitted wave-1 implementation. No further source change is needed for D01 or D12 in this lane.

`installService(config, onDefinitionWritten?)` already invokes the synchronous callback with the exact generated plist `{ path, data }` after its own successful `atomicWriteFile` and before the first status query or bootstrap. A matching existing plist, a failed write, or a call without a callback does not produce an owned-write event. The setup owner already records that event and captures loaded state in a `finally` around the call. The callback contract remains the settled direct callback API; no ABI or schema changes were made.

For D12, `removeLegacyRuntimeArtifacts` retains its active-command path-reference refusal and propagates cleanup failures. `setup.ts` already catches those failures after route commit and returns an optional warning with successful setup; `src/cli.ts` displays the warning. Changing the cleanup helper would blur its refusal contract, so it remains untouched.

Changed path: this report only. Manual review covered `src/service.ts`, the direct setup/CLI callers, `src/tunnel-service.ts` for callback parity, and the two adjudicated paths. No tests, typecheck, runtime launch, benchmark, production operation, or commit were performed. No outstanding material concern in this file; the parent owns its final focused cases and integration result.
