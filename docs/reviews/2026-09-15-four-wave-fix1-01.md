# Four-wave fix, wave 1 lane 01 — setup transaction

Baseline `3a66157a8943a80bbbe299699cc96bec82721ba1`. Changed `src/setup.ts` only; this report records the handoff. No tests, typechecks, benchmarks, installs, production changes, dependencies, or commits. Manual review of the focused diff and rollback/direct-caller paths; `git diff --check -- src/setup.ts` passed.

## Addressed

- **D01/D02:** `installService(config, onDefinitionWritten)` and `installTunnelService(config, onDefinitionWritten)` supply the exact `{ path, data: string }` immediately after their own atomic plist write and before a possible bootstrap/status failure. Setup records `Buffer.from(data)` as its owned expected bytes without rereading a later unknown write. Loaded-state transitions are checkpointed even when stop/uninstall/restart/install throws; deletion checkpoints record expected absence only after the definition is absent. Rollback still compares current bytes and loaded state with those owned checkpoints before restoring the previous definition/state. It refuses foreign definition changes and preserves the existing unknown tunnel-profile-write guard.
- **D10/D11:** direct DEV setup that would change an existing Full profile probes its old alias *before* tunnel-client/key/profile/config mutation. Only an exact successful JSON status with `process_running: false`, state `stopped`, and no error permits the transaction. Running, busy, unreadable, missing, or uncertain status rejects with launcher owner/idle-drain guidance. In particular, Full-to-browser-only cannot commit while the old alias is running. Setup does not stop or reconnect a runtime whose owner it cannot verify.
- **D12:** legacy artifact cleanup after the route commit is caught and returned as `SetupResult.warnings?: string[]`, leaving setup success and the committed route explicit. The helper's existing path-reference refusal remains intact; a cleanup failure is reported for retry.

## Cross-lane contract and limits

Owners 02/03 expose identical direct optional callback signatures: `installService(config, onDefinitionWritten?: ({ path, data: string }) => void)` and `installTunnelService(config, onDefinitionWritten?: ({ path, data: string }) => void)`. Invoke only after an actual atomic definition write, before bootstrap/status operations. Owner 14 reads `SetupResult.warnings?: string[]` and prints successful setup plus cleanup warnings. DEV CLI owner 05 can surface the fail-closed diagnostic or use its verified launcher setup handshake; no new callback or public tool schema is required.

This is a source implementation and manual diff review, not a reproduced launchd failure or live DEV owner handoff. The parent owns the single focused integration pass. A tunnel `connect` failure may still have written an unknown profile and remains a manual-recovery boundary; no process ownership is inferred from tunnel status alone.
