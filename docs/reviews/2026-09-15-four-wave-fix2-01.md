# Four-wave fix, wave 2 lane 01 — setup transaction residuals

Baseline `3a66157a8943a80bbbe299699cc96bec82721ba1`, with wave 1 uncommitted changes preserved. Changed `src/setup.ts` only; this report is the other authorized path. Manual review of the changed transaction, helper callbacks, rollback, DEV direct caller and CLI warning consumer. No tests, new test files, typecheck, suite, benchmark, runtime, production operation or commit. The parent owns the shared final focused cases.

- **D01/D02:** definition bytes remain the exact helper callback bytes from immediately after the atomic write. Generic checkpoints no longer reread a later plist or profile and label foreign bytes as owned. A successful tunnel `connect` captures the profile once in its completion callback; an uncheckpointed failed `connect` remains a manual-recovery boundary. Service/tunnel status probes after transitions now catch their own errors without masking the original bootstrap/stop/install error. Failed probes mark loaded state unknown, block route commit and appear alongside the primary error in rollback diagnostics. Recovery of an unknown service state is deliberately withheld, while config and the other independent service still get their rollback attempts. A rollback-time tunnel status failure is caught before independent client/key fallback; uncertain loaded state preserves dependent files. A failed config snapshot during rollback is caught rather than aborting other compensation.
- **D10/D11:** every direct setup against an existing Full DEV profile requires a successful, explicit stopped-status observation of its old alias before any key/client/profile/config mutation. Even an apparently unchanged setup can reinstall the tunnel client or recreate a missing profile. Running, busy, missing, unreadable or ambiguous status rejects. No process ownership or idle-drain capability is inferred from tunnel status; CLI's direct path has no safe stop/recovery handshake.
- **D12:** post-commit legacy cleanup remains a `SetupResult.warnings?: string[]` maintenance outcome. The result uses the service state observed before commit, so a new post-commit service probe cannot turn a committed route into a reported setup failure. CLI owner 14 already prints warnings.

Material limits: a status probe failure after a definition write can leave the new definition and an unknown launchd state; automatic rollback cannot safely restore the old service until state is independently determined. The DEV rejection of an existing Full profile may require an owner-managed stop before retry. Neither a live launchd failure nor a DEV owner handoff was exercised in this lane. No ABI/name pins changed.
