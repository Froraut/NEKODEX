# Lane 12 — connector identity and fixture map

Baseline: `d880b12`. Research only; no source edits, tests, typechecks, or account changes.

## Parent-owned identity changes

- `src/config.ts:24-38`: change current Automatic to `Codex Native4`, DEV to `Codex Native4 DEV`, and Manual to `Codex Zero Risk4`. Append `Codex Native3`, `Codex Native3 DEV`, `Codex Zero Risk2`, and `Codex Zero Risk3` to the legacy list; retain Native/Native2/Zero Risk aliases. Zero Risk3 is a defensive alias only; it was never published here. Make `currentChatGptConnectorName()` route all old Manual names to Zero Risk4. Old Automatic DEV names route to Native4 DEV; old plain Automatic names route to Native4. Custom Automatic names remain custom.
- `src/config.ts:371-389`: setup should migrate persisted `appName`, `automaticAppName`, and `manualAppName` according to active mode. Extend the special Automatic collision repair to old Manual identities (`Codex Zero Risk`, `Codex Zero Risk2`, and defensive `Codex Zero Risk3`), and rewrite those old `manualAppName` values to Zero Risk4. A saved Manual config with old `appName` and an old DEV `automaticAppName` must become Zero Risk4 active, Native4 DEV saved, with its separate tunnels untouched. Runtime `loadConfig()` still rejects retired active names via `parseConfig()` (`:439-462`); setup only targets fresh names and does not claim a connector exists in ChatGPT.
- `launcher/electron/connector-identity.cjs:1-48`: mirror the same current/legacy map. In `connectorNameForDevSetup()`, special-case old Zero Risk, Zero Risk2, defensive Zero Risk3, and current Zero Risk4 **before** generic legacy classification; otherwise an old Manual name incorrectly resolves to Native4 DEV. Current plain Native4 continues resolving to Native4 DEV in the development profile; custom names pass through. `requireCurrentRuntimeConnectorName()` must reject Native3, Native3 DEV, Zero Risk2 and defensive Zero Risk3 as legacy schemas.

## Fixture and direct-caller map

- `tests/runtime-layout.test.ts:164-229`: update the parameterized legacy Automatic map, collision repair, and Manual ABI fixture to use Native3/Native3 DEV/Zero Risk2 as the immediately retired inputs, asserting Native4/Native4 DEV/Zero Risk4 outputs. Preserve the old alias cases as historical migration checks; add Zero Risk3 only as a defensive alias case. The Manual case also asserts tunnel objects are preserved.
- `launcher/tests/runtime-host.test.cjs:80-85,139-152,492-516,827-864`: current fixtures should use Native4, Native4 DEV, Zero Risk4; legacy fixtures should include Native3, Native3 DEV, Zero Risk2, plus defensive Zero Risk3. The unchanged-version upgrade case should use Native3 to prove ABI migration occurs without a version bump. Keep one custom-name assertion to protect pass-through behavior in the identity helper path.
- `tests/browser-worker-contract.test.ts:190-229`: update migration guidance expectations and legacy-menu fixtures to current Native4 while retaining old names as legacy rows. Broader UI fixtures elsewhere in this file containing Native3 are current-name fixtures, not all legacy tests.
- `tests/chatgpt-web-harness.test.ts:2729-2740` and `tests/zero-risk-mcp-lifecycle.test.ts:294-300`: public complete-schema pins are explicitly tied to Native3 and Zero Risk2. #487 changes both schemas, so the pins/comments should identify Native4 and Zero Risk4 and reflect the new optional fields. See `docs/reviews/2026-09-15-upstream-01.md:13-15`. Do not describe Zero Risk3 as a published ABI.
- Additional direct current-name fixture files found by scoped search: `tests/cli.test.ts`, `tests/zero-risk-adapter.test.ts`, `tests/launcher-browser-host.test.ts`, `tests/launcher-helper-client.test.ts`, `launcher/tests/control-server.test.cjs`, `launcher/tests/runtime-supervisor.test.cjs`, `launcher/tests/localization.test.cjs`, and `launcher/tests/upgrade-download-focused.test.cjs`. Coordinate owner updates. Historical release/review prose is immutable.

## Parent verification suggestion (do not run in this lane)

Use exactly two named cases after parent/other-lane edits: `tests/runtime-layout.test.ts` — **manual ABI migration preserves its distinct tunnel and requires a new connector**; `launcher/tests/runtime-host.test.cjs` — **native and manual cached ABIs cannot be verified as current in either launcher profile**. Update the latter case's small table to cover Native3, Native3 DEV, Zero Risk2, defensive Zero Risk3, and one custom pass-through assertion. This leaves the parent's shared focused-test budget intact here.
