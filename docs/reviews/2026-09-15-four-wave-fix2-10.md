# Implementation wave 2, writer 10 — D07 journal handoff

Source changed: none. I manually reviewed the current `src/codex-integration-journal.ts` diff against the D07 adjudication, wave-1 lane 10/11 reports, the direct `src/codex-integration.ts` callers, and the JSON hook/guarded-write helpers. No tests, typecheck, runtime checks, or release work were run.

The wave-1 correction already satisfies D07 in this file. `readJournal()` and `{ reconcileInactiveHook: false }` never remove an inactive v11 JSON hook, including when both inactive copies agree. `{ reconcileInactiveHook: true }` can finish hook removal only when the inactive recovery copy exactly matches the old active primary with `active` flipped, the restored config matches, and the hook still matches the recorded JSON entry. The recovery write checks the full hook bytes immediately before writing. A missing hook instead permits the normal journal-copy repair; an occupied or changed slot fails closed. Lane 11 uses true only for explicit disconnect/uninstall and detects a reappeared hook before reconnect, setup, and uninstall.

Material boundary for parent: the final inactive-hook observation and journal deletion in `uninstallCodexIntegration()` are separate operations in lane 11's file. A hook inserted between them can survive without a journal; this cross-file race is outside writer 10 ownership. The existing exact-byte guard cannot prove provenance when an actor places identical content into the owned slot during a genuinely pending transition. No new source edit is justified by the D07 evidence here.
