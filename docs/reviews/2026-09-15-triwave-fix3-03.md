# NEKODEX triwave final correction 3 of 3 — tunnel-service

- **Date:** 2026-09-15
- **Scope:** `src/tunnel-service.ts` for source review and correction; this review artifact is written under `docs/reviews/`.
- **Baseline:** current working tree after triwave correction waves 1 and 2, including the current uncommitted `src/tunnel-service.ts` diff.
- **Boundary:** Native4, Native4DEV, ZeroRisk4 identities, connector/tool schemas, and prior public ABI shapes were preserved.
- **Verification:** manual focused source review only. No tests, typecheck, scripts, runtime/account actions, production actions, release actions, or Git commit operations were performed.

## Final disposition

**No source change.** No additional concrete bug survived the adversarial read of the accepted triwave paths and the current `src/tunnel-service.ts` diff.

The current diff correctly:

- keeps all new `AbortSignal` parameters optional, preserving existing call compatibility and the `installTunnelService(config, onDefinitionWritten)` callback position;
- checks cancellation before macOS mutation, after the launchd status probe, before plist directory/file mutation, before bootstrap, and before final status publication;
- passes the signal through synchronous `launchctl print`, `bootstrap`, and `bootout` calls;
- propagates cancellation through unload polling and removes the polling abort listener on both timer completion and abort;
- refuses a changed loaded definition before replacing it;
- preserves the existing ordinary nonzero `launchctl print` interpretation as unloaded and the existing bounded status timeout;
- keeps uninstall cancellation before plist removal and keeps restart as stop-then-start under the same optional signal.

No line in the current helper creates a new ABI, identity, definition-ownership, or cancellation mutation defect that can be fixed safely within this exclusive source scope.

## Cross-scope handoffs checked

1. **T3-15-1 stale-owner tunnel recovery cancellation.** The helper now exposes and propagates optional signals through status, stop, restart, install, uninstall, bootstrap/bootout, and unload waiting. The owning stale-owner path remains in `launcher/electron/runtime-supervisor.cjs`; it must pass its startup/recovery signal into the optional helper parameters to obtain the intended end-to-end cancellation behavior. That caller is outside this scope and was not edited.

2. **T1-15-1/T2-15-1 unresolved tunnel control-child ownership.** The retained-child registry, bounded termination, and control serialization are owned by `launcher/electron/runtime-supervisor.cjs`. The helper's `launchctl` cancellation changes do not replace or duplicate that supervisor ownership contract. No additional helper-local race was established.

3. **T2-1-1 divergent launchd definition rollback.** The exact-byte restoration and exact-path re-bootstrap are owned by `src/setup.ts` rollback. The current helper's loaded-definition guard was retained; no source-local change is warranted here.

4. **Direct CLI and setup callers.** Existing callers remain valid because every added parameter is optional. Their lack of a signal is a caller-level cancellation policy boundary, not a helper defect within this pass.

## Exact residual limits

- End-to-end stale-owner cancellation remains unproven until the supervisor caller passes its signal through the new optional helper parameters; this was outside the exclusive source scope.
- The helper uses synchronous `spawnSync` through `src/process.ts`; cancellation is delegated to Node's supported child-process signal behavior, and no live abort timing was exercised.
- A cancellation arriving after a synchronous filesystem mutation has started cannot roll that mutation back. The pre-mutation checks reduce the boundary but do not make `atomicWriteFile`, directory creation, or plist removal transactional.
- A cancellation after `launchctl bootout` has begun can reject before launchd has fully unloaded; callers still own reconciliation/manual recovery of the resulting service state.
- An ordinary nonzero `launchctl print` remains classified as unloaded. This existing status ambiguity is preserved for ABI and behavior compatibility.
- Definition replacement remains refused while the service is loaded. The helper does not solve external edits or ownership races that occur after a status/read check and before a filesystem operation.
- No runtime, account, browser, production, or installed-app behavior was proven by this pass.

## Counts

- New source bugs: **0**
- Source files changed: **0**
- Review artifact written: **1**
- Cross-scope handoffs inspected: **4**
- Native4 / Native4DEV / ZeroRisk4 ABI or identity changes: **0**
- Tests/typechecks/scripts/runtime/production/commit actions: **0**

