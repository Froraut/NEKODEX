# Implementation wave 1, lane 05 — DEV CLI

Frozen adjudication baseline: `3a66157a8943a80bbbe299699cc96bec82721ba1`. Source change: `src/dev-chat/cli.ts` only. Manual diff review of the changed `chat` catch/finally and the direct `driver.close()` / transport `close()` declarations; no tests, typechecks, benchmark, runtime invocation, package operation or publication.

## D18 / R1-5-2 — implemented

The chat operation records its original thrown value (including a non-`Error` value) before `finally`. Both closes still settle via `Promise.allSettled`. If cleanup also fails, the resulting `AggregateError.errors` lists the original operation failure first, then driver and transport close failures in that order. With no close failure, the original error is rethrown unchanged; with no operation failure, the existing cleanup-only aggregate remains. Interactive command errors that are handled and printed inside `interactive()` are not primary exceptions to this outer lifecycle.

## D11 / R2-5-1 — coordination with setup owner

CLI calls the existing `setupDevProfile(options): Promise<DevProfileSetupResult>` API and prints success only after it resolves. No API or caller change was needed in this lane. Writer 01 owns the pre-mutation fail-closed check for an actually running Full DEV tunnel, including any owner/idle-drain handshake and refusal when ownership or idleness cannot be proven. The CLI must receive that refusal as a rejected `setupDevProfile` promise before tunnel fields or config are changed. `tunnelStatus` alone cannot prove ownership or atomic idleness, and the CLI has no direct supervisor `stopForSetup` contract; it does not stop, reconnect or drain a runtime itself. Until writer 01's guard is integrated, the D11 Full-to-browser-only live-owner path remains open.

Native4, Native4 DEV, Zero Risk4 and the public tool ABI pins were untouched. The combined failure path is source-reviewed but not executed; the parent's shared focused cases and second-wave inspection remain the verification boundary.
