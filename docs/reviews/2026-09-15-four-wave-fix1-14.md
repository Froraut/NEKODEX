# Wave 1 fix, lane 14 — CLI setup output and tunnel status

Baseline: `3a66157`. D19 and the CLI presentation side of D12 addressed in `src/cli.ts` only. Manual diff review of the setup success path, tunnel status exit rule, and adjacent service-control branches; no tests, typechecks, runtime checks, benchmark, release, or commit.

- **D19:** `tunnel status` requires a healthy runtime for launcher ownership; terminal ownership still requires both a healthy runtime and a running launchd tunnel service. The printed `{ service, runtime }` shape stays the same. `start`/`restart` keep their existing service-control exit rule, and `stop` remains exempt.
- **D12:** after `setup()` resolves successfully, CLI prints `Setup complete`, `Config`, then each optional warning as `Setup warning: <message>`. Warnings do not set an error exit code or suppress connector/restart guidance. An actual setup rejection still follows the existing failure path.

Cross-lane API for writer 01: add `warnings?: string[]` to `SetupResult` and return post-commit cleanup warnings through it only after the committed setup succeeds. CLI consumes `result.warnings ?? []`; the current `src/setup.ts` does not yet declare this field, so source compilation depends on writer 01's change. This lane neither catches setup errors nor changes the cleanup/refusal guard in writer 01/02's files.

Limits: no live tunnel or setup cleanup failure was exercised; launcher `start`/`restart` ownership semantics remain a separate boundary. Native4/ZeroRisk4 names and public tools schema/ABI were untouched.
