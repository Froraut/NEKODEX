# NEKODEX implementation wave 1 of 3, lane 9 handoff

Baseline: `3740505b0107af9a83053fd523ae1ad30be36465` (`HEAD`).

## Implementation

Fixed **T2-14-1** in the source paths owned by this lane:

- `src/doctor.ts`
  - Guards the managed service probe, tunnel service probe, and tunnel runtime probe independently.
  - Converts thrown probe failures into `DoctorCheck` entries with `status: "error"` and the exception detail.
  - Continues assembling the report, so previously collected diagnostics remain visible.
  - The existing `DoctorReport.ok` calculation therefore keeps the command exit status failing when a probe cannot be determined.
- `src/cli.ts`
  - Guards the runtime and tunnel-service probes used by `tunnel status` and related status-producing actions.
  - Emits JSON with a failed runtime status or an `error` field on the service status when a probe throws.
  - Preserves nonzero exit semantics for probe failures.

No Native4, Native4DEV, or ZeroRisk4 public ABI, identity, registration, or schema was changed.

## Changed paths

- `src/cli.ts`
- `src/doctor.ts`
- `docs/reviews/2026-09-15-triwave-fix1-09.md`

## Verification and limitations

No tests, typechecks, scripts, runtime probes, production actions, or commits were run, per lane instructions. I performed a focused source diff review and `git diff --check`.

The fix is source-level and covers thrown status-probe exceptions. It does not establish the frequency of subprocess timeouts or live tunnel/service behavior. Parent-owned final focused verification remains pending, limited to the parent’s stated budget.
