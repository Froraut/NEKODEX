# Wave 1 implementation — lane 08 (`3a66157`)

Addressed **D14** and **D15** in `src/adapters/chatgpt-web/browser-worker.ts`.

- The connector mention path now looks for exact rows only inside a visible `.popover`, following the existing Think slash-popup DOM pattern. The visible popup must be unique before an exact row can prove personalized access or be selected; visible row titles, exact row count, and ArrowDown bound all use its nested `.__menu-item[tabindex="0"]` rows. Sidebar history rows cannot supply connector proof or inflate the highlight bound. The existing exact-name, `data-highlighted`, and selected composer pill keyword gates remain intact.
- A rejected/timed-out `allInnerTexts()` now throws `ChatGPT connector mention popup DOM title observation failed` with the original error as `cause`. A successful empty title observation still diagnoses a menu that did not open; observation errors fail closed and cannot silently become that diagnosis. Only the exact-row `waitFor` timeout enters the missing-row/catalog-refresh branch.

Cross-lane API: **none**. This is a private worker-only locator/diagnostic change; no public tool schema, ABI pin, identity constant, or caller signature changed. The existing caller still performs its single catalog refresh only for `ChatGptConnectorCatalogStaleError`; a DOM observation error surfaces instead of inventing a catalog diagnosis.

Fallback bound: `.popover` is source-observed for the adjacent Think menu. A mention menu rendered outside that container, a hidden popup, or multiple visible popovers cannot be proven by this path. There is deliberately no page-wide row fallback; the second wave should assess this DOM assumption against available account evidence. No live browser reproduction, Playwright run, tests, typecheck, or benchmark was performed. Manual diff review checked proof, missing-row error, row count, keyboard highlight, selected pill, and cleanup branches.
