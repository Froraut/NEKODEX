# NEKODEX — implementation wave 2 of 3, lane 2 (`src/service.ts`)

Baseline: `3740505b0107af9a83053fd523ae1ad30be36465`, with the uncommitted wave 1 edits present in the working tree. Source ownership for this lane was limited to `src/service.ts`.

## Review result

No additional source edit is warranted in this lane. The current diff already retains the correct wave 1 fix for the accepted triwave finding **T2-2-1 / P2**:

- `installService()` renders the next plist and observes the current launchd/file state before mutation.
- When the service is loaded and the saved plist is missing or differs, it throws before directory creation, plist replacement, the ownership checkpoint callback, or bootstrap.
- A loaded service with the same definition remains a no-op.
- An unloaded service retains the existing write, synchronous ownership checkpoint, bootstrap, and fresh-status behavior.

This closes the direct CLI `service install` path that could previously publish a changed plist while leaving the old daemon loaded. The later drain-based `restartService()`, `stopService()`, and `uninstallService()` paths continue to operate on the existing service lifecycle, and setup’s definition checkpoint and rollback callers remain compatible with the unchanged callback contract.

## Cross-scope review

I manually traced the current diff together with `src/cli.ts` direct service actions and the setup service checkpoint/rollback path. No new concrete interaction bug was found beyond T2-2-1, and no correct wave 1 change was reverted.

The remaining boundary is the status-to-filesystem observation window: an unrelated actor can still change launchd or the plist after the installer’s observation and before a later operation. The helper has no shared cross-process transaction or launchd definition-generation token with which to close that external race. The current guard prevents the demonstrated loaded-definition mismatch before this installer writes; adding an implicit drain/restart or a new locking protocol would expand the lane into an optional lifecycle redesign and would overlap setup/tunnel ownership.

Native4, Native4DEV, and ZeroRisk4 identities, schemas, tool registrations, and public ABI are unchanged.

## Verification and limits

Manual source review only. No tests, typechecks, scripts, runtime or account actions, production actions, release work, or commit were performed. The parent retains final focused verification ownership under the standing fast-verification limit.

Changed paths in this lane: this report only. `src/service.ts` retains the uncommitted wave 1 diff exactly as found.

## Counts

- Additional concrete bugs: **0**
- Fixed accepted findings in this wave: **0**; T2-2-1 was already fixed by wave 1
- Cross-scope refinements: **0 new**
- Known limits retained: **1** external status/filesystem interleaving boundary
- Optional redesigns: **0**
- Source edits in this wave: **0**
