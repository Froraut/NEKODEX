# NEKODEX two-wave review and implementation — 2026-09-21

Base: `adf3be6`, source version `5.9.0-nekodex.2`.
Requested workflow: 16 GPT-5.6 Sol agents review and challenge improvements, followed by 16 Sol implementation/verification lanes. The same 16 agents are reused in wave two. Parent owns integration, source publication, and the final evidence boundary.

All 16 first-wave reports were collected before implementation dispatch. Shared files have one writer; lane 05 owns App.tsx and any shell CSS. Lane 07 independently checks renderer changes using isolated, credential-free fixture state. No real account changes or provider requests are part of these checks.

| Lane | Scope | Accepted work / decision |
| --- | --- | --- |
| 01 | Overview | Align manual setup CTA with canonical tools readiness; retain automatic optional-tools and active-run access. |
| 02 | Accounts UI | Preserve per-account quota loading failure; expose meaningful disabled-action reasons. |
| 03 | Updates UI | Distinguish unknown download speed from zero; preserve active updater phase on ancillary cancellation error. |
| 04 | Usage | Both proposals rejected after producer tracing: max intentionally means Pro; CSV exported strings are constrained. Independently reviews passkey changes in wave two. |
| 05 | App shell | Compact drawer focus/Escape; sibling tab close action; explicit setup sign-in choice (from lane 06). |
| 06 | Onboarding/passkey UI | Safe localized passkey errors, coordinated with lane 12 codes. App.tsx change assigned to lane 05. |
| 07 | Proxy/safety UI | No justified correction. Wave two owns independent renderer flow verification. |
| 08 | Route diagnostics | Treat rejected catalog 3xx as failure consistently with runtime acceptance. |
| 09 | Updater backend | Stop network-idle timer before local hash; preserve ownership on failed recovery registration. |
| 10 | Lifecycle | Restart-blocking proposal withdrawn after real caller inspection; operation-ID redesign lacks proven consumer impact. Independently reviews updater patches. |
| 11 | Quota/safety data | Accurate unique-bucket truncation; reject persisted timestamps beyond Date domain. |
| 12 | Passkey backend | Bounded cancellable handoff with retained old ownership; explicit safe public progress. |
| 13 | HTTP/event queue | Cancel rejected oversized body; preserve enqueue-time byte accounting. |
| 14 | Models | Update stale test expectations to intentional composer limits and subagent opt-in. Independently reviews lane 13. |
| 15 | Config recovery | Validate journal structures before route restoration; preserve legitimate legacy formats. |
| 16 | Tunnel | Bound readiness subprocess probes by remaining overall deadline. |

## Verification and handoff

Focused behavioral checks belong to each changed contract. Completed checks:

| Check | Result |
| --- | --- |
| Canonical setup progression | 5 passed |
| Account UI failure/retry and accessible explanation | 4 passed |
| Update UI missing speed and active ancillary error | 2 passed |
| Passkey UI safe errors and control gating, six languages | 3 passed |
| Catalog redirect and subsequent recovery, backend and UI | 2 selected cases passed |
| Authenticated download, cancellation and hash deadline | 6 passed |
| Recovery registration ownership/preparation conflict | 3 selected cases passed |
| Quota normalization and safety timestamp bounds | 11 passed |
| Passkey handoff and public projection | 2 selected handoff cases and 3 projection cases passed |
| HTTP body and event queue | 14 passed |
| Model catalog and Web model contracts | 31 passed |
| Malformed integration journals preserve config | 2 selected cases passed |
| Valid Compatibility V1 exact restoration, structured/inline tables | 3 selected cases passed |
| Tunnel readiness deadline/cancellation | 3 passed |
| Runtime TypeScript | Passed |
| Launcher TypeScript and renderer build | Passed after correcting a literal-inference error; latest renderer `index-DTLSddhv.js` |

Independent reviews: lane 10 challenged and then accepted updater cancellation/ownership; lane 04 challenged and then accepted coordinated passkey backend/UI; lane 14 accepted HTTP/queue patches without duplicate test runs. Parent inspected integration-journal, tunnel, quota/safety and composed UI diffs. Four speculative proposals were rejected rather than implemented (lane 04's two, lane 10's two).

Known baseline limitation: an incidental whole-file `browser-host.test.cjs` run reported 29 failures in existing fixtures. Inspection of exact base source/test content showed missing constructor dependencies (`configureAccountSession`, `authProbeTail`, `getManualSubmitTimeoutSec`, and others) and stale options expectations already present at `adf3be6`; the passkey patch does not touch those failing paths. Only the relevant named handoff/projection cases establish this task's backend verification. No claim that the full legacy file or repository suite passes is made.

Renderer verification completed on `index-DTLSddhv.js`: 8 focused flow groups passed across nonduplicated batches. Coverage: compact drawer active focus, Tab wrap and Escape restoration; desktop nonmodal navigation; arrow-key tab selection, sibling Close and valid remaining selection; fresh Setup Next revealing sign-in choices without login IPC; manual missing-tools Overview routing and automatic optional-tools workspace; per-account quota failure and refresh recovery; active updater warning and unknown speed; safe passkey failure; catalog 302 followed by 200 recovery; Activity/Settings navigation. No renderer page errors were observed in these flows. Unknown passkey input and six-language mapping remain covered by the component checks rather than duplicated browser checks.

The reusable runner is `launcher/tests/two-wave-ui-preview.cjs`, backed by the existing credential-free fixture. Its initial failures were incorrect selector/fixture assumptions (busy login state, English close label, unopened disclosure, and Activity selector); these were corrected and only affected cases repeated. No product regression was identified in the rendered checks. Source-regex App tests were not retained as a substitute for behavioral coverage; the stale diagnostics mounting regex was removed.

Parent and renderer reviewer visually inspected `compact-overview.png`, `desktop-overview.png`, and `updates-installing-warning.png` under the ignored `launcher/output/playwright/wave2-ui/` evidence directory. Layout and active-phase messaging were clear. The exact temporary Chromium and loopback server were closed; screenshots are retained as concise evidence.

Renderer fixtures establish frontend behavior only, not real sign-in, updater installation, provider quota, or installed runtime behavior. No full repository suite, release packaging, version bump, or installed-app replacement was performed. The working installation, account data, existing untracked branding inventories, and shared dependency links were preserved. Source changes are delivered on `codex/nekodex-two-wave-20260921`.
