# Lane 11 — Manual mode ABI fixture handoff

Baseline: `d880b12`. Scope: `tests/zero-risk-mcp-lifecycle.test.ts` only, plus this handoff. No tests, typechecks, builds, commits, account changes, or release work were performed.

The Manual mode ABI fixture at lines 293–302 is the sole `Codex Zero Risk2` reference in the assigned test file. Its ownership comment now targets `Codex Zero Risk4`, synchronized with `Codex Native4` and `Codex Native4 DEV`. Its existing digest, `e05351002a367d8891dbeb9f314779378a0a59853a62b1fc3f7115b2e6496911`, remains the **legacy Zero Risk2 schema digest** and must be replaced when the #487 public `codex_exec` fields are integrated. `Codex Zero Risk3` was never published here; the parent's defensive alias support for that name does not establish a released ABI. No old-name legacy checks exist in this assigned file; preserve them wherever the parent owns connector identity migration tests.

Digest recipe: connect the test's `StdioClientTransport` to `src/cli.ts mcp --contract safe --broker-socket <socketPath>`, call `client.listTools()`, and map each listed tool in returned order to `{ name, title: title ?? null, description: description ?? null, inputSchema, outputSchema: outputSchema ?? null, annotations: annotations ?? null }`. Serialize that array through `canonicalJson`, which recursively sorts object keys but preserves array order, then compute `createHash("sha256").update(canonicalJson(publicConnectorAbi)).digest("hex")`. Replace only the pinned literal after the coordinated schema patch is present and the parent runs its allowed focused ABI check. The stable name-list and lifecycle assertions need no identity rewrite.

Direct-file review: `src/config.ts` and `launcher/electron/connector-identity.cjs` still define Native3/Native3 DEV/Zero Risk2 at baseline, and `docs/architecture.md` states that a public schema change requires a fresh identity. These files belong to the parent. The prior upstream review deferred #487 for this reason. The assigned fixture does not itself test configured connector names.
