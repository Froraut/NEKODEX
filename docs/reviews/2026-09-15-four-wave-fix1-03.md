# Implementation wave 1, lane 03 — D02 tunnel definition checkpoint

Baseline: `3a66157`. Changed `src/tunnel-service.ts` only for source behavior.

`installTunnelService(config, onDefinitionWritten?)` accepts the same direct optional second-argument callback as `installService`: `(definition: { path: string; data: string }) => void`. It fires synchronously after this call successfully writes a changed/new plist with `atomicWriteFile`, before any `launchctl bootstrap` attempt. `path` is the installed tunnel plist path and `data` is the exact generated UTF-8 plist string. It does not fire when the definition already matches, when validation/loaded-definition guard fails, or on rollback's existing one-argument call. The callback's exception propagates; the setup owner must make its checkpoint safe and preserve exact ownership guards.

Setup owner (lane 01): pass `({ path, data }) => { /* record the owned definition bytes at path */ }` as the direct second argument at the forward install call. This hook covers the write-before-failed-bootstrap portion of D02; stop/uninstall loaded-state transitions and compensation remain in `src/setup.ts` ownership. The helper does not itself roll back or infer loaded state. Existing status probe timeout, unload wait bounds, readiness behavior, loaded-definition guard, and public tool ABI remain untouched.

Manual diff review: checked the new branch against the original write condition, bootstrap ordering, direct setup/CLI callers, and rollback call. No tests, typechecks, benchmarks, suites, production calls, dependency changes, or commit were run in this lane. Live launchd failure and end-to-end compensation remain unverified for the parent's shared focused pass.
