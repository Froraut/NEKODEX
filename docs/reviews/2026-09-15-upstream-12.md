# Upstream lane 12 — PAC/network

Scope: upstream PR [#469](https://github.com/miuuyy/codex-chatgpt-web/pull/469), open when fetched on 2026-09-15. GitHub issue API resolves `issues/469` to this same pull request; it is counted once. Its live body, comments (none), review list (none), and five-file PR diff were fetched through `gh api`/`gh pr diff`. Comparison target: NEKODEX checkout `359d8fc000d73b32497cf6465fbd3cd1998d3a14`, with concurrently edited `src/tunnel.ts` and `src/tunnel-service.ts` left untouched. This was manual source review only; no tests or runtime probes.

| Item | Classification | Source evidence and assessment | Recommendation |
| --- | --- | --- | --- |
| [PR #469](https://github.com/miuuyy/codex-chatgpt-web/pull/469), `445362d3dd3baa445d566e561c9abb30c995453c` | **Already addressed in current source** | The PR body describes Electron following system PAC while launcher-owned native children dial directly, causing tunnel control-plane timeouts and `Session terminated`. Its diff adds `launcher/electron/native-proxy.cjs`, resolves PAC for the native Codex URL and `/v1/tunnels`, and injects process-local env in `main.cjs`/`runtime-supervisor.cjs`. NEKODEX already has an adapted resolver at [`launcher/electron/native-proxy.cjs:1–50`](../../launcher/electron/native-proxy.cjs), wired to the browser partition at [`main.cjs:1140–1152`](../../launcher/electron/main.cjs) and [`main.cjs:1209`](../../launcher/electron/main.cjs). The daemon consumes its environment at [`runtime-supervisor.cjs:515–520,1206–1212`](../../launcher/electron/runtime-supervisor.cjs); tunnel control commands at [`runtime-supervisor.cjs:1761–1774`](../../launcher/electron/runtime-supervisor.cjs); setup/doctor/tunnel/uninstall children at [`runtime.cjs:684–705`](../../launcher/electron/runtime.cjs). Native passthrough actually uses the endpoint-specific Bun fetch proxy option at [`src/native-passthrough.ts:32–42`](../../src/native-passthrough.ts). The current resolver additionally merges existing `NO_PROXY`/`no_proxy` with loopback and respects explicit `ALL_PROXY` as well as HTTP/HTTPS proxy settings. The earlier local review explicitly records the adapted #469 decision at [`docs/reviews/2026-09-14-investigation.md:74`](2026-09-14-investigation.md). | Keep the NEKODEX adaptation; do not apply the upstream patch wholesale. If a PAC-network symptom recurs, inspect the actual selected PAC directive, explicit environment variables, control-plane dial error and whether the broker saw the tool call before changing routing. |

The PR author reports one affected macOS network recovering, a successful Full-harness turn, and large suite counts in the upstream branch. Those observations support that author's environment, not current NEKODEX runtime or all PAC configurations. The local coding-iterations note concerns E01–E21 and three optimizations, and its verification section says no live model/connector combinations were exercised ([`docs/reviews/2026-09-15-coding-iterations.md:47–60,74–80`](2026-09-15-coding-iterations.md)). No live PAC result was collected in this lane. Unsupported PAC directives such as SOCKS remain an explicit source error rather than a silent direct connection; the first PAC directive is the only one translated in both implementations. Any need for more directive types requires a specific observed network case.

Counts across one deduplicated upstream item: already addressed **1**; still applicable concrete defect **0**; external service behavior **0**; insufficient evidence as the item's primary classification **0**; useful optional feature **0**. Runtime coverage for the current network remains unverified by this source review.
