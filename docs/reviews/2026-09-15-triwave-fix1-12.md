# NEKODEX — implementation wave 1, lane 12 handoff

Baseline: `3740505b0107af9a83053fd523ae1ad30be36465` (`HEAD`).

## Applied fix

Fixed `T3-15-1` in `launcher/electron/runtime-supervisor.cjs`. The stale-owner tunnel recovery branch now propagates its `startSignal` through:

- `waitForKnownTunnelStatus(config, 10_000, startSignal)` before deciding whether the stale tunnel is live;
- `runTunnelStopCommand(config, startSignal)` for the stale tunnel stop;
- `waitForTunnelStopped(config, 10_000, startSignal)` while confirming terminal state.

Each operation therefore uses the existing `assertRecoveryActive` and `runTunnelCommand` abort contract. A cancelled startup can settle the in-flight native control operation through the existing bounded cancellation path instead of continuing stale recovery after shutdown has begun. The final `assertCanStart(startSignal)` and verified-owner checks remain in place, and no broad process kill or public connector/ABI change was introduced.

## Coordination handoff

- **DID fixed:** `T3-15-1`.
- **Source path:** `launcher/electron/runtime-supervisor.cjs` only for implementation.
- **Public surface:** Native4, Native4DEV, ZeroRisk4, connector identities, tunnel command contract, and public ABI preserved.
- **Daemon generation:** no separate daemon-generation change was source-proven by the accepted T3-15-1 path; daemon ownership remains governed by the existing PID and health identity checks.
- **Verification:** no tests, typechecks, scripts, runtime/browser actions, production actions, or commits were run in this lane. Parent owns the final focused pass, limited to the shared `<=60s` / `<=10` scenario budget.
- **Limitations:** control-child retained ownership after unresolved SIGKILL and pipe-error settlement remains the previously reported T1-15-1/T2-15-1 lifecycle obligation; it was not expanded into this fix because it is a separate retained-child correction and would exceed the accepted T3-15-1 minimum path.
