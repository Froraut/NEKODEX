# OPEN upstream review 02 — browser completion

Date: 2026-09-15. NEKODEX checkout: `359d8fc` before concurrently owned tunnel edits. Scope: live issue bodies/comments for [#485](https://github.com/miuuyy/codex-chatgpt-web/issues/485), [#484](https://github.com/miuuyy/codex-chatgpt-web/issues/484), [#466](https://github.com/miuuyy/codex-chatgpt-web/issues/466), [#441](https://github.com/miuuyy/codex-chatgpt-web/issues/441), [#436](https://github.com/miuuyy/codex-chatgpt-web/issues/436). All five are issues, not PRs; no PR diff applies. This is source review only. The reports do not prove current NEKODEX runtime failures.

The [coding-iterations record](2026-09-15-coding-iterations.md#L29-L45) describes E13 environment persistence and other lifecycle repairs, but none asserts live resolution of these five browser/Manual symptoms. Its [remaining limits](2026-09-15-coding-iterations.md#L74-L80) include untested Windows and live model/connector combinations. This review uses the current source rather than treating the iteration report as runtime proof.

| Upstream item | Assessment | Direct NEKODEX source evidence and recommendation |
| --- | --- | --- |
| [#485](https://github.com/miuuyy/codex-chatgpt-web/issues/485) | **Insufficient evidence for a generic completion defect; conditional approval-dialog UX risk.** The original body claims browser-only turns with accepted prompt, visible response and completed tools, then no completed-turn action. The [author's correction](https://github.com/miuuyy/codex-chatgpt-web/issues/485#issuecomment-5670498985) says failing turns were **Full harness**, with a pending `Codex Native2` approval dialog; retest after approval was still underway. | Current worker recognizes an exact `Allow ChatGPT to use ${appName}?` dialog and waits up to 60 seconds or approves the one-shot action (`src/adapters/chatgpt-web/browser-worker.ts:845-884, 5071-5084`). Completion deliberately requires response text, stopped generation, and a visible action (`:1300-1310`); absent action receives a bounded DOM-health verdict (`:1587-1596`). **Recommendation:** if the same live symptom recurs, first capture the approval dialog and its exact label/checkpoint. Make a precise pending-permission diagnostic visible to the user; review matcher coverage only against an observed UI variant. Do not infer that dropping the completion-action requirement is safe: a pending tool approval can look idle. |
| [#484](https://github.com/miuuyy/codex-chatgpt-web/issues/484) | **Diagnostic opportunity; insufficient evidence of a current NEKODEX completion defect.** Body reports Zero Risk/Manual turns that remain `Working` after tool calls and ChatGPT-visible final text, plus repeated `trusted environment unavailable`. Its safe-log excerpt has no terminal event, but also does not establish whether the connector sent `safe_complete`, whether native activity remained outstanding, or whether the later environment warning caused the first stall. | Manual path waits for broker `safe_complete` or Launcher failure/cancel (`src/adapters/chatgpt-web/index.ts:598-630`). `completeSafeTurn` requires a nonempty final answer, `running` state, and no pending tool invocation or active MCP request (`src/adapters/chatgpt-web/turn-broker.ts:547-576`); the wait itself permits long work (`:601-610`). The Launcher already signals failure when the owner process exits (`launcher/electron/browser-host.cjs:1427-1444`). Its running tab is closable (`:564-581`), and close signals cancellation and invokes `cancelTurn` (`:1642-1661`); the separate guide's `Cancel turn` button is shown only in `awaiting-user`/`sent` (`launcher/src/App.tsx:1196-1205, 1295-1300`). **Minimum justified behavior:** keep accepted tool work and require the exact `safe_complete` contract for success; make a current failure/cancel/owner-exit signal terminal for the same trace, and diagnose any observed failure of that existing path. For a current stuck trace, establish whether `safe_complete` was sent/accepted or blocked by active work and whether close-tab cancellation reached the broker; only then repair the identified broken path. Later `environmentStore.resolve` warnings (`src/adapters/chatgpt-web/index.ts:860-870`) warrant identity-level investigation if correlated, not causal attribution from this excerpt. **Optional policy/UX:** expose a clearly labeled running-turn cancel action and progress/idle diagnostics; a global post-Sent timeout is not justified because legitimate Manual tasks may run long. |
| [#466](https://github.com/miuuyy/codex-chatgpt-web/issues/466) | **Insufficient evidence for a NEKODEX defect.** Windows Full harness issue says first message works, second cannot select model; final error and diagnostics are `NA`, so the failure layer (Codex catalog versus ChatGPT composer) is unknown. | NEKODEX rechecks the live model/effort control before every submission, including retained continuations (`src/adapters/chatgpt-web/browser-worker.ts:4779-4792`), and gives a specific error if the composer control does not become ready (`:2481-2495`). **Recommendation:** request or obtain a safe second-turn checkpoint and exact final error before altering model catalog or selector code. In a current reproduction, compare fresh versus retained turns and the `effort-control-ready` checkpoint. No blanket upstream patch is justified. |
| [#441](https://github.com/miuuyy/codex-chatgpt-web/issues/441) | **External ChatGPT service behavior, already handled at the bridge boundary; retry policy may still merit UX review.** The body says existing macOS long-running chats intermittently display ChatGPT's own `Something went wrong` and then retry five times. [Comments](https://github.com/miuuyy/codex-chatgpt-web/issues/441#issuecomment-5638813469) withdraw the initial “message too long” attribution. No local bridge defect is established. | The worker detects that terminal alert and reports `upstream_server_error`, retryable (`src/adapters/chatgpt-web/browser-worker.ts:827-842`), checks it while observing a response (`:5071-5073`), and the adapter retires retryable sessions to start a fresh surface while tracking retries (`src/adapters/chatgpt-web/index.ts:1417-1432`). **Recommendation:** keep the exact upstream error in the user-facing result and avoid repeated blind retries of the same long-context payload if current diagnostics show deterministic failures. Inspect the attached safe log only for a current reproducible case; do not label this as a NEKODEX completion bug. |
| [#436](https://github.com/miuuyy/codex-chatgpt-web/issues/436) | **Diagnostic opportunity; insufficient root cause evidence.** The upstream trace records a missing personalization menu, unpersonalized state and a 30-second deadline on a large multipart prompt. No deterministic current NEKODEX mismatch is demonstrated. A bounded failure when connector access cannot be proved may be the correct result; the GPU observation is unrelated and unsubstantiated. | Current preflight already has a bounded 30-second deadline and emits the same failure text (`src/adapters/chatgpt-web/browser-worker.ts:694-731`). When labeled controls are absent, it attempts exact connector proof, then structural toggle/proof/restore (`:583-628`); the `Unpersonalized` branch requires a verifiable choice (`:642-689`). **Minimum justified behavior:** preserve the deadline and fail explicitly without submitting a tool-capable prompt when connector access is unproven. On an exact current reproduction, record which proof/menu/toggle step consumed the deadline and whether cleanup restored state; fix only a demonstrated incorrect wait or misleading terminal reason. **Optional policy:** one fresh-surface retry only after recoverable state is established, with an explicit retry boundary. A blanket personalization bypass or automatic fallback is unsupported. |

Counts (five unique issues; no issue/PR duplication): **0 demonstrated current concrete defects**; **1 external service error already handled at the bridge boundary** (#441); **4 insufficiently attributed reports or diagnostic opportunities** (#485, #484, #466, #436). #485 offers a conditional permission-dialog diagnostic; #484 offers optional running-turn cancel UX. Neither an arbitrary Manual timeout nor an unproven personalization fallback is a justified patch. None is confirmed resolved by a current end-to-end NEKODEX run; no tests or code edits were performed in this lane.
