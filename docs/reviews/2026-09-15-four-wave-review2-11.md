# Review wave 2, lane 11 — browser connector selection

Frozen source: `3a66157a8943a80bbbe299699cc96bec82721ba1`. Manual read-only review of connector preflight/menu, `verifyConnectorExclusive`, and the prompt-attachment caller. Compared `2026-09-15-four-wave-review1-11.md` and the adjacent helper-verification and generation-4 reports. No execution, source edits, or account operation.

## New conditional defect (1)

**R2-11-1 — A failed menu-title observation becomes a missing-menu diagnosis (P2, conditional).** Trigger: after `@codex`, the exact configured row does not become visible within 2.5 seconds, and `allInnerTexts()` of visible `.__menu-item` rows rejects or exceeds its observation timeout (for example, an unresponsive DOM observation) while the turn signal has not aborted. `browser-worker.ts:3128-3145` catches *every* such non-abort error and substitutes `[]`; `:3316-3343` then cannot detect a visible retired/DEV identity or a hydrated alternate catalog and may retry until it reports `connector_not_found` with “menu did not open” (`:3147-3165`, `:191-197`). Consequence: the original observation failure is lost and the caller can misdirect recovery toward creating a connector. The direct caller at `:4900-4955` refreshes only on `ChatGptConnectorCatalogStaleError`, so this substituted empty list can also suppress its single catalog refresh. Counterevidence: when the exact row is visible, `:3309-3315` skips title observation; if the turn signal aborts, `:3139` rethrows; subsequent `appResult.count()` and selected-pill checks (`:3347-3397`) still fail closed. Source review does not establish how often the observation rejects in a live account.

## Repeated candidate (1), no new ID

**R1-11-1 remains conditional:** `browser-worker.ts:3222-3225` builds the exact-name locator from page-wide rows, while `:3249-3252`, `:3309-3315`, `:3318-3335`, `:3347-3379` use page-wide observations. Trigger: an exact-name sidebar history row is visible while the mention popup lacks that row, or both expose it. Consequence: false access proof in the no-labeled-control preflight branch, skipped missing-row diagnosis, duplicate-row failure, or a sidebar-driven stale/migration decision. Counterevidence: `:3390-3397` demands the exact selected composer pill before return and `:3401-3411` clears failures; `:1380-1385` already scopes the unrelated Think popup. No live reproduction or independent second root established. A retired/DEV title in the sidebar affecting the `:3318-3325` guidance is another manifestation of this same page-wide-row root, not R2-11-2.

## Challenge to wave 1 and boundaries

- Reject the broad reading that a sidebar hit *always* proves personalization: `browser-worker.ts:633-635` accepts an explicit Personalized button without invoking the catalog proof, and `:644-692` uses the owned personalization menu when Unpersonalized is labeled. The sidebar false proof applies to the structural/no-labeled-control branch at `:587-630` (and affects normal selection separately). `R1-11-1` remains a valid conditional candidate with that narrower trigger.
- Reject any inference that absent direct IPC means verification lacks settlement: `browser-worker.ts:2237-2241` queues the exclusive helper operation; `:3824-3843` returns the name only after selection and composer cleanup. For a real turn, `:4903-4927` awaits attachment, and `:2322-2336` awaits the aborted action's settlement on stage timeout. The adjacent `review1-10` describes the helper's correlated reply. A saved false readiness flag or missing live account-side schema proof is not, by itself, a runtime failure.
- Retained connector leases use the identity-aware launcher start (`:4433-4453`) and the retained attachment mode (`:1332-1338`, `:3426-3443`); this source review does not prove migration of a live retained task. Account-side generation-4 schema load/approval and the unreleased distribution remain known delivery limits in `2026-09-15-connector-generation4-results.md`. No optional improvement or public identity/schema change is proposed.

Counts: **1 new conditional source defect; 1 repeated R1 candidate; 1 narrowed/rejected broad first-wave interpretation; 1 known live-evidence limitation; 0 optional improvements; 0 confirmed live failures.**
