# Four-wave implementation wave 2, lane 16 — state publication

No further source change. The wave-1 `launcher/electron/state.cjs` diff already implements the minimal D03/D13 state contract: an explicitly false stored `coreSetupComplete` clears current catalog, picker and MCP proof on read and on each update, including a late positive patch while core remains false. An explicit negative catalog patch clears picker confirmation. Core becoming true by itself does not restore those proof flags.

Manual review of the current diff and direct callers found writer 06's monitor checks epoch, supervisor, setup state and configuration before writing catalog-positive proof; writer 15 supplies identity-bound health proof. Production startup failure paths publish core false and MCP false. Preserved historical verification timestamps, runtime installation metadata, DEV configuration semantics and persisted smoke fields have separate meanings. D06 session/persisted smoke invalidation remains explicit in the main/renderer owners; unrelated state-store updates must not erase a valid smoke pass.

Changed paths this wave: this report only. D03/D13 residual state-store fixes: none. Material concern for parent integration: state-store invalidation cannot reject a retired probe that returns while core remains true after a new setup; the epoch/config/identity checks in writers 06/15 remain required. No tests, typecheck, runtime, benchmark, production or commit were performed; the parent owns the final focused pass.
