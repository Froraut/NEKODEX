# NEKODEX triwave implementation wave 3 of 3, lane 11 — final codex integration pass

- **Date:** 2026-09-15
- **Baseline:** `3740505b0107af9a83053fd523ae1ad30be36465` plus the current uncommitted correction waves
- **Exclusive source scope:** `src/codex-integration.ts`
- **Result:** no source change; the current caller policy is the minimum concrete correction supported by this scope

## Review performed

I manually re-read the current diff in `src/codex-integration.ts`, every direct `readJournal()` caller in that file, the wave-1 and wave-2 lane-11 handoffs, lane-10 journal handoffs, the accepted D07 contract, and the T3-14 status-read correction. I also checked the direct setup, CLI, doctor, and protocol-discovery callers, the current journal `repair` contract, and the preserved connector identity boundary.

The current diff has exactly three caller-policy changes:

- `readCodexSubagentProtocol()` uses `readJournal({ reconcileInactiveHook: false, repair: false })`;
- `preflightCodexIntegration()` uses the same non-repairing read;
- `inspectCodexIntegration()` uses the same non-repairing read.

The remaining callers retain their intended lifecycle behavior:

- `installCodexIntegration()` and `activateCodexIntegration()` use the normal repairing journal read because they are state-changing lifecycle operations and immediately perform guarded integration writes;
- `deactivateCodexIntegration()` and `uninstallCodexIntegration()` retain explicit `reconcileInactiveHook: true`, which is the ownership-authorized recovery path for the proven active-primary/inactive-recovery transition;
- the inactive-hook guards remain in the lifecycle callers, so a normal inactive journal with a reappeared managed hook is preserved and rejected.

No caller-side omission or incorrect `repair`/recovery mode survived this pass. Adding `repair: false` to the lifecycle writers would change their intended transition semantics and would leave recovery/repair work for a later unrelated operation; no such change is justified.

## Cross-scope handoffs checked

- **D07 / T3-14-1:** the status and doctor path now reaches `inspectCodexIntegration()` with `repair: false`. Route/status inspection therefore does not invoke journal-copy repair, pending JSON-hook recovery, or one-sided journal creation. This closes the caller-side ownership defect in the present file.
- **D07 / explicit recovery:** disconnect and uninstall still opt into explicit recovery only; equal inactive journal copies do not authorize hook removal.
- **T2-10-2:** the remaining compensation ownership condition is in `src/codex-integration-journal.ts` / `src/codex-integration-shared.ts`, especially the shared rollback behavior for an unguarded write that this operation may not have started. It cannot be repaired by changing this caller file without changing lifecycle transaction semantics, so no out-of-scope edit was made.
- **Lane 11 DEV finding T3-11-1:** duplicate DEV `call_id` handling belongs to `src/dev-chat/driver.ts`, outside this exclusive scope; no related source change was made.
- **Native4 / Native4DEV / ZeroRisk4:** no connector name, App ID, tool schema, tool count, route contract, or generation-4 ABI was changed or proposed for change.

## Exact residual limits

1. This is a bounded manual source review only. No tests, typecheck, scripts, runtime/browser/account probes, production actions, release work, or commit were run.
2. Source review does not prove filesystem scheduling, crash timing, or cross-process behavior. The compare-before-write window in the shared compensation primitive remains a known limitation.
3. One-sided journal-copy creation still uses the shared atomic-write path and has no absent-file compare-and-create ownership guard in this scope.
4. The uninstall sequence still cannot infer writer provenance when an external actor inserts byte-for-byte identical managed hook content between the final absence check and journal removal. Changed or foreign content remains fail-closed.
5. No source result here proves live ChatGPT account authentication, connector attachment, picker rendering, Native4/Native4DEV/ZeroRisk4 availability, or end-to-end model behavior.

## Final disposition

- **Source files changed in this wave:** 0
- **Review document written:** this file only
- **New concrete defects in `src/codex-integration.ts` after the current waves:** 0
- **Unresolved in-scope defects requiring a source edit:** 0
- **Out-of-scope residuals:** the shared compensation/ownership limits listed above
- **ABI/identity regressions:** 0
