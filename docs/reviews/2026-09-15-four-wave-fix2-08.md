# Wave 2 final correction — lane 08 (`3a66157` plus wave 1)

Changed paths: `src/adapters/chatgpt-web/browser-worker.ts` and this report. No direct caller or other owner's file changed.

**D14:** Kept the wave-1 visible `.popover` row scope, uniqueness gate, exact connector name/count, keyboard `data-highlighted`, and exact selected composer pill. Added a pre-trigger check that no visible popup already exists after clearing/focusing/settling the composer, for both the structural personalization proof and normal selection. Also require the literal `@codex` mention to remain in that composer after triggering, on both the visible-row and missing-row paths. This closes the conditional case where a popup already present before typing could provide a false catalog row. A retained foreign popup now produces a diagnostic error and the existing cleanup path, rather than supplying connector proof.

**D15:** The wave-1 title observation already retains the original failure as `cause` and does not convert it to an empty menu; no further change to that catch was needed. Empty, successfully observed titles still indicate that no visible menu rows were found. Only the exact-row wait timeout enters the missing-row/catalog-refresh path.

The popup structure follows the source-observed Think slash menu (`.popover` and nested visible `.__menu-item[tabindex="0"]`), without inventing a connector-specific selector. Freshness, uniqueness, and an attached mention narrow popup provenance, but source DOM alone cannot prove that a foreign popup opened concurrently with the mention is owned by that editor. Such a popup with the same exact row remains a conditional false-proof concern if it is the sole visible popup; the exact selected-pill check still prevents successful wrong-connector attachment. The focus flag is recorded in an integrity error but not a gate: source does not establish that a mention menu always leaves DOM focus on the editor. A connector menu outside `.popover`, a popup remaining open after composer clearing, and concurrent multiple visible popovers fail closed. Account-specific live DOM evidence would be needed to refine this bound.

Manual review covered the proof, timeout/title failure, catalog-refresh, count, highlight, selected-pill, and cleanup branches. `git diff --check` passed for the assigned source. Per instruction, no tests, new test files, typecheck, Playwright runtime, benchmark, production action, or commit were performed. Public Native4/ZeroRisk4 ABI pins and helper API signatures were untouched.
