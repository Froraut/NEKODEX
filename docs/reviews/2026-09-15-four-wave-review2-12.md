# Four-wave review 2, fresh Sol lane 12 — retained turn lifecycle

Frozen source: `3a66157a8943a80bbbe299699cc96bec82721ba1`. Read-only manual review of `src/adapters/chatgpt-web/index.ts`, `turn-execution.ts`, and direct cancellation/HTTP caller `src/server.ts`; challenged `2026-09-15-four-wave-review1-12.md` and consulted the adjacent first-wave lifecycle/helper report (`review1-13.md`) for the physical-settlement boundary. No tests, typechecks, scripts, source changes, installation, or publication.

## Findings

**0 new concrete defects; no `R2-12-n` assigned.** No inspected path established a new trigger, incorrect settlement, and consequence beyond the candidates and limits already recorded in R1. There is no R1 duplicate to relabel as a new finding.

## Trace and challenge

- An explicit native interruption reaches `ChatGptTurnSessions.cancelNativeTurn` from `src/server.ts:1022-1037`, while the HTTP stream is separately aborted via `HttpTurns.beginCancelTurn` (`src/server.ts:175-189`). The hook returns its counts before cleanup, but observes all three settlement promises and logs rejection (`src/server.ts:1034-1053`). Thus fast acknowledgement is **not** proof of finished cleanup, and lack of synchronous IPC settlement is **not** proof that cleanup was dropped. An exact replacement owner waits on `ownerRetirements` or the prior session's physical settlement (`turn-execution.ts:666-703, 964-977, 1044-1065`). `cancellableBrowserTurn` keeps `physicalSettlement` tied to the underlying worker promise, irrespective of the fast cancellation race (`index.ts:93-120`).
- Client observer abort is terminal for Manual/Zero Risk mode and revokes its capability (`index.ts:1404-1415`); an automatic DOM-observed turn instead remains addressable by exact execution/round keys. Generic error handling differentiates submitted failures and deterministic terminal replay from retryable retirement (`index.ts:309-328, 1417-1458`). An unavailable optional capability flag does not, by itself, demonstrate runtime failure: the non-tool branch still starts a browser worker (`index.ts:689-725`), while Manual mode resolves its own tool capability (`index.ts:415-425`).
- Retained epoch closure waits on all matched physical settlements, calls the separate Launcher release callback, and only then detaches/removes entries (`turn-execution.ts:795-886`). An acknowledgement rejection keeps the obligation attached for the next owner or compaction to retry (`:604-609, :648-658, :758-767`); compaction retires the epoch before returning the summary and preserves only a settled final response (`index.ts:983-1099`; `turn-execution.ts:779-792`). This supports the central R1-12 finding of **no concrete lost retained release**. The adjacent R1-13 helper report's `turn_settled`/launcher-finally evidence is compatible with this separate release acknowledgement; neither frame alone substitutes for it.

**Qualification of R1-12, not a rejected defect claim:** R1-12 says failed acknowledgements remain retryable through a next owner. Its cited `closeConversationAndWait` obligation covers retirement and compaction. Model switching uses `releaseOtherOwnerConversations` (`turn-execution.ts:707-744`): if one of multiple distinct callbacks succeeds and a later one rejects, the entries stay attached and the next switch retries the entire callback sequence. A prior successful callback can therefore be invoked again. No source evidence here establishes that the Launcher callback is non-idempotent or that this produces lost state; classify as an optional simplification/conditional operational limit, **not** `R2-12-1`. R1-12's general retryability claim still holds because the failed path does not detach ownership.

Known proof boundary: in-memory obligations, fast abort and source-level fences do not establish survival across daemon restart or a live account transition. No public schema or Native4 / Native4 DEV / Zero Risk4 identity change follows from this review.

Counts: **0 new defects, 0 R1 duplicates elevated, 0 first-wave defect claims rejected, 1 qualified optional path, 1 known proof boundary**. Manual source review only.
