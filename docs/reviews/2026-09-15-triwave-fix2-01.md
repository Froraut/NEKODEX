# NEKODEX triwave fix2 of 3, lane 1 — setup and rollback

Baseline source: `3740505b0107af9a83053fd523ae1ad30be36465` (`3740505`), reviewed with the current uncommitted wave1 edits.

Scope was exclusive to `src/setup.ts`. This was a manual source review of the actual current diff, the setup transaction, direct service/tunnel compensation, launcher-owned handoff, direct DEV setup, final Codex route commit, status-unknown handling, and concurrent-edit guards. No tests, typechecks, scripts, runtime or account actions, production actions, release actions, or Git commit operations were performed.

## Result

- Source changes in this lane: **none**.
- New residual defects: **0**.
- Accepted triwave findings still requiring a change in `src/setup.ts`: **0**.
- Optional redesigns: **0**.
- Native4, Native4DEV, ZeroRisk4 identities, public tool schemas, and ABI pins: **unchanged**.

The current wave1 edit in this file is correct and was retained:

- `bootstrapRestoredDefinition()` uses the current user launch domain and `runChecked("launchctl", ["bootstrap", "gui/<uid>", path])`.
- Service rollback restores the captured service bytes and bootstraps that exact path. It no longer regenerates the definition from `existing`.
- Tunnel-service rollback follows the same exact-byte path.

That closes accepted `T2-1-1`: an initially loaded but byte-divergent service or tunnel plist can now be restored byte-for-byte after a later setup failure. The direct bootstrap does not reserialize the persisted config and therefore preserves pre-existing plist fields. Existing checks still require known loaded state, safe config rollback, and unchanged setup-owned bytes before compensation.

## Cross-scope review disposition

1. **Service rollback and tunnel rollback:** the branches keep independent snapshots and compensation gates. A service compensation failure is recorded and does not prevent the later tunnel branch from attempting its own guarded recovery. The tunnel fallback still avoids removing a client or runtime key while a validation tunnel may be using them.

2. **Launcher-owned handoff:** setup may remove the prior terminal-owned service and tunnel service before the launcher-owned config is committed. The rollback path records those removals, restores the exact prior definitions, and uses the exact-path bootstrap for definitions that were loaded before setup. No regenerated definition path remains in this file.

3. **Final Codex route commit:** a failure in `installCodexIntegration()` reaches the existing setup compensation path. Config, service, tunnel definition, profile, client installation, and runtime key are each protected by their own snapshot and ownership checks. No new cross-scope loss path was found in the current source.

4. **Concurrent edits and unknown status:** current bytes and loaded state are checked before rollback. A changed setup-owned file, an unknown service/tunnel state, or an unsafe config rollback fails closed and records manual recovery rather than overwriting the observed state. The wave1 exact-definition change preserves these guards.

5. **DEV setup versus production setup:** direct DEV setup still refuses an existing running or indeterminate Full alias before its first owned mutation and compensates its own confirmed writes. The interval between a successful stopped observation and the first mutation remains a distributed ownership race. It is the previously documented limitation; this lane found no new consequence or minimal correction in `src/setup.ts` that would provide an atomic owner/idle-drain handshake without an optional redesign or changes outside the exclusive scope.

6. **Status probes and service helper behavior:** ordinary unloaded-service results remain handled by the existing helper contract; thrown probe failures are treated as unknown. The current setup code does not convert an unknown status into an assumed ownership result.

## Remaining limits

- No live launchd failure, concurrent external edit, interrupted setup, DEV alias restart, or final route failure was reproduced. The dispositions above are based on source ordering and ownership guards.
- The DEV stopped-observation-to-first-mutation window remains non-atomic and can only be closed with a broader ownership/idle-drain coordination design.
- Exact-path re-bootstrap still depends on the current macOS `gui/<uid>` launch domain and fails closed if launchd rejects the restored bytes.
- Source review does not establish account-side connector approval, picker rendering, or a completed Native4/Native4DEV/ZeroRisk4 turn.

No source edit was warranted after the current-diff and interaction review. This report is the only new artifact for wave2 lane1.
