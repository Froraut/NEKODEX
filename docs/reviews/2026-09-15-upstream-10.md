# Upstream lane 10 — native tools and retired-handle scrubbing

Reviewed 2026-09-15 against NEKODEX `359d8fc` (source review only). The four numbered items are **four open PRs**, not separate issue reports. The associated #446 and #342 are context cited by the PR authors; they are not additional lane findings. Live GitHub PR bodies and file patches were fetched with `gh api repos/miuuyy/codex-chatgpt-web/issues/{number}` and `/pulls/{number}/files`. No code, tests, install, runtime, or production state was changed. The current `src/tunnel.ts` and `src/tunnel-service.ts` edits by other lanes were not inspected or touched. [Coding iterations](2026-09-15-coding-iterations.md) establishes earlier development verification boundaries, not installed runtime proof for these upstream scenarios.

| PR | Upstream claim and diff | NEKODEX source evidence | Disposition and recommendation |
| --- | --- | --- | --- |
| [#442](https://github.com/miuuyy/codex-chatgpt-web/pull/442) | `prompt.ts` replaces the three-kind, open-length regex with six kinds, an exact 32-character body, and JSON-escape-aware left boundaries. Patch also adds a near-miss regression case. | `src/adapters/chatgpt-web/prompt.ts:146-158` already contains the six-kind, exact-length, escape-aware scrubber. It is applied to record, multipart, and envelope JSON at `:326`, `:395`, and `:652`. `tests/prompt-contract.test.ts:552-585` documents escape-boundary and near-miss cases. | **Already addressed in current source.** Do not apply the upstream regex blindly. Static source and prior focused test evidence do not prove all installed replay paths or future broker ID formats. |
| [#430](https://github.com/miuuyy/codex-chatgpt-web/pull/430) | `mcp-server.ts` adds `codex_read_thread` with read-only annotations, exact outer `mcp__codex_app__read_thread` routing and unavailable-tool error; the public connector ABI changes. Body reports an installed High-mode before/after task read after connector refresh. | `src/adapters/chatgpt-web/mcp-server.ts:26`, `:124-129`, `:763-801` implements this action and rejects ambiguous/freeform advertised tools. `src/adapters/chatgpt-web/prompt.ts:509` directs task reads to it; `src/config.ts:40-45` documents connector identity migration. | **Already addressed in current source, with a stronger exact-tool guard.** Keep the dedicated action. The reported upstream safety refusal occurred before connector dispatch and was an external-service behavior; the upstream success is specific to one installed account/turn and does not prove general refusal prevention in NEKODEX. |
| [#451](https://github.com/miuuyy/codex-chatgpt-web/pull/451) | `mcp-diagnostics.ts` observes `tools/call` before SDK input validation and correlates replies, including validation failures. Patch fingerprints argument shapes, inventory schemas and error text; `mcp-server.ts` wraps stdio transport. Body says a webpage later stopped with unknown cause. | `src/adapters/chatgpt-web/mcp-diagnostics.ts:14-61` already observes receipt and reply/send failure, logs only an allowlisted tool name, outcome/code/elapsed time, and preserves transport replies. Wired at `src/adapters/chatgpt-web/mcp-server.ts:990`. `tests/mcp-transport-evidence.test.ts:5-24` describes the boundary/secret-omission case. | **Core diagnostic gap already addressed.** Current evidence gives protocol error code (for example `-32602`) rather than upstream's named `input_validation_error` category and inventory fingerprints. That richer telemetry is an **optional feature**, contingent on a concrete diagnostic need and privacy review. Neither implementation explains or fixes an upstream safety rejection/webpage stop. |
| [#464](https://github.com/miuuyy/codex-chatgpt-web/pull/464) | Draft PR adds `codex_read_text_file` through a fixed Python program, path/offset/max-byte input (64 KiB cap), read-only annotations, UTF-8 chunk handling, native `exec_command` routing with `/bin/sh`, and explicit Windows failure. Body reports one installed Pro/ultra synthetic file read after connector refresh; prior rejection details are unavailable and failures intermittent. | `src/adapters/chatgpt-web/mcp-server.ts:20-30`, `:625-660` exposes generic `codex_exec` (destructive/open-world annotations) but no dedicated text reader. `src/adapters/chatgpt-web/prompt.ts:504-509` gives no text-reader instruction. The [coding-iterations report](2026-09-15-coding-iterations.md) contains no implementation or installed read-only text-tool proof. | **Useful optional feature, not a confirmed NEKODEX defect.** A task needing ordinary UTF-8 file reads may benefit from a narrow annotated action, but the cited safety rejection is external and lacks raw cause evidence. If prioritized, design for actual advertised native command schemas, bounded output, explicit platform/interpreter failures, chunk continuity and connector ABI migration; preserve the outer sandbox/approval/refusal path. Review PR implementation independently before adoption. |

Counts: **3 core behaviors already addressed** (#442, #430, #451); **1 absent optional action** (#464); **0 confirmed concrete current-source defects** from these four PRs; **2 external-service symptom claims with insufficient causal evidence** (#430 and #464), plus the unexplained webpage stop in #451. #451's richer telemetry is a separate optional enhancement. These counts are PR-level, deduplicated from their related issue references.

Priority: leave #442/#430/#451 source as-is. Consider #464 only if a real NEKODEX task reproduces refusal of an ordinary read or product requirements call for a dedicated text reader. Before any implementation, resolve its POSIX `/usr/bin/python3` assumption and connector refresh path for this app. No report here asserts runtime success for NEKODEX or general platform safety behavior.
