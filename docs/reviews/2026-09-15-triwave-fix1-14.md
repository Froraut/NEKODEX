# Implementation wave 1 of 3, lane 14 — browser-worker handoff

Baseline: `3740505b0107af9a83053fd523ae1ad30be36465` (HEAD).

## Changed source

- `src/adapters/chatgpt-web/browser-worker.ts`

## Fix

The connector mention retry loop now calls the existing `clearChatGptComposerState(page)` after a visible popup was observed without the configured exact connector row, when the retry budget remains.

This clears a popup that ChatGPT left visible before the next mention trigger. Cleanup remains fail closed: it uses the existing Escape/composer cleanup path and verifies both empty visible composer text and no configured selected connector pill. The retry budget remains shared and bounded. The exact visible popup, exact row, keyboard highlight, and selected-pill guards are unchanged.

## DIDs / handoff

- **T3-13 popup/connector retries:** addressed at the source-proven retry boundary where a prior popup can block the next `assertNoPriorPopup()` call before another trigger.
- **D14 residual:** addressed only for the concrete stale-popup retry path. The existing visible `.popover` scope, exact-row proof, highlight gate, and selected-pill guard remain intact. No unobserved popup ownership selector was added.
- The reports' account-dependent foreign-popup provenance limit remains a limitation; this change does not claim composer-to-popup ownership.

## API and scope

- Private method signatures are unchanged.
- Native4, Native4 DEV, ZeroRisk4 names and public ABI are unchanged.
- No other lane source file was edited.
- No tests, typecheck, scripts, runtime/account checks, production actions, or commit were run.
- Parent owns the final focused verification within the requested <=60 seconds and <=10 scenarios.
