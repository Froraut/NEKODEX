# Implementation wave 1 lane 12 — parent-collected handoff

Implemented lane 12’s D06 and D13 changes in [launcher/src/App.tsx](/Users/alex/Dev/nekodex/launcher/src/App.tsx). That is the only path I changed.

For **D06**, the smoke row now uses the persisted pass only when its version matches the current app version. State events, initial hydration, explicit state updates, and snapshot refresh replace the displayed value instead of preserving an earlier `true`. This also keeps an older same-process IPC snapshot from making the row look passed after migration clears its evidence. The renderer change depends on lane 06 clearing its retained session flag so backend snapshots and setup checks agree.

For **D13**, Automatic tool displays treat a failed production `runtime-start` operation as lacking current tool proof while the owning state update arrives. The recorded “Last connector verification” time remains visible as historical evidence; the Manual local-verification path keeps its separate meaning. This complements the failure-state invalidation owned by lanes 06 and 16 and the Overview display owned by lane 13.

I reviewed the scoped diff and ran `git diff --check`; it was clean. I ran no tests, typechecks, benchmarks, suites, or release steps. These are source-path fixes, not a live account or runtime reproduction. No public tool schema, ABI pin, identity, or i18n file changed.

The requested `docs/reviews/2026-09-15-four-wave-fix1-12.md` remains unwritten because the assignment gives this lane exclusive write ownership of `App.tsx` and requires outside-file work to go to the parent. The paragraphs above are the concise lane report for that file.


