# Implementation wave 2, lane 03 — D02 tunnel helper review

Baseline `3a66157` plus wave 1 uncommitted changes. Source owner: `src/tunnel-service.ts`. No additional source change is needed.

The existing optional direct callback `installTunnelService(config, onDefinitionWritten)` already fires synchronously after a changed/new plist is written and before `launchctl bootstrap`. Its `{ path, data }` describes the installed path and exact generated UTF-8 definition. A matching definition skips the callback, and a loaded service with a mismatched/missing definition still fails closed before writing. The setup caller passes `onTunnelDefinitionWritten` at its forward installation and snapshots the owned bytes; the one-argument rollback installation remains intact. Stop polling and readiness bounds are unchanged. D02's write-before-failed-bootstrap helper requirement is satisfied.

Material concern for parent / setup owner (outside this lane): `src/setup.ts` calls `getTunnelServiceStatus()` inside the forward install's `finally` and in rollback ownership checks. If the launchctl status probe itself times out, that throw can mask the primary bootstrap exception or interrupt compensation; resolve in setup if this is within the final focused case. Also verify its stop/removal checkpoints against ownership of the prior loaded state. Do not change the helper's status timeout to hide this case.

Manual review covered this helper's write guard/order, `atomicWriteFile` completion semantics, forward and rollback setup calls, and direct CLI removal. No tests, typecheck, runtime/production calls, benchmark, suite, dependency changes, or commit were performed. Changed path in this lane: this report only.
