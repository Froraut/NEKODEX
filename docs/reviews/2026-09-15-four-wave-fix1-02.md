# Implementation wave 1, lane 02 — service helper

Baseline: `3a66157`. Addressed D01 in `src/service.ts`; reviewed D12 without changing the cleanup helper.

`installService(config, onDefinitionWritten?)` accepts an optional synchronous callback `(definition: { path: string; data: string }) => void`. It fires immediately after this helper successfully calls `atomicWriteFile(path, next)`, before `getServiceStatus()` or `launchctl bootstrap`. `data` is the exact generated plist string written at `path`. It does not fire if the existing definition already matches, or if the write throws. Existing one-argument callers retain their behavior. A callback exception propagates before status/bootstrap; the setup owner should make its checkpoint callback non-throwing and retain the write snapshot even if a later operation fails.

D01's rollback and loaded-state compensation belong to writer 01 in `src/setup.ts`; this helper supplies only the owned-write event. D12's `removeLegacyRuntimeArtifacts` still refuses removal when the active runtime command references either artifact, and filesystem cleanup errors still propagate. Writer 01 owns post-commit warning/result handling; no setup file was edited here.

Changed paths: `src/service.ts`, this report. Manual diff review checked write ordering, no-write branch, direct callers and the cleanup refusal guard. No tests, typecheck, benchmark, installation, publication or ABI/schema changes were performed.
