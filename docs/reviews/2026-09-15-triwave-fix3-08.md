# NEKODEX — final implementation wave 3 of 3, lane 8 — browser worker

- **Date:** 2026-09-15
- **Baseline:** `3740505b0107af9a83053fd523ae1ad30be36465` (`3740505`), with the accepted wave 1 and wave 2 working-tree edits present.
- **Exclusive source scope:** `src/adapters/chatgpt-web/browser-worker.ts`.
- **Review mode:** final adversarial manual source review. No tests, typecheck, scripts, runtime/browser/account actions, production actions, commit, or publication were run.
- **Contract boundary:** `Native4`, `Native4DEV`, `ZeroRisk4`, connector identities, public schemas, tool registrations, and generation-4 ABI pins were preserved.

## Final disposition

**No source change. No new concrete browser-worker defect was established.**

The current scoped diff contains the accepted wave 2 correction at `browser-worker.ts:3376-3382`: when a trigger opens a visible mention surface but no exact configured row becomes visible, and the branch will retry, the worker clears and verifies the complete composer state before the next trigger. This is correctly placed after the catalog-refresh decision and the final-attempt failure branch. It prevents a stale mention popup or connector pill from contaminating the next attempt while preserving the existing success gates.

## Adversarial paths reviewed

### Retry cleanup and attempt accounting

The retry path increments the shared `triggerAttempts` budget once per trigger, observes the popup rows, and invokes `clearChatGptComposerState(page)` only when another trigger is permitted. The next loop reacquires the active composer, so it does not reuse a locator for the replaced Lexical subtree. Cleanup is fail-closed: it dismisses the overlay, clears the composer, waits for UI settling, and verifies both empty composer text and absence of the configured connector pill.

A cleanup failure is propagated as a persistent browser-state failure through the existing outer selection catch. A failed cleanup therefore cannot be converted into another attempt or connector success.

### Catalog refresh and known identity mismatches

A stale-catalog condition still throws `ChatGptConnectorCatalogStaleError` before the new retry cleanup when its existing refresh conditions are met. The caller owns the single page reload and rebuild, then disables the refresh opportunity. Legacy and production-versus-DEV identity mismatches still fail through their dedicated error paths; the outer catch owns final composer cleanup. The new line does not consume or bypass the refresh handoff.

### Selection, attachment, and verification handoffs

`attachPrompt` receives a composer only after `selectConnector` returns a verified selected composer. It then owns the selected pill and prompt insertion as one transaction and retains its existing cleanup on attachment failure. Connector verification still clears the selected composer after selection and before reporting success. The changed retry branch cannot return a partially selected composer, and it does not alter the shared attempt budget used across compaction or catalog-refresh handoffs.

### Popup scope and observation failures

The current visible `.popover` scope, one-popup ambiguity rejection, exact-row count, keyboard-highlight requirement, selected-pill proof, and observation-error cause preservation remain intact. The retry cleanup does not broaden popup selectors or reinterpret a failed observation as an empty menu.

## Exact residual limits

1. **Popup provenance remains account-DOM dependent.** If a sole foreign visible `.popover` appears after the zero-popup precheck and exposes a same-named exact row, structural personalization proof cannot prove popup ownership from the current DOM evidence. Normal selection still requires the exact selected composer pill. No stable composer-to-popup ownership marker is available in this source.
2. **The maximum trigger budget is not a guarantee of three completed triggers.** If ChatGPT leaves a popup visible after cleanup, the next `assertNoPriorPopup()` fails before another trigger. The source fails closed and reports the persistent state; it does not promise three completed attempts.
3. **Unsupported or changed DOM shapes fail closed.** A connector surface outside the observed visible `.popover` structure, multiple visible popovers, missing exact rows, missing keyboard highlight, a failed bounded DOM observation, or a cleanup that cannot prove an empty composer remains an explicit failure path.
4. **Bounded cleanup can remain externally timing-sensitive.** A page or Chromium process that does not respond within the existing cleanup deadline can produce a persistent-state error. The source preserves the primary failure and cleanup failure boundary; manual review cannot prove host-specific teardown timing.
5. **Live service behavior is unverified here.** This review does not prove ChatGPT account connector creation, connector approval/schema availability, live DOM variants, CDP timing, retained-turn behavior, installed-app readiness, or end-to-end browser success.

## Source and verification boundary

The only source path inspected for possible modification was `src/adapters/chatgpt-web/browser-worker.ts`; it was left unchanged during this final pass. Cross-scope handoffs were read for the direct helper-verifier, account-pool, prompt-attachment, catalog-refresh, and connector-verification boundaries, but no file outside the exclusive source scope was edited. This report is the requested review artifact.

**Counts:** 0 new concrete defects; 0 source changes; 1 accepted retry-cleanup correction retained; 5 exact residual limits; 1 cross-scope handoff review; 0 ABI changes.
