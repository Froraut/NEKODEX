# Review wave 1, lane 11 — browser connector selection

Baseline: `3a66157`. Scope: `src/adapters/chatgpt-web/browser-worker.ts` connector preflight, mention menu, verification and direct turn caller; `src/config.ts` identity constants/mapping; generation4 and tunnel-upstream result boundaries. Manual source review only. No tests, typechecks, live account operation or code changes.

## Potential finding (1)

**R1-11-1 — Connector mention search is not scoped to its popup (P2, conditional).** Trigger: a visible sidebar history row uses the same `.__menu-item[tabindex="0"]` class and is titled exactly `Codex Native4` (or the configured connector name), while `@codex` has no matching connector row, or both rows are visible. The connector path creates `menuRows` from the whole page and `appResult` from those rows at [browser-worker.ts](../../src/adapters/chatgpt-web/browser-worker.ts#L3222). The personalization proof at lines 3249–3252 can treat the sidebar row as connector access; the actual selection at lines 3309–3315 can treat it as a visible menu row or fail the exact-row count at lines 3347–3355 when both rows exist. With only the sidebar row, keyboard highlight at lines 3362–3379 likely fails after unrelated ArrowDowns, resulting in a misleading connector-selection error despite the true missing catalog entry. The same file explicitly records that slash popups share these classes with sidebar history and scopes the Think command to a visible `.popover` at lines 1380–1385. Generation4 names make an old/new connector title in conversation history a plausible migration-support scenario, but this is **not a live reproduction** and depends on the actual sidebar row matching the exact text predicate. Counterevidence: the final keyboard-highlight gate and exact selected-pill keyword check at lines 3362–3397 prevent an ordinary sidebar false positive from silently submitting a turn under the wrong connector; failure cleanup at lines 3401–3411 clears the draft. Suggested implementation lane: scope the connector row/count/title observations to the owned, visible mention popup before proof and selection, retaining the exact-name, highlight and pill checks. No identity or schema change is justified.

## Reviewed without a defect claim

- The generation4-specific alias mapping at [config.ts](../../src/config.ts#L24) and `legacyConnectorInMenu` at [browser-worker.ts](../../src/adapters/chatgpt-web/browser-worker.ts#L3168) distinguish Native4, Native4 DEV and Zero Risk4. If a visible old mode-specific connector is encountered without the exact new one, lines 3318–3343 fail with migration guidance; when a nonlegacy catalog row is visible, the direct turn caller permits one document refresh at lines 4899–4955. Verification deliberately does not submit a turn and clears the selected draft at lines 3814–3843. No separate defect established in this identity path.
- The direct turn caller uses `retained` rather than a fresh mention for a reused connector lease ([browser-worker.ts](../../src/adapters/chatgpt-web/browser-worker.ts#L1332), lines 3426–3443 and 4433–4512). The documented migration says old retained leases are not relabeled; source-only inspection here does not prove a retained live task succeeds across migration.
- The generation4 results document says ChatGPT account-side creation/schema loading and live approval remain unverified; the tunnel/upstream results distinguish optional custom-name compatibility and personalization reports from confirmed defects. Their absence of live proof is a delivery limit, not another finding.

Counts: 1 conditional potential finding; 0 confirmed live defects; 0 optional improvements proposed independently; 0 code changes. Review path: `docs/reviews/2026-09-15-four-wave-review1-11.md`.
