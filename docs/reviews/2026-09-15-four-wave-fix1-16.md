# Four-wave implementation wave 1, lane 16 — state publication

Baseline `3a66157`. Addressed D03 and D13 in `launcher/electron/state.cjs`: when stored `coreSetupComplete` is explicitly false, both persisted reads and subsequent store updates clear current catalog, picker and MCP verification flags. A late catalog-positive patch cannot restore current proof while core remains false. An explicit catalog-negative patch still clears picker confirmation. Historical `setupVerifiedAt` / `pickerVerifiedAt`, installed-runtime metadata, DEV configuration meaning and persisted smoke evidence remain intact. D06 smoke invalidation stays with the migration owner; unrelated state updates do not invalidate a valid smoke pass.

Cross-lane contract for writer 06: publish a core failure using `stateStore.update({ coreSetupComplete: false, ... })`; returned state and `stateStore.read()` have `codexCatalogVerified: false`, `codexPickerConfirmed: false`, and `mcpSetupComplete: false`. Do not depend on this store for probe epoch/config/runtime identity: writer 06 must reject retired catalog probes before a positive update and writer 15 supplies identity-bound health proof. Writer 12/13 should distinguish these current false flags from preserved historical verification times and external-runtime ownership.

Manual diff review only, as requested. No tests, typechecks, scripts, runtime probes, ABI/schema changes or publication. A previously persisted smoke pass remains valid until an explicit migration/version/identity invalidation elsewhere; state store does not infer one. A core-positive update alone does not restore catalog or MCP proof.
