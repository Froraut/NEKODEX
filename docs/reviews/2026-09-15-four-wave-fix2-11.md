# Implementation wave 2, writer 11 — D07 caller policy

Baseline `3a66157` plus wave-1 uncommitted implementation. Source changed: `src/codex-integration.ts`; this report is the authorized additional path. No tests, typecheck, runtime, release, or commit. Manually reviewed the assigned diff and direct `readJournal`/CLI/doctor/setup callers under the shared verification limit.

- Added `assertJournalTargetsConfig(journal, getCodexConfigPath())` immediately after the explicit recovery read in `uninstallCodexIntegration()`. Identical journal copies can be returned without validation by `readJournal`; an inactive copy targeting another config must not proceed into uninstall's config/cache/journal removals. This closes a residual caller guard without changing the public ABI.
- Follow-up on lane 10's second review: `uninstall` now compares each journal copy with its snapshotted bytes before removal, removes the primary first, checks inactive hook absence again, and removes recovery last. A hook or journal change observed between these steps aborts while recovery evidence remains; the existing expected-state rollback can restore the removed primary without overwriting a later edit. Neither inactive hook nor foreign journal bytes are blindly cleaned up.
- Wave-1 D07 behavior otherwise stands: ordinary inspection, protocol/model and setup reads use `reconcileInactiveHook: false`; explicit deactivate/uninstall use `true` only for writer 10's proven active-primary/inactive-recovery pending disconnect. Reappeared exact or occupied foreign inactive JSON hooks fail closed. The current writer-10 code independently checks restored config and hook state for the pending pair.

Final disposition: D07's ordinary-read no-delete policy is accepted. The path guard and bounded journal-removal checks above are implemented. A noncooperating external actor can still insert an identical hook after the final absence check and before the last recovery-copy removal; without cooperation, the check/delete gap remains. This is a documented boundary, not a request to add a cross-process lock or revise the ownership protocol. Parent owns the final focused cases and commit. No outside-owner files were edited.
