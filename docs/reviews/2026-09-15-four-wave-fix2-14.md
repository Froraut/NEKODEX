# Wave 2 final correction, lane 14 — CLI

Baseline `3a66157` plus uncommitted wave 1. Assigned source: `src/cli.ts`. D19 and D12 need no additional source edit after direct-path manual review.

- **D19:** `tunnel status` checks `runtime.ok` for launcher ownership; terminal ownership also requires `service.running`. The printed `{ service, runtime }` result and `start`/`restart`/`stop` service-control branches retain their existing behavior. `src/tunnel.ts` defines `ok` as runtime readiness and `src/doctor.ts` treats an absent launcher launchd service as normal.
- **D12:** CLI consumes `SetupResult.warnings ?? []` after `setup()` resolves, prints each post-commit cleanup warning, and leaves successful exit status untouched. `src/setup.ts` now declares the optional field and populates it for post-commit legacy cleanup failure while returning committed success. Connector/restart guidance remains after warnings.

Changed paths in this wave: this report only. Wave 1's existing `src/cli.ts` diff is retained unchanged. No material residual concern identified within the assigned CLI behavior. No tests, typecheck, suite, benchmark, runtime check, production action, or commit; parent owns the single shared focused pass. Native4/Zero Risk4 public pins and ABI remain untouched.
