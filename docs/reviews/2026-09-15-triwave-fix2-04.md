# NEKODEX implementation wave 2 of 3, lane 4 — MCP gateway review

Baseline source: `3740505b0107af9a83053fd523ae1ad30be36465` (`3740505`), reviewed together with the uncommitted wave1 working-tree edits. Exclusive source scope: `src/adapters/chatgpt-web/mcp-server.ts`. No tests, typecheck, scripts, runtime/account actions, production actions, commits, or ABI changes were performed.

## Result

No additional source correction is justified in this lane. The current working-tree change already covers the accepted MCP finding `T2-9-1`; the remaining reviewed paths are clean at source level. No optional redesign was applied.

## Current change verified

The existing wave1 diff in `execGatewayResultProgram()` now handles a successful nested gateway result that has distinct structured data:

- recursively emits image, audio, image URL, and generated-image media through the outer freeform primitives;
- retains the complete nested `content` array;
- serializes `content`, `structuredContent`, and `_meta` into one JSON text envelope when either structured field is present;
- keeps the pre-existing recursive content/media path when neither structured field is present;
- leaves the earlier `isError === true` throw path before successful-result emission.

The shared helper is used by both the generic nested gateway and the command gateway fallback. Therefore the correction covers both `codex_tool_call` and `codex_exec` paths without changing their registrations or public schemas.

## Interaction and race review

The following cross-scope paths were inspected manually against the current source:

- concurrent nested calls through `codex_tool_call` and `codex_exec` converge on the same result emitter, so there is no second divergent structured-result path;
- direct structured calls continue through `asMcpResult()` and retain fields that actually reach the broker;
- `claimTurn()` uses a unique activity ID, and failed or cancelled claims retire that activity without using the caller abort signal for cleanup;
- `withClaimedTurn()` settles each activity in `finally`, while invocation transport failure releases the immutable binding so sibling calls cannot continue on a failed turn;
- delayed claim versus cleanup ordering is protected by the broker's completed-activity tombstone contract;
- the nested `isError` guard and the command-gateway `shell_command` option guard remain ahead of nested dispatch.

No new `T2-4-n`, `T3-4-n`, or other lane-local finding was established. No source edit was needed for this wave2 lane.

## ABI boundary

`Native4`, `Native4DEV`, `ZeroRisk4`, connector names, public MCP tool registrations, public schemas, and generation-4 ABI shapes were not changed.

## Remaining limits

- The review is source-level only. It does not prove the live outer freeform runtime's exact media/result rendering, account-side connector state, model behavior, or a completed native turn.
- The outer freeform result channel still carries nested structured data through a JSON text envelope; it does not add a new public metadata field or alter the broker/type ABI.
- JSON serialization still depends on nested successful result fields being serializable under the existing MCP result contract.
- Duplicate bare outer tool cards, bounded completed-activity tombstone retention, and cross-process ownership races remain previously documented optional or out-of-scope boundaries; no forced redesign was introduced here.

## Handoff

Wave2 lane4 is clean after review. The parent may retain the current wave1 `mcp-server.ts` correction and focus final verification elsewhere. This lane performed no tests, typecheck, scripts, runtime, production, or commit actions.
