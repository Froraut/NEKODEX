# Upstream review lane 08 — retry and model selection

Reviewed against NEKODEX `359d8fc` on 2026-09-15. This is source review only: live GitHub issue bodies, comments, and PR file patches were fetched with `gh api`; no runtime request, test, or installation was performed. The earlier [coding iterations](2026-09-15-coding-iterations.md#verification-performed) describe a separate focused verification and explicitly leave live model combinations untested. Counts: **5 items** (3 PRs, 2 issues); **3 substantially addressed in current source**, **1 partially addressed optional diagnostic**, **1 unresolved report requiring trace evidence**. #407's retry conflict overlaps #468; it is one failure-path theme, not a second patch request.

| Item and live upstream evidence | Current-source comparison | Assessment / recommendation |
| --- | --- | --- |
| [PR #468 — preserve retry turn handoff](https://github.com/miuuyy/codex-chatgpt-web/pull/468). The live patch adds `retry-continuation.ts`, accepts one successor with an exact user-revision digest, and records only emitted retryable 503 errors in `server.ts`. | NEKODEX already credits this PR in [`retry-continuation.ts:1`](../../src/adapters/chatgpt-web/retry-continuation.ts#L1). The scoped process-local digest and successor binding are at [`:12-85`](../../src/adapters/chatgpt-web/retry-continuation.ts#L12); fresh instructions clear it and old revisions require compaction or retry authority at [`environment.ts:226-245`](../../src/adapters/chatgpt-web/environment.ts#L226). The server records only a delivered, non-aborted, retryable 503 at [`server.ts:616-635`](../../src/server.ts#L616). | **Already addressed at source level.** Do not reapply upstream patch. A future focused runtime reproduction would be needed to claim that native Codex actually retries successfully after a live 503. |
| [PR #439 — optional Automated Pro model version](https://github.com/miuuyy/codex-chatgpt-web/pull/439). The live diff covers authorized persistence, an automatic-Pro request snapshot, exact family selection, and pre-send verification; its body reports one live 5.6 Pro request and no live 5.5/6 request. | The Settings control and save path exist at [`App.tsx:2008-2018`](../../launcher/src/App.tsx#L2008) and [`:2067-2074`](../../launcher/src/App.tsx#L2067); the command validates and saves the preference at [`pro-model-config.ts:39-70`](../../src/pro-model-config.ts#L39). Only automatic Pro snapshots the live setting at [`server.ts:495-500`](../../src/server.ts#L495). The adapter keeps the pin out of non-Pro execution identity at [`index.ts:196-211`](../../src/adapters/chatgpt-web/index.ts#L196); exact picker and pre-send checks occur at [`browser-worker.ts:2504-2609`](../../src/adapters/chatgpt-web/browser-worker.ts#L2504) and [`:3557-3564`](../../src/adapters/chatgpt-web/browser-worker.ts#L3557). | **Already implemented as an optional feature.** Preserve Follow ChatGPT default and fail-closed version verification. Upstream's live 5.6 claim is for its branch, not proof of NEKODEX's installed 5.5/6 or Pro route. No further source change follows from this PR alone. |
| [PR #432 — expose Pro retry date](https://github.com/miuuyy/codex-chatgpt-web/pull/432). The patch hovers a visible Pro row and reads a globally matched `Try again after` text from the page. Its body reports a live Linux tooltip but only fixture coverage of the patched adapter error. | NEKODEX already probes a Pro row on the unavailable five-position request at [`browser-worker.ts:2574-2586`](../../src/adapters/chatgpt-web/browser-worker.ts#L2574). [`pro-retry-hint.ts:21-65`](../../src/adapters/chatgpt-web/pro-retry-hint.ts#L21) requires one visible row, a linked `aria-describedby` tooltip, a valid date, and no conflicting descriptions. It deliberately avoids reading an unrelated page-wide portal. | **Partially addressed, useful optional diagnostic.** The current narrower association is justified by missing upstream ownership DOM. If a live NEKODEX case lacks the date, capture sanitized row/tooltip linkage first; do not copy the global text selector or invent a date. Pro availability still comes from the picker, not the tooltip. |
| [Issue #416 — Extra High blocked when Pro unavailable](https://github.com/miuuyy/codex-chatgpt-web/issues/416) and [follow-up](https://github.com/miuuyy/codex-chatgpt-web/issues/416#issuecomment-5601425814). The report distinguishes a four-position selector's usable Extra High from its disabled Pro and later reports workspace tool calls with a local patch. | NEKODEX detects independent four-position Extra High at [`chatgpt-session.ts:232-268`](../../src/chatgpt-session.ts#L232); its route has `requiresExtraHigh` rather than `requiresPro` at [`chatgpt-web-models.ts:356-375`](../../src/chatgpt-web-models.ts#L356) and the catalog gate is at [`:405-408`](../../src/chatgpt-web-models.ts#L405). Adapter xhigh and max gates differ at [`model.ts:67-73`](../../src/adapters/chatgpt-web/model.ts#L67), with legacy config fallback at [`config.ts:547-573`](../../src/config.ts#L547). | **Already addressed at source level.** Do not couple Extra High back to Pro. The reporter's installed Windows result belongs to its local patch; NEKODEX live account/catalog/tool access remains unproven by this review. |
| [Issue #407 — Luna/Think capacity plus reused-task `turn_id` conflict](https://github.com/miuuyy/codex-chatgpt-web/issues/407). Fresh Browser-only and Full attempts reportedly show Codex's `Selected model is at capacity`; reused tasks also show a revision conflict. The maintainer's [latest comment](https://github.com/miuuyy/codex-chatgpt-web/issues/407#issuecomment-5635461359) requests the underlying Codex HTTP/SSE error separately; no cause is established by the display text alone. | The local revision check remains deliberate at [`environment.ts:226-245`](../../src/adapters/chatgpt-web/environment.ts#L226); #468's authenticated retryable-503 continuation is present, but arbitrary old instructions stay rejected. The adapter emits status/code/retryable fields at [`index.ts:1417-1444`](../../src/adapters/chatgpt-web/index.ts#L1417), and overload mapping is explicit at [`errors.ts:147-155`](../../src/lib/errors.ts#L147). | **Insufficient evidence for the capacity cause; retry-specific conflict substantially addressed.** Do not label this an OpenAI quota, bridge defect, or permission problem yet. On a fresh NEKODEX occurrence, pair the precise Codex error body/code and safe launcher event at one timestamp, then inspect a reused-task conflict separately. A genuine non-503 stale-turn conflict is not authorized by #468 and needs its own exact revision/turn trace before relaxing the guard. |

## Parent handoff

1. Treat #468, #439, and #416 as source-covered; avoid blanket adoption or duplicate implementation.
2. Keep #432's tooltip behavior optional and attribution-bound. A sanitized live DOM example is the only useful trigger for broadening it.
3. If #407 recurs, collect the underlying response code/body and separate timestamps for fresh capacity and reused-turn conflict. Compare those with emitted adapter status/retryability before choosing a repair. Upstream's comments themselves say the generic Codex message does not establish model overload.

No changes were made to `tunnel.ts`, `tunnel-service.ts`, tests, launcher, or runtime source. The report does not upgrade upstream test claims or prior coding-iteration source checks into installed NEKODEX proof.
