# v6 runtime lane — 2026-09-24

Scope: `eaf4f09..212ceef` plus `212ceef..7579422`, compared with the architecture-refactored `afd9e5a` checkout. Upstream diffs and the per-item GitHub snapshots under `/Users/alex/Dev/nekodex-upstream-v6-evidence-20260924/items/` were treated as candidate evidence. This report describes source behavior; no installed app, provider account, release or production route was exercised. Other lanes own `src/types.ts`, model catalog/routing implementation, browser worker, prompt compiler, architecture map, integration build and publication.

| Upstream change / item | Disposition and local evidence |
| --- | --- |
| [PR 593](https://github.com/miuuyy/codex-chatgpt-web/pull/593), current-turn cross-thread follow-up | **Already present, with stronger authority.** `src/adapters/chatgpt-web/native-delegation.ts:12-53` verifies the exact delivery against the native rollout and promotes it to a verified parent message; `src/server.ts:267` invokes that normalization before the browser adapter. `src/adapters/chatgpt-web/environment.ts:188-204` accepts only the verified parent form. The upstream direct `function_call_output` recognition by envelope shape alone was not copied because serialized HTTP input cannot confer parent authority. |
| [PR 601](https://github.com/miuuyy/codex-chatgpt-web/pull/601), environment across native compaction summary | **Already present, then adapted.** The existing `src/adapters/chatgpt-web/thread-environment-resolver.ts:82-137,198-213` projects a bounded summary gap and compares the claim with the current native rollout. The new `src/adapters/chatgpt-web/compaction-continuation.ts:56-88` also recovers a source instruction when the completed checkpoint is the only surviving user item; `src/adapters/chatgpt-web/environment.ts:269,295,626-656,685-706` anchors environment lookup to that checkpoint. A matching summary/owner is required; text alone grants no authority. |
| [PR 604](https://github.com/miuuyy/codex-chatgpt-web/pull/604), replacement broker socket | **Adapted after a confirmed local defect.** The existing inode check ran after `Server.close()` (`src/adapters/chatgpt-web/turn-broker.ts:678-703`), but Bun had already unlinked a replacement path. The disposable probe at `tests/turn-broker-lifecycle.test.ts:228-261` established a reachable replacement before old-broker close and initially failed with `ENOENT` afterward. The broker now listens on a private sibling and publishes an exclusive hard link at the public path (`turn-broker.ts:731-765`); Bun closes its own private path, then the broker removes the public link only when its inode still matches (`turn-broker.ts:683-704`). A changed stale-socket inode is rejected before startup cleanup (`turn-broker.ts:817-829`). No live runtime path is renamed. The probe and ordinary broker start/revoke cases pass on Bun 1.4.0/macOS arm64. Linux filesystem support for hard-linked Unix sockets and a real overlapping daemon transition remain unverified. |
| [PR 650](https://github.com/miuuyy/codex-chatgpt-web/pull/650), brief-only Web subagent | **Needs evidence; outside this lane.** Its opt-in prompt filtering belongs to `src/adapters/chatgpt-web/prompt.ts` and the verified child lineage contract. The current prompt compiler has no `CODEX_WEB_BRIEF_ONLY_V1` path. Before adoption, the prompt owner should verify marker stripping, direct-parent provenance, and whether omitting inherited system/developer/tool context preserves the child's required instructions. No prompt change was made here. |
| Tool delivery and browser settlement diagnostics | **Adapted.** `src/adapters/chatgpt-web/turn-broker.ts:543-558` records bounded counts at capability retirement; `src/adapters/chatgpt-web/turn-execution.ts:218-233` records final/error outcome and submission phase. These logs contain trace IDs and counts, not prompt/tool content. They are diagnostic evidence, not proof of provider delivery. |
| Explicit tab-close cancellation versus physical cleanup | **Adapted.** `src/adapters/chatgpt-web/compaction-run-registry.ts:161-170,203-214` and `src/adapters/chatgpt-web/turn-execution.ts:609-621` expose synchronous revocation with a separate settlement promise. `src/server.ts:970-988` acknowledges ordinary tab close after revocation; lease-failure cleanup still waits. The new focused HTTP case proves the receipt arrives while the physical compaction owner remains active. |
| Structured environment fragment detection and calendar refresh | **Partly adapted, partly already present.** `src/adapters/chatgpt-web/environment.ts:112-181` restricts context detection to user fragments, keeps typed malformed attempts visible, and excludes prose/assistant/tool mentions. Calendar-only policy claims were already constrained by `extractChatGptTrailingEnvironmentDeltaClaim` at `environment.ts:383-471`; `thread-environment-resolver.ts:178-196` requires exact current-rollout corroboration. No generic permission relaxation was imported. |
| Completed compaction continuation and native memento text response | **Adapted.** `src/responses/compaction.ts:21-40` validates the `responses/memento` metadata shape; `src/responses/parser.ts:540-550` distinguishes text compaction from remote-v2 item compaction. `src/server.ts:320-366,513-547` emits the format native Codex expects and remembers only a completed, exact handoff. `src/adapters/chatgpt-web/compaction-continuation.ts:15-90` binds replay to thread, turn, model family, effort, summary hash and owner. The registry is process-local, so a daemon restart remains a separate recovery boundary. |
| Fresh-per-turn compaction option | **Adapted without changing default retention.** Existing `src/adapters/chatgpt-web/turn-runtime.ts:207-220` already disables retained conversation reuse for this preference. `src/adapters/chatgpt-web/index.ts:255-262,476-487` now releases an unfinished source before a fresh compaction and preserves a committed final response. Default retained handoff stays on its existing path. This is source-level coverage; no live ChatGPT compaction was run. |
| Reasoning-aware Web route and family separation | **Adapted with parent-owned route/types fields.** `src/server.ts:94-104` passes validated request reasoning to the route and sets family only for an automatic route. `src/adapters/chatgpt-web/turn-runtime.ts:304-318` passes that family to `worker.run`; `src/adapters/chatgpt-web/conversation-key.ts:35-43` and `turn-execution.ts:43-50` prevent cross-family retained/replay reuse. `src/adapters/chatgpt-web/index.ts:357-360` removes the source family only from an explicitly configured compaction-model execution copy, because its own version is authoritative. Model catalog and worker selection remain with their owners. |
| Saved Chats provider preference | **Already present through current shared work.** `src/config.ts:648` supplies `useSavedChats` to the provider; `src/adapters/chatgpt-web/index.ts:70-76` includes the provider setting in the execution namespace; `src/adapters/chatgpt-web/browser-worker.ts:714,2854,3074` consumes it. No extra adapter-index field or lifecycle change was needed. Its Launcher/UI behavior is owned by the other lane. |
| `7579422` follow-up | **Inapplicable to this runtime patch.** Its source delta is browser connector selection and localized model control in `browser-worker.ts`/`model-selection.ts`; no owned runtime file changed between `212ceef` and `7579422`. Browser lane owns verification. |

Focused checks: `bun test tests/runtime-v6-focused.test.ts` (4 pass: fragment provenance, memento completion/summary-only continuation and environment, family replay isolation, cancel receipt before physical cleanup); `bun test -t 'automatic lifecycle shares callbacks' tests/adapter-execution-refactor.test.ts` (2 pass, includes runtime-to-worker family field); `bun test -t 'continuation requires a matching current rollout' tests/environment.test.ts` (4 pass); `bun test tests/runtime-v6-focused.test.ts tests/upstream-date-delta.test.ts` (11 pass before the later family and compaction-copy edits). The new PR 604 probe first failed at the intended close boundary with `ENOENT`; after the broker repair, `bun test -t 'turn broker creates its private runtime directory|turn broker revokes only channels|closing an old broker preserves' tests/turn-broker-lifecycle.test.ts` passed 3 cases. After a private-path length check, the cold-start and replacement cases passed again (2 cases). `git diff --check` passed. No broad suite or integrated build was run; parent owns that gate.

Remaining integration points: parent should include the new cancellation/compaction and broker socket ownership contracts in the architecture documents, confirm `src/types.ts` and route changes in its own diff, and run the integrated build after all lanes stabilize. Browser owner should verify the worker/helper family field and Saved Chats navigation with its own focused case. The explicit configured compaction-model override was source-reviewed here; its browser behavior was not exercised. PR 650 needs a separate prompt-owner decision. `POST /responses/compact` checks its route before converting to an internal request; this precheck only excludes the Luna backend, while the internal `responseRequest()` applies validated effort routing.

## Independent runtime counter-review

All three candidates were confirmed against the current checkout and fixed within this lane:

| Finding | Evidence and correction |
| --- | --- |
| Mixed native environment and task parts were dropped as an instruction | The new `tests/runtime-v6-focused.test.ts:30-49` failed before repair: `contextualUserMessage` returned true for `[environment, "Complete the review"]`. `src/adapters/chatgpt-web/environment.ts:177-197` now treats the whole user item as contextual only if every part is a recognized context fragment. A grouped environment/plugins/AGENTS preamble remains contextual; a mixed task remains the active instruction. |
| A later user instruction without ID/turn provenance could be skipped during checkpoint recovery | The new continuation case at `tests/runtime-v6-focused.test.ts:89-101` initially returned the old task. `src/adapters/chatgpt-web/environment.ts:275-283,301-319` now stops before an older revision when a later substantive user item is unproven. `src/adapters/chatgpt-web/compaction-continuation.ts:119-146` refuses recovery past such an item while allowing a pure environment fragment. A matching checkpoint is still required. |
| Completed checkpoints retained up to 256 full source objects with no byte bound | Source inspection of the former `structuredClone(sources[0])` confirmed a 128 MiB image-bearing instruction could be retained per entry. `src/adapters/chatgpt-web/compaction-continuation.ts:14-18,50-99` now retains at most 1 MiB per source and 8 MiB across the registry; no more than 256 checkpoints and two source hashes per checkpoint are kept. Older source payloads are shed first, while their hash proofs remain. `tests/runtime-v6-focused.test.ts:136-175` exercises oversized input, total-budget eviction, count eviction and the two-hash limit. Summary-only recovery fails closed once its source payload is shed; an explicit matching instruction still validates against its hash. No wall-time expiry was added, preserving long-running checkpoint proof. |

Focused follow-up checks: the two new behavior cases failed at the intended boundaries before repair. After repair, `bun test tests/runtime-v6-focused.test.ts` passed 6 cases, `bun test -t 'continuation requires a matching current rollout' tests/environment.test.ts` passed 4, and `bun test tests/subagent-environment-history.test.ts` passed 2. `git diff --check` passed. Parent retains the affected core build/typecheck and integrated architecture-document review.
