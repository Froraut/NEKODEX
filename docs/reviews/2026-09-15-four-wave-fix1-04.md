# Implementation wave 1, lane 4 — D08/D09 gateway

Baseline `3a66157`. Changed only `src/adapters/chatgpt-web/mcp-server.ts` and this report. Manual diff review and `git diff --check`; no tests, typecheck, runtime, benchmark, suite, install, release, commit or production operation.

- **D08:** The generated gateway checks a nested result's `isError === true` before emitting content. It throws an error naming the nested tool and retaining up to 8192 characters each of text content and serialized `structuredContent`. This uses the existing error propagation path because the generic `text`/`image`/`audio` emission API does not expose an `isError` setter. Direct structured command relay remains unchanged. Non-text media and `_meta` are not copied into the error message; unusually long details are truncated, and unserializable structured data is labeled.
- **D09:** The generated command gateway resolves the unique nested native command, then rejects `tty` and/or `max_output_tokens` when that command is `shell_command`, before accessing or invoking its function. Nested `exec_command` receives the original supplied fields. The existing direct shell rejection and escalation guard remain unchanged.

Cross-lane internal API: `export function execCommandGatewayProgram(execCommandArguments: Record<string, unknown>, shellCommandArguments: Record<string, unknown>): string` returns the generated JavaScript for the parent to run with synthetic `ALL_TOOLS`, `tools` and emission functions. This TypeScript module export does not change the public MCP tool names, descriptions, input schemas, annotations or ABI pins. The nested `codex_tool_call` generator shares the same failed-result guard.

Manual review checked the failed nested-result branch, shell selection before dispatch, direct command routing, and public registration block. Runtime behavior and the exact outer gateway rejection shape remain for the parent's single focused verification pass. Native4 / Native4 DEV / Zero Risk4 identity and public ABI are unchanged by this patch.
