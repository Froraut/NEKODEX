# Coding wave 1 — local repairs

Sixteen gpt-5.6-sol medium workers. Raw implementation reports; parent integration corrected DEV snapshot narrowing and updated compaction fixture calls, connected update-recheck IPC/UI and localized its labels. All workers closed. Backend and renderer typecheck passed; behavior tests reserved for final shared budget.

## 1. Sartre

В первой волне внесены правки для **E01 и E11** только в [launcher-helper-client.ts](/Users/alex/Dev/nekodex/src/adapters/chatgpt-web/launcher-helper-client.ts) и [browser-helper-main.ts](/Users/alex/Dev/nekodex/src/adapters/chatgpt-web/browser-helper-main.ts).

Для E01 клиент после неудачной доставки Abort сразу завершает обещание вызывающей стороны, но сохраняет trace ID занятым. Повтор с тем же ID допускается после сообщения helper о завершённой уборке состояния или после выхода именно этого helper. Для более старого helper, который не посылает такое сообщение, предусмотрено освобождение по его финальному ответу. Другие операции helper при сбое Abort не прерываются.

Для E11 Send activation и его подтверждение теперь связаны идентификатором запроса. Клиент не отправляет подтверждение после отмены; helper игнорирует позднее подтверждение удалённого waiter и не применяет подтверждение прежнего запуска к новому с тем же trace ID.

Просмотрел изменённый путь и diff вручную; `git diff --check` прошёл. По ограничению этой волны сборку, тесты, запуск приложения и сетевую проверку не проводил. Родительской задаче предлагаю **один случай**: отменить запуск между `send_activated` и завершением callback, задержать Send ack, затем повторить тот же trace ID; исходная отмена должна сохранить результат Abort, а поздний ack не должен завершить повторный запуск. При этом сценарий недоставленного Abort и длительного владения ID helper остаётся проверенным только по коду.

Commit, push и изменения опубликованных ресурсов не выполнялись.



## 2. McClintock the 2nd

Исправил E02, E09, E17 и E19 в закреплённом scope. Изменены только [account-pool.cjs](/Users/alex/Dev/nekodex/launcher/electron/account-pool.cjs), [browser-host.cjs](/Users/alex/Dev/nekodex/launcher/electron/browser-host.cjs) и [account-registry.cjs](/Users/alex/Dev/nekodex/launcher/electron/account-registry.cjs).

- **E02:** глобальная capacity освобождается только при повторном использовании tab на выбранном host: проверяются режим, состояние и, для автоматического turn, connector. Если нужен новый tab, пул учитывает pending reservations и освобождает достаточно мест.
- **E17:** host отклоняет другой активный trace с тем же conversation key. Пул проверяет конфликт до возможного вытеснения удержанного tab; host повторяет проверку на границе `beginTurn`.
- **E09:** при отказе `ready()` новая запись условно откатывается вместе с выбором account; пустой созданный host закрывается. Если запись уже изменена другим действием или откат не удался, ошибка прямо сообщает, что account остался сохранённым.
- **E19:** занятый host отклоняется до сброса capability evidence. Если начатая проверка завершилась ошибкой, прежнее connector evidence удаляется, а новое состояние публикуется.

Просмотрел diff и прямые пути вызова вручную. По вашему ограничению не запускал сборку, тесты, приложение или трафик; поведение при реальном отказе I/O остаётся непроверенным. Родительской задаче предлагаю **один** сфокусированный случай: при заполненной глобальной capacity удержанный tab с тем же conversation key, но другим connector, не должен считаться повторно используемым или приводить к превышению лимита.

IPC вызовы `account-add` и `account-check` сохраняют прежние сигнатуры. Commit, push и изменения UI или опубликованных assets не выполнялись.



## 3. Gauss the 2nd

Исправил E03, E04 и E12 в [src/setup.ts](/Users/alex/Dev/nekodex/src/setup.ts) и [src/tunnel.ts](/Users/alex/Dev/nekodex/src/tunnel.ts).

- **E03:** если сохранение config завершилось до замены файла, исходные байты теперь считаются неизменёнными. Это позволяет компенсировать записи ключа и tunnel-client. Если текущие байты отличаются и от исходных, и от запланированных, откат их сохраняет.
- **E04:** DEV проверяет Tunnel ID и конфликт с другим режимом до записи ключа. При последующей ошибке он откатывает только подтверждённые собственные записи config, ключа, клиента и профиля; временный tunnel сначала пытается остановить.
- **E12:** перед каждой заменой управляемого runtime key проверяются ожидаемые прежние байты. Изменение между снимком и этой проверкой прекращает запись и сохраняет чужую правку.

Провёл только ручной просмотр diff и прямых вызовов. Сборки, тесты, трафик, запуск приложения и публикация не выполнялись. Остаётся документированный зазор между сверкой файла и его атомарной заменой; без общего механизма блокировки он не устраняется этой правкой. Файл профиля также сохраняется для ручного восстановления, если внешний `runtimes connect` мог записать его без подтверждения или временный tunnel не удалось остановить. `src/config.ts` не менял.

Родителю для общего бюджета проверки предлагаю **один случай**: DEV full setup с новым ключом и некорректным Tunnel ID; операция должна завершиться ошибкой, оставив прежние байты ключа и клиента. Изменения оставлены в общей рабочей копии без commit, push или ветки.



## 4. Peirce the 2nd

Исправил **E13** в [thread-environment.ts](/Users/alex/Dev/nekodex/src/adapters/chatgpt-web/thread-environment.ts:230). При обновлении записи и при удалении по TTL хранилище теперь готовит новую карту, записывает её и только после успешной записи публикует в памяти. Если запись завершается ошибкой, прежняя карта остаётся доступной; при неудачном TTL-удалении ошибка передаётся вызывающему коду, поэтому просроченная запись не выдаётся как действующая authority.

В родительском `index.ts` проводку менять не требуется: он уже получает ошибку из `environmentStore.resolve()`. Изменён ровно один файл. Я просмотрел его diff и прямой путь вызова вручную; по условию волны не запускал тесты, сборку или приложение и не делал commit/push.

**Один сценарий для проверки родителем:** принудительно вызвать сбой записи при `set()`, затем восстановить запись и выполнить запрос того же thread без нового environment envelope. До успешной записи запрос должен видеть прежнюю сохранённую authority. Ограничение: поведение при одновременных записях в этот файл из разных процессов этой правкой не проверялось.



## 5. Gibbs the 2nd

В первой итерации внесены исправления для **E05 и E18** в [runtime-supervisor.cjs](/Users/alex/Dev/nekodex/launcher/electron/runtime-supervisor.cjs) и [main.cjs](/Users/alex/Dev/nekodex/launcher/electron/main.cjs).

Начальный запуск runtime теперь имеет собственный сигнал отмены, который передаётся командам туннеля и ожиданию готовности. Остановка ждёт завершения этого запуска в пределах существующего лимита. Если он не завершился вовремя, принудительная остановка сообщает `forced-partial` и не начинает конкурирующую очистку или запись нового состояния владения.

Обработчик ошибки запуска теперь проверяет, начался ли Quit, перед восстановлением маршрута Codex и перед публикацией результата. Если восстановление уже началось, его операция видна существующей проверке Quit; если Quit прошёл эту проверку первым, обработчик восстановление не запускает.

Просмотрел diff и непосредственные пути вызова вручную. По вашему ограничению не запускал тесты, сборку или приложение; поведение при живом сбое остаётся непроверенным. **Один случай для родительской проверки:** начать Quit во время ожидающей команды начального запуска туннеля, затем вызвать ошибку запуска; проверить ограниченное завершение остановки и отсутствие восстановления маршрута после начала Quit.

Добавление IPC для `update-check` в `main.cjs` оставлено родительскому агенту и agent16. Коммитов, публикации и изменений сертификатов нет.



## 6. Lorentz the 2nd

Исправил **E06** в [turn-execution.ts](/Users/alex/Dev/nekodex/src/adapters/chatgpt-web/turn-execution.ts). `clear`, TTL `prune` и точное native interruption теперь направляют удаление привязанной сессии через обязательство на release: запись и head остаются доступными до подтверждения Launcher, а неудачное освобождение можно повторить при следующем обращении владельца. Тот же путь применён к прямому retirement сессии и обработке ранее прерванных turn ID.

TTL не освобождает разговор, пока в нём есть более свежий или активный ответ, который ещё нужен для точного replay. Если settlement отдельного turn завершился ошибкой, попытка Launcher release всё равно выполняется; без подтверждённого release обязательство сохраняется. Существующие лимиты записей и replay остаются границей удержания.

Вручную просмотрел diff и прямые вызовы в `index.ts` и `server.ts`; `git diff --check` прошёл. Сборки, тесты и запуск приложения не выполнял по ограничению этой волны, поэтому runtime-подтверждения нет. **Один сценарий для родительской проверки:** завершённый retained turn → `/admin/cancel-turns` → первая попытка release отвергнута → следующий turn того же владельца повторяет release и получает доступ лишь после acknowledgement. Дополнительной wiring в других файлах не требуется.



## 7. Maxwell the 2nd

В первой волне исправил **E07 и E14** в двух файлах: [parser.ts](/Users/alex/Dev/nekodex/src/responses/parser.ts) и [schema.ts](/Users/alex/Dev/nekodex/src/responses/schema.ts).

- **E07:** результат инструмента с `input_image`, у которого есть только `file_id`, теперь явно отклоняется. Мост не может получить по этому ID содержимое изображения; раньше блок исчезал, а результат из одного такого блока становился пустым. Изображения с `image_url` продолжают передаваться как структурированные части.
- **E14:** `tool_search_call` и `tool_search_output` получили отдельные схемы и больше не проходят через схему неизвестных элементов. Вызову нужен непустой `call_id` или `id`, результату — непустой `call_id`; парсер также проверяет эти условия перед созданием истории. Некорректный запрос теперь попадает в существующий путь ошибки разбора в `server.ts`, до маршрутизации обращения к модели.

Просмотрел diff и прямой путь от парсера до проекции результата вручную. Сборок, тестов, запуска приложения и сетевого трафика не выполнял по ограничению этой волны. Для общей проверки родителю предлагаю **один случай**: `tool_search_call` с корректным `call_id` и следующий `tool_search_output` без `call_id` должны давать ошибку запроса, а не результат с пустым ID. Поведение с реальным нативным клиентом остаётся непроверенным; новых тестов и изменений вне двух закреплённых файлов нет.



## 8. Plato the 2nd

**E08 исправлен** в [src/browser-login.ts](/Users/alex/Dev/nekodex/src/browser-login.ts:478). При повторном входе старый маркер проверки теперь удаляется до замены сохранённого состояния. Новый маркер появляется только после успешной записи состояния. Если запись маркера завершится ошибкой, прежняя отметка уже не сможет подтверждать новые байты.

Я вручную просмотрел diff, путь вызова из setup и проверку наличия маркера в browser worker. Изменены только три строки кода и комментарий в назначенном файле; логику закрытия Chrome и очистки профиля я не менял. Сбой записи на диске не воспроизводился, поэтому это исправление подтверждено по коду, без runtime-проверки.

Для общей проверки родителю предлагаю **один случай**: повторный вход с ошибкой записи нового маркера после успешной записи состояния; ожидается отсутствие маркера и `browserLoginStateExists(config) === false`. Коммит, сборку и запуск приложения я не выполнял.

## 9. Raman the 2nd

Исправил E15 в [browser-worker.ts](/Users/alex/Dev/nekodex/src/adapters/chatgpt-web/browser-worker.ts:2349). Если Chrome успешно запущен, но создание контекста или страницы завершается ошибкой, worker теперь закрывает локальный браузер и повторно выбрасывает исходную ошибку. В пути управляемого Chrome браузер также закрывается при отказе `newContext()` до записи handle в состояние worker. Успешное создание по-прежнему передаёт handle в обычный `close()`.

Просмотрел diff и прямые вызовы обоих путей. Изменён только этот файл; E15 закрыт на уровне кода. Отказ Playwright и успешное закрытие процесса в этом сценарии не воспроизводились. Сборку, тесты и запуск приложения не выполнял, согласно границам этой волны.

Родителю предлагаю **один** сфокусированный случай: после успешного `chromium.launch()` заставить `newContext()` отклонить вызов и проверить, что браузер закрыт, исходная ошибка сохранена, а повторное открытие возможно. Дополнительной проводки в других файлах для E15 не требуется. Коммита и публикации нет.



## 10. Aquinas the 2nd

Исправил **E21** в [compaction-handoff.ts](/Users/alex/Dev/nekodex/src/adapters/chatgpt-web/compaction-handoff.ts:462) и [index.ts](/Users/alex/Dev/nekodex/src/adapters/chatgpt-web/index.ts:900). Адаптер теперь передаёт одну и ту же native identity в оба пути получения structured compaction. Перед возвратом cached result каждый путь проверяет запись об interruption и abort сохранённого owner.

Сроки хранения согласованы с существующей логикой: успешный cache живёт до 30 минут от регистрации, interruption — до 30 минут от отмены. Если физическое завершение owner затянется за этот срок, его abort signal всё ещё запрещает возврат cached result. Это закрывает описанный replay после вытеснения identity из HTTP карты на 1 024 записи. Выполнение browser turn и `turn-execution.ts` не менял.

Проверил diff и путь вызова вручную; `git diff --check` прошёл. По границам этой волны сборок, тестов, трафика, commit и push не выполнял. Родительской задаче предлагаю **один** прицельный случай: сохранить успешный compaction, отменить его native turn, вытеснить identity из HTTP interruption map и повторить точный запрос до истечения 30 минут; ожидается ошибка отмены без возврата summary.



## 11. Einstein the 2nd

Исправил **E10** в [install-launcher.sh](/Users/alex/Dev/nekodex/scripts/install-launcher.sh) и [install-launcher.ps1](/Users/alex/Dev/nekodex/scripts/install-launcher.ps1). При выборе версии по умолчанию установщики теперь просматривают не более десяти опубликованных релизов, включая prerelease, и берут первый релиз с точным ассетом для текущей платформы и архитектуры. Если подходящего ассета нет, выводят явную ошибку. Явно заданная `CODEX_WEB_GPT_VERSION` по-прежнему работает; загрузка `checksums.txt` и проверка SHA-256 сохранены. Unix-скрипт использует уже доступный `awk`, без новой обязательной зависимости.

Ручной просмотр diff и `git diff --check` не выявили проблем форматирования. По ограничению этой волны скрипты не запускал, тесты и сетевые запросы не выполнял. Выбор ограничен десятью релизами: более старый совместимый релиз автоматически найден не будет. Опубликованные ассеты и сертификаты не менял; commit, push и ветку не создавал.

**Один случай для проверки родителем:** в ответе API первый релиз содержит только macOS-ассет, а второй — нужные Windows/Linux-ассеты. Проверить, что каждый установщик выбирает соответствующий второй релиз и затем сверяет SHA-256 скачанного файла.



## 12. Rawls the 2nd

E16 исправлена в [focused-pr-check.ts](/Users/alex/Dev/nekodex/scripts/focused-pr-check.ts:78). Для push скрипт теперь явно распознаёт переименования и передаёт классификатору **исходный и новый пути**. Если исходный файл с назначенным проверочным случаем переименуют в файл метаданных, его поведенческая классификация сохранится. Порог в 30 путей, выбор только именованных случаев и сообщения о необходимом ручном просмотре остались ограниченными.

[ci.yml](/Users/alex/Dev/nekodex/.github/workflows/ci.yml:30) не менял: push уже получает историю обоих коммитов, а существующий шаг вызывает исправленный скрипт. Дифф и прямую связку со шагом CI просмотрел вручную. Сборок, тестов, запусков приложения, коммитов и публикации не было.

Родителю предлагаю один сфокусированный случай в общем бюджете: push-переименование `src/responses/parser.ts` в `docs/parser.md` должно сохранить выбор именованного parser case и указать оба пути в сводке. Фактическое поведение в CI пока не проверено.



## 13. Chandrasekhar the 2nd

Исправил E20 в [launcher/scripts/package.cjs](/Users/alex/Dev/nekodex/launcher/scripts/package.cjs:138). Скрипт теперь проверяет, что staging содержит ровно по одному ожидаемому дистрибутиву для целевой платформы, что файлы не пустые и что их имена не столкнутся после переименования Linux-артефакта. Затем он копирует новый набор в соседнюю подготовленную директорию, сохраняя прочие файлы из `artifacts`. Прежний набор остаётся на месте до завершения всех копирований; если финальное переименование не удастся, скрипт пытается вернуть его.

Ручной просмотр diff выполнен. Сборок, тестов, запусков приложения, commit и push не делал. Ограничение: замена использует два переименования, поэтому между ними `artifacts` может кратко отсутствовать; при одновременном запуске двух упаковок нужна отдельная координация. Если возврат после ошибки переименования тоже не удастся, ошибка укажет путь к сохранённому прежнему набору.

Родителю предлагаю один точный случай для общей проверки: при существующем полном наборе артефактов вызвать ошибку копирования одного файла из staging и убедиться, что прежний набор остался целым. Остальные E ID вне моей области; изменён только E20.



## 14. Mendel the 2nd

Реализовал оптимизацию истории в [src/responses/state.ts](/Users/alex/Dev/nekodex/src/responses/state.ts). Между самодостаточными контрольными состояниями запись теперь хранит только новый ввод и вывод. Глубина цепочки ограничена семью ссылками; следующий ход создаёт контрольное состояние. Это убирает повторное копирование и сериализацию всей накопленной истории при записи обычных продолжений.

Воспроизведение собирает полную историю по запросу. TTL, удаление старых ID и лимит памяти учитывают предков, которые ещё нужны оставшимся записям. Дисковый снимок остаётся самодостаточным в формате версии 1, поэтому загрузка старых снимков и пропуск крупных записей не создают висячих ссылок.

Это работа по предложению оптимизации истории из обзора; **E ID она не закрывает**. Ограничение: развёртывание запроса и отложенная запись снимка на диск по-прежнему копируют полную историю. Ускорение не измерялось.

Просмотрел diff и прямой путь вызова из сервера; `git diff --check` не выявил ошибок оформления. Сборку, тесты и запуск приложения не выполнял. Для родительской проверки предлагаю **один случай**: цепочка из девяти продолжений, где старый ID удалён, затем снимок записан и загружен заново; проверить, что последний ID воспроизводит ровно исходный ввод и все выводы. Изменён только указанный файл; commit, push и ветку не создавал.



## 15. Jason the 2nd

В `launcher/src/App.tsx` реализовал предложение O02 из сводки: completion-события, пришедшие в одном цикле, объединяются в один запрос snapshot. Если установка MCP или явная проверка запрашивает обновление до отправки отложенного запроса, она использует этот слот; уже выполняющийся completion-запрос также можно повторно использовать, пока не началась следующая операция.

Сохранены две защиты корректности: событие изменения `state` имеет приоритет над более старым ответом snapshot, а новый запрос не применяет ответ предыдущего. При завершении эффекта таймер очищается и ожидающий ответ теряет право обновить UI. Других файлов не менял. **E01–E21 эта задача не затрагивает**: исправлено только предложение O02. Лёгкого IPC для одних metadata-полей сейчас нет, поэтому полный snapshot всё ещё запрашивается; выигрыш от объединения не измерялся.

Провёл ручной просмотр diff; `git diff --check` не выявил ошибок форматирования. Сборок, тестов, запуска приложения, трафика и публикации не было. Родителю предлагаю один сфокусированный случай: успешная MCP verification с completion и последующим `updateSnapshot()` должна дать актуальные credentials/capabilities одним применимым refresh, а более позднее событие `state` должно сохранить свой revision. Проверка и её бюджет остаются за родительской задачей.



## 16. Dirac the 2nd

Реализовал явную повторную проверку обновлений в назначенной области. После завершения проверки следующая попытка допускается через 60 секунд; одновременные вызовы получают результат одной и той же проверки. Если обновление уже доступно, загружается или устанавливается, `recheck()` возвращает текущее состояние и сохраняет выбранный релиз и ход установки. Стартовый `checkOnce()` по-прежнему выполняется только один раз за процесс.

Изменены [update.cjs](/Users/alex/Dev/nekodex/launcher/electron/update.cjs), [preload.cjs](/Users/alex/Dev/nekodex/launcher/electron/preload.cjs) и [types.ts](/Users/alex/Dev/nekodex/launcher/src/types.ts). Это реализация третьего предложения по оптимизации из обзора; **E01–E21 в этой области не закрывались**.

Родительскому агенту нужны два соединения в принадлежащих ему файлах:

- В `registerIpc()` файла `launcher/electron/main.cjs` зарегистрировать `handle("launcher:update-recheck", async () => { if (!updateController) throw new Error("Launcher updates are unavailable"); return updateController.recheck(); });`. Preload и тип `LauncherApi.recheckUpdate(): Promise<UpdateState>` уже используют этот канал.
- В `launcher/src/App.tsx` дать пользователю действие повторной проверки для состояний `error` и `up-to-date`, вызывать `api.recheckUpdate()` и применять возвращённое `UpdateState` либо существующее событие `launcher:update-state`. При вызове в течение 60 секунд контроллер возвращает прежнее состояние; интерфейсу следует понятно обозначить ожидание, чтобы нажатие не выглядело потерянным.

Ручной просмотр diff завершён; сборки, тесты, запуск приложения и сетевые запросы не выполнялись по условиям волны. Один предложенный родителю случай проверки: первая проверка завершается ошибкой, немедленный повтор не делает второй запрос, а повтор после 60 секунд возвращает найденный релиз. Без IPC и действия в интерфейсе пользователь пока не сможет вызвать новую проверку.
