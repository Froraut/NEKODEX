# NEKODEX triwave review 2, lane 8 — helper verifier

Baseline: `3740505b0107af9a83053fd523ae1ad30be36465` (`HEAD`). Independent, read-only manual trace of `launcher/electron/browser-helper-verifier.cjs`, `src/adapters/chatgpt-web/browser-helper-main.ts`, the direct maintenance methods in `src/adapters/chatgpt-web/browser-worker.ts`, and the direct launcher/account callers. Challenged `2026-09-15-triwave-review1-08.md` against the adjudicated four-wave ledger and current source. No tests, typechecks, scripts, broad audits, runtime/account actions, production actions, code edits, or commits. This review document is the only new artifact. Native4, Native4 DEV, and Zero Risk4 names and ABI pins remain untouched.

## Finding disposition

**No new concrete bug established in this scope; no `T2-8-n` finding IDs issued.** The examined branches either implement D16, fail closed, or depend on a live helper/account condition this source review cannot establish.

## First-wave challenge and source trace

- **D16 repeat, not a new finding.** `browser-helper-verifier.cjs:43-65` requests shutdown, waits 5 seconds, signals the exact child with TERM and waits 2 seconds, then signals it with KILL and waits another 2 seconds. If exit is still unobserved, the exact `ChildProcess` is retained in `unsettledHelperChildren` until `exit`/`close`; the operation returns a cleanup error. `:167-192` keeps a preceding operation error as primary and attaches `cleanupError` and `operationId`. The four-wave ledger's original concern was relinquishing an unsettled child after TERM. That specific ownership loss has been corrected at this HEAD. The retained set is process-local; it cannot prove cleanup after launcher termination, which the four-wave results already identify as a known limit.
- **Correlation and failure handling support the first-wave conclusion.** The verifier generates a random operation ID (`:81`), accepts one readiness frame (`:125-137`), rejects responses with another ID (`:139-155`), handles stream and child errors (`:106-117,157-164`), and awaits physical stop after the operation result (`:167-192`). The helper's `verify` and `maintain` methods return a result or error with the same ID (`browser-helper-main.ts:348-360,379-399`). The parent rejects an unexpected connector text (`browser-helper-verifier.cjs:195-211`). Nothing in this path establishes a second success route for an incorrect connector.
- **Direct callers keep independent evidence checks.** `browser-host.cjs:3071-3094` requires the exact smoke response; `:3102-3125` awaits the exact-name connector verification; `:3136-3165` requires authenticated temporary-session and coherent capability evidence. `main.cjs:682-714` sets `mcpSetupComplete` only after the awaited connector check succeeds; `account-pool.cjs:216-233` records per-account connector evidence only after its host verification succeeds. These callers do not treat a malformed maintenance result as verified.

## Repeats, optional work, and known limits

- **Repeat / D16 shutdown latency:** `browser-helper-main.ts:118-154` closes output and waits for browser-worker teardown before exiting. If teardown stalls, the verifier's bounded TERM/KILL path can turn an otherwise successful maintenance operation into cleanup failure after up to nine seconds. That is the explicit D16 residual behavior, not an additional root. `maintain` registers an abort controller for smoke/inspection (`:379-399`); `verify` relies on worker shutdown rather than a maintenance abort signal (`:348-360`). A live shutdown timing case would be needed to establish a further defect.
- **Optional observability:** When a primary verification error and cleanup error coexist, `browser-helper-verifier.cjs:177-180` preserves both on the error object, while `browser-host.cjs:3117-3123` logs only the primary name/message and `main.cjs:702-714` reports only its message. A bounded cleanup detail in diagnostics could help support, but the primary result remains failed and the child remains owned. This repeats the wave-1 optional note; no finding ID.
- **Known source/live boundary:** No manual source review proves that Chromium/CDP teardown exits within the windows, that the helper's pipe behaves the same on every host, or that a real account exposes the intended connector/capabilities. The helper's temporary-surface and connector-selection methods (`browser-worker.ts:3855-3900`) are the source of the returned evidence; account integration remains a separate live-proof layer. No current path found here requires changing connector identities, public tool schemas, or generation-4 ABI pins.

Counts: **0 new defects; 1 repeated D16 root; 1 repeated optional diagnostic improvement; 1 known live-proof boundary; 0 source/code changes.** IDs: **none**. Parent retains the sole final focused verification budget of at most 60 seconds and 10 scenarios.
