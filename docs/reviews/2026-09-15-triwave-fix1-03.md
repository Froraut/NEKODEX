# NEKODEX tri-wave fix 1, lane 3 — tunnel cancellation handoff

Baseline: `3740505b0107af9a83053fd523ae1ad30be36465` (`HEAD`). Implementation was
restricted to `src/tunnel.ts` and `src/tunnel-service.ts`, as requested. No tests,
typechecks, scripts, runtime or production actions, or commit were performed.

## Implemented

- Added optional `AbortSignal` propagation to `connectTunnel`, `stopTunnel`,
  `tunnelStatus`, and `waitForTunnelReady`.
- Passed the signal into every tunnel-client `runCommand` probe/stop/connect and
  checked cancellation before and after each command.
- Made readiness polling abortable while sleeping and while probing.
- Added optional signal propagation to launchd tunnel-service status, install,
  start, stop, restart, uninstall, and unload polling paths.
- Passed the signal into launchd bootstrap/bootout commands and made the unload
  wait abortable. Existing callers remain source-compatible because all new
  parameters are optional.

## Finding disposition

- **T3-15-1 stale-owner tunnel recovery cancellation:** source-side cancellation
  propagation is implemented in the allowed tunnel helpers. The owning
  `launcher/electron/runtime-supervisor.cjs` stale-owner caller still must pass
  its recovery signal into these optional parameters; that caller was outside
  this lane's exclusive write scope.
- **T1-15-1 / T2-15-1 control-child ownership:** not claimed fixed here. Exact
  control-child retention, escalation after pipe errors, and cross-operation
  serialization live in `launcher/electron/runtime-supervisor.cjs`, outside the
  two permitted source paths. `src/process.ts` remains a synchronous command
  wrapper and is also outside scope.
- Wave2's direct loaded tunnel-service definition guard was already present at
  HEAD and was preserved.

## Verification boundary

`git diff --check` was run for the two source files and was clean. The parent
owns the final focused verification pass (at most 60 seconds and 10 scenarios).
The requested `docs/reviews/2026-09-15-triwave-review3-03.md` was absent from
the checkout; available lane-15 wave2/wave3 reports were used instead.

