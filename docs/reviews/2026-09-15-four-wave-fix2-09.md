# Wave 2 final correction, lane 09 — D16

Baseline `3a66157`, including the uncommitted wave-1 implementation. No additional source change is warranted in this lane. Wave 1 already sends protocol shutdown with a bounded 5-second observation, then SIGTERM with a 2-second observation, then SIGKILL with a final 2-second observation, all through the exact spawned `ChildProcess` handle. The write callback cannot indefinitely extend shutdown grace. A child whose exit remains unobserved is held in `unsettledHelperChildren` until its eventual `exit` or `close` event and produces an explicit cleanup error. A primary operation failure retains its error object, `operationId`, and an attached `cleanupError`.

Manual source review covered `browser-helper-verifier.cjs`'s result/cleanup ordering, the helper's asynchronous SIGTERM/shutdown handler, and direct `browser-host.cjs` and `main.cjs` callers. No subprocess, tests, typecheck, suite, benchmark, or live operation was run; the parent owns the shared final focused pass. Native4 / Zero Risk4 public ABI pins and connector identity are unchanged.

Residual material limit: if the OS does not report exit after the final SIGKILL window, cleanup is accurately reported as unobserved. In-process retention of the exact child handle cannot establish survival of that ownership after the launcher itself terminates. No additional in-scope deterministic correction follows without changing the process ownership contract or launching a runtime reproduction.
