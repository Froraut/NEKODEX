# v5.0.7 adaptations implemented in NEKODEX

Date: 2026-09-16. Starting commit: `ab42225fe4cf9c37fb28260274760e1e9c4fdd25`.
Working branch: `codex/upstream-v507-improvements`.

Implemented all ten items in the user's pasted list: the seven primary recommendations and three lower-priority additions. The source review is [2026-09-16-upstream-v5.0.7-review.md](2026-09-16-upstream-v5.0.7-review.md). The unresolved Extra High entitlement/context-limit policy was not an implementation recommendation; its values are unchanged.

## Delivered changes

| Item | Implementation |
| --- | --- |
| Local tunnel readiness | `src/tunnel.ts` reads non-applying local inventory and matches the configured alias. `launcher/electron/runtime-supervisor.cjs` discovers the exact alias's health URL through local list output, checks its local file and loopback endpoint, and preserves cancellation and independent transport probes. |
| CSS-aware response observation | `browser-worker.ts` rechecks rendering dependencies before accepting an unchanged response cache. This covers stylesheet changes outside the response subtree without replacing the local answer/commentary classifier. |
| Localized stopped-thinking status | `ui-labels.ts` supplies exact observed labels, including Russian; the observer continues to exclude answer text, commentary, code and quotations. |
| Explicit expiry cancellation | Production automatic tabs await cancellation of the exact runtime trace before disposal. Sweeps coalesce, carry bootstrap/heartbeat failure reasons, and retain failed cleanup for another attempt. Identity checks protect replacement owners. Manual cancellation, suspension handling and bounded server request parsing remain intact. |
| Correct startup Restart environment | The launcher captures its original profile environment before internal overrides and restores it before relaunch. Native recovery remains bounded, privacy-safe, and now localized in all five launcher languages. |
| Reliable MCP diagnostics | Duplicate in-flight IDs become uncorrelated; tracking overflow and transport closure are observable. Timing is monotonic and records carry PID without payloads or raw request identifiers. |
| Localized Pro explanations | The optional hint accepts a bounded localized explanation from one visible tooltip explicitly linked to a disabled Pro row. Conversation/composer exclusion remains; English retry-date validation remains supported. |
| Traditional Chinese and Korean | Shared language registry; onboarding/settings choices; persistence; 395-key catalogs for each added language; account/settings, route diagnostics, native launcher/startup and Chrome import dialogs. Both catalogs have compile-time key requirements. |
| Release download-link consistency | `scripts/readme-downloads.ts` checks the actual fork, version and artifact names through the existing `check-version` release step. It requires the advertised macOS downloads without requiring unpublished Windows/Linux assets. Chinese/Japanese READMEs now point to the current macOS downloads instead of stale future-install commands. |
| Decoded history-value scrubbing | `prompt.ts` scrubs string values, preserving JSON keys and native call/result linkage only at recognized inline or multipart history positions. Nested tool payloads cannot imitate those positions to bypass scrubbing. All six local broker capability kinds remain covered. |

Four Sol medium workers implemented disjoint browser, lifecycle, MCP/history, and localization areas. One lifecycle owner held the coupled host/supervisor/server/main changes. Localization requests touching main/startup were routed through that owner. Parent implemented the release-link check, corrected related documentation, integrated results, reviewed direct code paths and ran the bounded checks. No additional reviewer waves were launched.

## Focused verification

Nine named regression checks passed:

1. Release links use the current fork/version/macOS artifact naming and reject stale or foreign links.
2. Duplicate MCP request IDs cannot falsely attribute replies; transport closure is recorded without payload disclosure.
3. One decoded history envelope preserves keys/native linkage, scrubs broker values after escaped newlines, and scrubs nested payload impersonation.
4. Russian stopped-thinking UI is terminal; a quoted answer remains ordinary content.
5. The real production response observer, in isolated headless Chrome, detects an answer revealed by changing a stylesheet outside the response subtree after a cache hit.
6. Tunnel readiness selects the configured alias and does not borrow another alias's readiness.
7. Expiry cancellation precedes tab disposal, overlapping sweeps share the request, and a replacement owner survives delayed acknowledgment.
8. Startup Restart restores the original environment before relaunch and removes hidden launch mode.
9. Traditional Chinese and Korean persist and have complete, nonempty launcher catalogs.

Backend and renderer `tsc --noEmit` passed. `scripts/check-version.ts`, including the new README download checks, passed. Combined timed check commands took approximately six seconds. No full suite, bulk audit, repeated passing test, or live-account request ran. Large existing history-boundary and localization/browser suites were not selected. Other changed branches, including localized Pro tooltips, were reviewed manually.

The browser fixture used a fresh headless Chrome session and closed it in `finally`; no development app/server was launched. Temporary test-state directories were removed by their fixtures. There were no failed automated checks in this implementation pass.

## Delivery and remaining evidence boundaries

- At the implementation handoff, changes were in the source working tree on the named branch. Git publication was subsequently requested separately; no release, signing, or installed-app replacement is included.
- Package version remains `5.2.0-nekodex.3`; a future release needs a new version rather than replacing the published tag/assets.
- Existing unrelated untracked branding files and node_modules links were preserved. Account data, credentials, active routing and installed app were not changed.
- Production automatic expiry uses the runtime callback. DEV retains its existing direct-reap behavior because it has no launcher-owned cancellation callback.
- The tests prove the stated fixtures and source compilation. They do not establish live tunnel behavior on a particular proxy, a real ChatGPT account response, installed-app behavior, or linguistic/visual review of both new languages. Long translations have not been reviewed by a native-language editor or inspected in a launched UI.
- No account quota, context capacity, global throughput or upstream safety-rejection improvement is claimed.

## Source-publication follow-up

The user authorized commit and push. Before publication, the same source passed a Vite renderer bundle build in 0.60 seconds, in addition to the focused runtime/logic tests and TypeScript evidence above. The temporary bundle output was removed. No installed application or development server was started, and passed tests were not repeated.

The renderer bundle contains approximately 457.29 kB JavaScript (141.78 kB gzip) and 84.10 kB CSS (15.28 kB gzip); these are emitted asset sizes, not RAM measurements or a comparison with an upstream bundle. Source inspection shows matching runtime dependencies, a default task ceiling of 16 versus upstream v5.0.7's 5, and one browser host per opened/enabled account. The ceiling does not preallocate that many tabs. Multiple enabled account sessions and more actual simultaneous turns can therefore consume materially more resources. No matched-load CPU/RAM or end-to-end latency comparison was performed, so no overhead percentage or speed multiplier is claimed.
