# NEKODEX triwave implementation wave 1 of 3 — lane 7 handoff

- **Baseline:** `3740505b0107af9a83053fd523ae1ad30be36465` (`HEAD`)
- **Implementation scope:** `src/dev-chat/driver.ts` only, with this coordination handoff record.
- **DIDs fixed:** `T1-11-1`, `T3-11-1`
- **ABI boundary:** Native4, Native4DEV, and ZeroRisk4 identities and public tool schemas are preserved.

## Changes

- `T1-11-1`: `compactInput()` now sends `DEV_CHAT_SYSTEM_INSTRUCTIONS` only in Full mode and sends `DEV_CHAT_BROWSER_ONLY_INSTRUCTIONS` in browser-only mode. Manual and automatic compaction share this mode-specific path.
- `T3-11-1`: DEV tool rounds now reject a nonempty duplicate `call_id` already present in retained tool-call history or repeated within the current Responses envelope before emitting a simulated receipt or appending a tool output. Distinct IDs across valid rounds remain accepted.

## Preserved contract

- The existing simulated tool receipt shape and `simulatedFunction`/`DEV_CHAT_TOOLS` declarations were not changed.
- Connector names, public tool schemas, response routing, server-side compaction tool stripping, and Native4/Native4DEV/ZeroRisk4 behavior were not changed.
- No undeclared or malformed-tool validation was added; that separate `T2-11-1` finding remains outside this requested implementation wave.

## Verification and limitations

- No tests, typecheck, scripts, runtime/browser actions, production actions, release actions, or commit were performed, per lane instructions.
- Manual review covered the changed helper, its call site before receipt generation, the compaction request construction, and the direct `config.mode` type (`"browser-only" | "full"`).
- The fix establishes source-level fail-closed behavior. It does not prove live Responses envelopes, live model behavior, browser/account availability, or production deployment.
