# Implementation wave 2, lane 4 — D08/D09 gateway correction

Baseline `3a66157` plus uncommitted implementation wave 1. Read the D08/D09 adjudication, lane 4's wave 1 report, current `mcp-server.ts` diff and its direct `codex_exec` / `codex_tool_call` callers. Manual source review only; no test, typecheck, suite, runtime, benchmark, release or commit.

**Result: no further source edit.** The wave 1 change already handles both assigned cases:

- **D08:** Both generated nested-call paths check `result.isError === true` before their generic content emitter. A descriptive throw carries the nested tool name, bounded text from all text blocks and bounded serialized `structuredContent` (or an unserializable marker). The generic `text` / `image` / `audio` emitters cannot mark a failed result; throwing uses the gateway's existing error path. Direct structured calls still relay `isError` and structured content through `asMcpResult`.
- **D09:** The generated command gateway selects exactly one native command from `ALL_TOOLS`. For selected `shell_command`, it rejects supplied `tty` or `max_output_tokens` before looking up or invoking the native function. Selected `exec_command` receives those fields unchanged. The direct `shell_command` path retains its own rejection and approval-schema checks.

No remaining material D08/D09 source defect was found in the assigned paths. The exact outer runtime error shape and nested native tool behavior remain for the parent's focused synthetic cases; truncated details and non-text media are not reproduced in a thrown message. Public MCP names, descriptions, schemas and Native4 / Zero Risk4 ABI pins were not changed. Changed path in this wave: this report only.
