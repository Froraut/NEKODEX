# NEKODEX correction wave 3, Sol medium lane 5

- Lane: 5
- Scope: final residual review of `fix1-05` and `fix2-05` for `main.cjs`, `state.cjs`, and `upgrade-readiness.cjs`, including owned `L08-recovery-error`
- Verification: manual changed-path and direct-caller review only; no tests, typechecks, scripts, broad audits, runtime actions, app starts, commits, push, or delegation

## New correction

### L08-recovery-error

The previous correction preserved `browserHost.destroy()` failures only in the startup recovery callback. The normal `requestQuit()` path and packaged launcher smoke path still called `browserHost.destroy()` before `browserControl.close()`, so a host teardown exception could skip control-server closure. `main.cjs` now routes all three paths through `closeBrowserResources()`: host destruction and control closure are attempted independently, the first host failure remains primary, and a control-close failure is appended when both fail.

## Reviewed with no new correction

- `cancelInstall()` is Promise-returning and its `main.cjs` caller awaits it after a failed `requestQuit()`. If cancellation fails, the original quit message remains the thrown primary error and the cancellation error is attached as `cause`.
- Runtime proof uses the existing supervisor `ownerPid`, `daemonPid`, and `tunnelPid` snapshot fields. Startup and runtime-profile mutations invalidate the complete proof record; snapshot publication reconciles child identity changes. No supervisor edit was needed.
- `state.cjs` still fails closed for incomplete positive MCP proof and clears dependent proof fields together.
- `upgrade-readiness.cjs` remains configuration/account identity only; runtime instance identity remains enforced by `main.cjs` as intended.
- Connector names, schemas, ABI, and parent-owned tests were left unchanged.

## Limits

This is source-level evidence only. No runtime, installed-app, account-side, or parent integration behavior was exercised in this lane.
