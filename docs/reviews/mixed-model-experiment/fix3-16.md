# NEKODEX correction wave3 Sol medium lane 16

- Lane: 16
- Identity: connector identity
- Review basis: `review3-16.{md,json}`, `fix1-16.{md,json}`, `fix2-16.{md,json}`, `coordinator-notes.md`, and current source
- Scope: `src/config.ts`, `launcher/electron/connector-identity.cjs`

## Result

Fixed one residual connector-boundary inconsistency:

- `src/config.ts` now canonicalizes `automaticAppName` before applying the 80-character limit, matching the already-canonicalized `appName` path.
- `launcher/electron/connector-identity.cjs` now trims before applying the 80-character limit and returns that canonical value.

This closes the reachable case where a connector name with surrounding whitespace was within the canonical limit but was rejected by the raw-length check in one caller. Legacy detection, mode/name equality, provider projection, and Electron setup/runtime identity checks now use the same trimmed value and length policy. `Codex Native4`, `Codex Native4 DEV`, `Codex Zero Risk4`, public schemas, and ABI remain unchanged.

## Correction register

- Root: canonical connector length validation occurred before canonicalization in two boundaries.
- Trigger: a current or custom connector name whose raw value exceeds 80 characters only because of leading/trailing whitespace.
- Change: trim first, then validate the canonical connector length in the TypeScript config parser and Electron validator.
- Introduced by: `fix1` boundary completion; the original wave1 fix canonicalized values but left these raw-length checks inconsistent.

## Manual review and limits

Reviewed the changed functions and direct identity callers in setup/config loading, runtime connector resolution, browser-host validation, browser-helper verification, provider projection, and browser-worker exact matching. Normal accepted, rejected, and verification failure paths remain reachable through existing callers; no cancellation or async resource lifetime is owned by these identity boundaries. No tests, typechecks, scripts, broad audits, runtime actions, app starts, commits, pushes, or delegation were performed. This is source-level evidence only and does not establish live ChatGPT connector approval, DOM state, installed-app behavior, or production behavior.
