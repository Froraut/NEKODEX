# NEKODEX correction wave 2 — Sol medium — lane 2

- Lane: 2 (`src/setup.ts`)
- Repository: `/Users/alex/Dev/nekodex`
- Source scope: `src/setup.ts`, its direct callers, and the current lifecycle/integration APIs needed for compatibility review.
- Verification: manual source review only. No tests, typechecks, scripts, broad audits, runtime actions, app starts, commits, pushes, or delegation.

## Result

No source change was justified. `review3-02.json` and `review3-02.md` contain no accepted setup roots, and the wave-2 review found no new setup defect introduced by the lane-1 or lane-3 edits already present in the shared tree.

`src/setup.ts` was unchanged at the start and remains unchanged at the end of this lane. No accepted root was newly corrected in this wave.

## Caller and compatibility assessment

- `src/setup.ts` continues to call `getServiceStatus`, `installService`, `restartService`, and `uninstallService` with their existing synchronous or asynchronous shapes. Lane 1's service changes preserve these call shapes while adding bounded status and mutation behavior internally.
- `src/setup.ts` continues to call `getTunnelServiceStatus`, `installTunnelService`, `stopTunnelService`, `restartTunnelService`, and `uninstallTunnelService` with the existing shapes. Lane 1's optional `AbortSignal` parameters do not require a setup adapter because setup has no lifecycle signal to propagate; the existing calls remain valid and preserve their current sync/async behavior.
- Manual cancellation feedback remains owned by the tunnel-service layer. Setup observes thrown cancellation/status failures through its existing fail-closed status checkpoints and does not reinterpret cancellation as an unloaded or successful state.
- `installCodexIntegration(config, { replaceExistingRoute })` remains the final setup mutation. Lane 3's ownership changes are inside the integration writers and do not alter the setup caller contract or the route publication path.
- The coordinator's symlink compensation, initially absent managed-write handling, and removal compensation requirements are implemented at the integration shared-writer boundary. `setup.ts` does not directly own those Codex integration file writes, so adding compatibility code there would be speculative and could change successful setup paths.

## Integration requirements

Lane 1 must retain its bounded launchd status and mutation behavior, classify only the documented missing-service result as unloaded, and preserve unknown status as an error. The current setup callers are compatible with that contract.

Lane 3 must retain inode-aware symlink ownership, expected-byte guards for managed writes including absent-file cases, and guarded removal compensation. Setup should continue treating integration failure as the primary setup failure and rely on the integration writer's compensation; no setup-side wrapper or alternate publication path is required.

The source review establishes source-level compatibility only. It does not establish live launchd behavior, runtime proof publication, installed-app behavior, ChatGPT connector approval, or external authorization state.
