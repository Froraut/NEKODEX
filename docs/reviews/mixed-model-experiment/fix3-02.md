# NEKODEX correction wave 3 — Sol medium — lane 2

- Lane: 2 (`src/setup.ts`)
- Repository: `/Users/alex/Dev/nekodex`
- Scope: current `src/setup.ts`, its direct setup callers, and the lane 1 lifecycle and lane 3 integration APIs required for compatibility review.
- Verification: manual source review only. No tests, typechecks, scripts, broad audits, runtime actions, app starts, commits, pushes, or delegation.

## Result

No source change was justified. The current tree contains no accepted setup root from `review3-02`, and the wave 1 and wave 2 reports identify no newly introduced setup compatibility defect.

`src/setup.ts` remains unchanged. The correction ledger records no new roots because this wave records only corrections made by this lane.

## Compatibility and lifecycle review

- Setup calls `getServiceStatus` synchronously and continues to call `installService` synchronously. `restartService` and `uninstallService` remain awaited asynchronous operations at their existing call sites.
- Setup calls the tunnel-service status probe synchronously, and awaits `stopTunnelService`, `restartTunnelService`, and `uninstallTunnelService` while retaining their existing optional-signal-compatible signatures. No setup signal exists that should be forwarded.
- Tunnel validation keeps the existing success and failure ownership boundaries: a successful connect is checkpointed before readiness polling, validation cleanup runs after the readiness result, and a failed connect before that checkpoint is preserved for manual recovery.
- `installCodexIntegration(config, { replaceExistingRoute })` remains the final setup mutation. Lane 3 ownership and compensation remain inside the integration writers; setup does not need a wrapper or alternate route path.
- The current catch and rollback paths preserve the primary setup error and append bounded rollback failures. Unknown lifecycle state remains fail-closed instead of being reinterpreted as unloaded or successful.

## Limits

This establishes source-level compatibility and reachability only. It does not establish live launchd behavior, installed-app behavior, ChatGPT connector approval, account-side state, or external authorization. The parent-synchronized route diagnostic files and reserved test files were not edited.
