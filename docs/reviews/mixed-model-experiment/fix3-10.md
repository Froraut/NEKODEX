# NEKODEX correction wave 3 — lane 10 (dev)

- Working tree: `/Users/alex/Dev/nekodex`
- Scope: final residual correction after fix1 and fix2 in the four owned files
- Verification: manual source review only; no tests, typechecks, scripts, broad audits, runtime or app starts, commits, pushes, or delegation

## Result

Corrected one residual under `L10-broker-startup-rollback` in `src/adapters/chatgpt-web/turn-broker.ts`.

The successful `server.listen()` callback performed its socket identity and permission operations outside a rejection boundary. If `lstatSync()` or `chmodSync()` failed after the listener was created, the startup promise could remain pending and the transactional rollback path would never run. The callback now catches those failures and rejects the startup attempt, allowing the existing attempt-scoped server close, identity-checked socket cleanup, registry cleanup, and original-error propagation to run.

No changes were needed in `src/dev-chat/driver.ts`, `src/dev-chat/cli.ts`, or `src/dev-chat/transport.ts`. The malformed call boundary, custom/freeform string ABI, optional `AbortSignal` propagation, interactive cancellation path, and successful transport cleanup remain source-reviewed. Connector identities and parent-owned diagnostic/test files were left unchanged.

## Focused source review

Reviewed the corrected `server.listen()` success/failure path, its direct `start()` rollback, `close()` ownership checks, `startDevChatTransport()` lifetime, `DevChatDriver.send()` callers, automatic compaction propagation, and interactive SIGINT handling. The normal success path resolves after the listener identity is captured and permissions are set. Failure from those post-listen operations now reaches the existing rollback path. The rollback still unlinks only a socket with the startup attempt's captured device/inode identity, so a replacement or another broker's live socket is not removed.

No runtime proof was collected because the user reserved this lane for source correction only and explicitly prohibited tests, scripts, runtime starts, and broad verification. Parent integration remains the source/runtime boundary for its reserved checks.
