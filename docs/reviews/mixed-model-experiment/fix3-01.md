# NEKODEX correction wave 3, Sol medium lane 1

- Repository: `/Users/alex/Dev/nekodex`
- Review input: `review3-01`, `fix1-01`, `fix2-01`, and `coordinator-notes.md`
- Scope: `src/service.ts`, `src/tunnel-service.ts`, `src/process.ts` (lifecycle support only)
- Verification mode: manual source and diff review only; no tests, typechecks, scripts, broad audits, runtime actions, app starts, commits, pushes, or delegation

## Result

Applied two residual corrections: one to the fix2 implementation for `L01-launchd-print-unknown`, and one baseline lifecycle gap under `L03-launchd-mutation-deadline`.

`isMissingLaunchdService` still allowed arbitrary same-line text after the recognized `system` or `user` domain because its suffix was optional and unconstrained. A nonzero `launchctl print` response could therefore include the absent-service sentence plus a diagnostic tail and still be treated as unloaded. The classifier now requires the complete trimmed combined output to match the recognized absence form exactly. Unknown nonzero results continue to throw from both service status readers, so lifecycle callers remain fail closed.

No changes were needed in `src/service.ts` or `src/tunnel-service.ts`. Their existing status, timeout, mutation, cancellation, normal success, failure, and compensation paths were manually re-read with their direct callers. The synchronous `runCommand` and asynchronous lifecycle contracts remain unchanged, and no resource ownership or cleanup behavior was altered.

The direct setup rollback caller `bootstrapRestoredDefinition` still reached `runChecked("launchctl", ["bootstrap", ...])` without an explicit timeout. Because `setup.ts` is outside this lane's ownership, `runChecked` now supplies the same bounded 20-second default only when a `launchctl bootstrap` or `bootout` call omits a timeout. Existing explicit per-operation timeouts remain authoritative, and unrelated commands are unchanged.

## Compatibility and limits

Public service status fields, connector identities and schemas, launchd labels, timeout arguments, and caller signatures remain unchanged. The exact classifier intentionally recognizes only the known absent-service response; a differently formatted nonzero response remains unknown and blocks lifecycle mutation. Source review does not establish installed-app, launchd, runtime, account-side connector, or live cancellation behavior.
