# NEKODEX correction wave 2 — Sol medium — lane 6

- Lane: 6 (`launcher/electron/account-pool.cjs`)
- Repository: `/Users/alex/Dev/nekodex`
- Source scope: `launcher/electron/account-pool.cjs` only; report files are the requested lane artifacts.
- Verification: manual source and direct-caller review only. No tests, typechecks, scripts, broad audits, runtime actions, app starts, commits, pushes, or delegation.

## Result

One residual correction was made in the existing fix1 check-invalidation path. The other fix1 lane roots remain unchanged after review because their guards are reachable through the existing direct callers and preserve the normal account-selection and turn-acquisition paths.

## Correction

- `L06-check-invalidation-publication`: `checkAccount()` now includes `host.ready()` and the active-operation guard inside the existing `try`/`catch`/`finally` scope. If readiness or the active-operation guard fails after fix1 has invalidated evidence, the failure path removes any same-epoch connector claim and `finally` publishes the cleared account snapshot. The renderer therefore cannot retain the previous positive check solely because the first readiness await failed.

## Manual review and integration requirements

The direct callers remain unchanged: the renderer and main-process IPC continue to call `checkAccount(id, connector)`, and the control server continues to use `beginTurn()` with optional effort and routing inputs. I reviewed the fix1 guards around `checkAccount()`, `beginTurn()`, `openAccountLogin()`, and balanced account eligibility, including their sync/async boundaries and cleanup paths. No source change was justified for login selection, fresh-turn currentness, balanced evidence, selected-primary compatibility, manual cancellation, or unrelated atomic-file rollback behavior.

Integration must retain the ordering that `checkAccount()` invalidates capability and connector evidence before its first await, keeps all post-await publication behind the captured epoch, and publishes the invalidated state on every failure path. The parent owns focused verification and any cross-lane integration check.

## Limits

This establishes a source-level failure-publication contract only. It does not prove live browser readiness, account-side connector approval, installed-app behavior, runtime interleavings, or account/ChatGPT state.
