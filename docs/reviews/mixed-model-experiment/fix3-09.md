# NEKODEX correction wave3 Sol medium lane 9 gateway

- Lane: 9
- Scope owner: `src/adapters/chatgpt-web/mcp-server.ts`, `src/adapters/chatgpt-web/index.ts`
- Result: no-change final correction pass

The actual current source was reviewed after wave2. `withClaimedTurn` still preserves a rejected MCP action as the primary failure while settling activity, and uses settlement failure as primary only after a successful action. `claimTurn` retains the same protection for failed claim cleanup, while `invoke` preserves transport failure and release failure ordering.

`prepareWith` still keeps preparation failure primary when asynchronous broker revocation fails, and retains the token when revocation is uncertain. `finishBrowserOutcome` still preserves an existing browser outcome error over asynchronous revoke failure and only exposes revoke as primary for an otherwise successful browser outcome. Manual and automatic cancellation paths retain their fast client-facing cancellation, asynchronous capability revoke, and separate physical settlement ownership.

No concrete residual error introduced or missed by the current patches met the correction threshold. No source files were changed. The gateway freeform JSON envelope remains the explicit design limit recorded by review3. Connector identities, public schemas, ABI, and parent-owned route diagnostics remain untouched.

Manual source review only. No tests, typechecks, scripts, broad audits, runtime actions, app starts, commits, pushes, or delegation were performed, per the lane instruction.
