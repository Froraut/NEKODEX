# Coordinator integration observations

- Source review denominators count only candidate defects: 35 in Luna review1, 24 in Luna review2. Some Sol reports number confirmed safeguards/zero-finding statements; those are excluded from precision denominators.
- Review1: 30 accepted claim instances map to 27 roots. Review2: 20 accepted claim instances map to 19 roots, seven additional to review1. Sol adds one login-probe root. Review2's observation-error claim is partially correct but its cached-snapshot mechanism is wrong; raw adjudication retains this distinction.
- Initial relative skill lookup failed in part of review2; absolute path was supplied during the wave. One completed diagnostic lane did not re-read it. All later wave prompts use absolute paths.
- Agent-authored UTC stamps are not used for timing because some are inconsistent with dispatch. Measurements use coordinator tool dispatch and collection stamps.
- After fix1, inspect symlink rollback carefully: atomic writes replace target inode, while writeFileSnapshot now enforces the original target inode. A rollback using the original snapshot must still restore an owned replacement; external replacements must remain protected. Also inspect initially absent managed-write targets, where expectedData is undefined, and general removal compensation.
- A coordinator concern that RuntimeSupervisor lacked snapshot() was withdrawn before implementation: snapshot(status, detail) exists at line 449 in baseline. An exact empty-argument search had missed the declaration. This is not a defect and must not generate a patch.
- Terra's capacity errors are service availability failures, not successful zero-change reviews. User authorized a same-model retry, then model replacement only if that retry fails.
- During correction wave2 integration, coordinator synchronized new `profile-invalid` and `profile-unreadable` issue codes with launcher/electron/route-diagnostics.cjs validation and launcher/src/RouteDiagnostics.tsx presentation. Without this, source diagnostic improvements from fix1 would be rejected at the Electron boundary. These two files are coordinator changes, not Sol editor productivity.
- Final coordinator rejected fix3 lane8's claimed listener-registration race and removed its redundant recheck: the initial check and listener registration run synchronously with no await/yield, and ordinary ChildProcess event callbacks cannot interleave that span. The independently useful startup recovery result correction is retained. Raw agent evidence remains, but this claim is excluded from accepted correction counts.
- Final read-only host observation queried a unique nonexistent launchd label (no lifecycle mutations). macOS returned status 113 and `Bad request.\nCould not find service "..." in domain for user gui: 501\n` in 0.007 seconds. All agent classifiers rejected this legitimate absence response. Coordinator restored a complete-output grammar for exactly the known prefix/domain form with status 113, while preserving rejection of unrelated diagnostics. The service fixture now uses that observed format. This is a parent correction to fix1/fix2/fix3, not evidence of agent success.
