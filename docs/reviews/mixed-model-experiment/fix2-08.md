# NEKODEX correction wave 2 — Sol medium lane 8 recovery

- Lane: 8
- Scope: recovery review of the accepted roots from `review3-08.json` after Luna fix1
- Verification: manual source review only; no tests, typechecks, scripts, benchmarks, runtime/app starts, commits, push, or delegation.

## Result

No source change was made in this wave.

`L08-helper-stdio` and `L08-helper-response` are already corrected in the current `launcher/electron/browser-helper-verifier.cjs` by fix1. The helper now retains the exact child until both process `exit` and stdio `close` are observed, and defers an exit failure until the close boundary so buffered protocol output can be consumed. Direct callers continue to await the same async operation and consume the existing response shape.

`L08-recovery-error` remains partial and requires the lane 5 change in `launcher/electron/main.cjs`. The assigned scope forbids editing `main.cjs`; `startup-recovery.cjs` cannot restore an exception that its cleanup callback has already discarded. No safe residual correction was identified in either owned file.

The coordinator's symlink/inode, absent-target, and external-edit compensation note does not identify a path in `browser-helper-verifier.cjs` or `startup-recovery.cjs`; changing either file for that concern would be speculative. The rejected `L08-startup-timeout` root remains rejected under the review3 adjudication.

## Integration requirements

Lane 5 must retain the first `browserHost?.destroy()` failure, continue independent tray/window/control cleanup, and return or reject a bounded cleanup failure. Then `recoverStartupFailure()` can report `cleaned: false` while preserving the startup failure as primary when both failures exist. No runtime proof was made by this lane.
