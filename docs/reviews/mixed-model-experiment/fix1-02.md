# NEKODEX correction wave 1 — Luna medium — lane 2

- Lane: 2 (`src/setup.ts`)
- Repository: `/Users/alex/Dev/nekodex`
- Source scope: `src/setup.ts` only; report files are the requested lane artifacts.
- UTC start: `2026-09-15T20:49:42Z`
- UTC end: `2026-09-15T20:50:03.300Z`
- Verification: manual source review only. No tests, typechecks, scripts, benchmarks, runtime actions, app starts, commits, pushes, or delegation.

## Result

No source change was justified. `review3-02.json` and `review3-02.md` contain zero accepted roots for lane 2 (`roots: []`; four accepted zero-finding conclusions and one design-limit conclusion). The accepted roots are outside this lane: lane 1 concerns launchd status/deadline behavior in `src/service.ts` and `src/tunnel-service.ts`; lane 3 concerns Codex integration ownership in `src/codex-integration.ts` and `src/codex-integration-shared.ts`.

## Caller and compatibility assessment

The direct setup callers remain compatible with the lifecycle APIs currently present in the shared tree:

- `src/setup.ts` calls `getServiceStatus`, `installService`, `restartService`, and `uninstallService` using their current signatures, and records status-probe exceptions as unknown setup state before rollback decisions.
- `src/setup.ts` calls `getTunnelServiceStatus`, `installTunnelService`, `stopTunnelService`, `restartTunnelService`, and `uninstallTunnelService` using their current optional-signal signatures. No lane 1 API change is present in this checkout that requires a setup-side adapter.
- `installCodexIntegration` is the final setup mutation and is called with its existing `{ replaceExistingRoute }` options. Lane 3's ownership changes belong to the integration writers; there is no accepted integration API change requiring a setup caller rewrite in this tree.
- `src/cli.ts` consumes the unchanged `SetupResult` and `src/dev-chat/cli.ts` consumes the unchanged `DevProfileSetupResult`. Connector names and schemas remain untouched, including Native4, Native4 DEV, and ZeroRisk4 behavior.

## Counterevidence against forced fixes

Changing setup to compensate for lane 1's accepted roots would be incorrect under the exclusive file scope: the current `service.ts` and `tunnel-service.ts` implementations still expose the old status semantics and mutation call shapes, while the accepted fix targets are those modules themselves. Adding speculative timeout arguments or reinterpreting `loaded` in setup would either not affect the underlying blocking/nonzero-result defect or would change setup's existing ownership and rollback behavior without an accepted setup root.

Likewise, changing setup's `installCodexIntegration` call to anticipate lane 3 would invent an API contract not present in the current source and could alter external authorization or route replacement behavior. The lane 3 accepted roots are correctly localized to the integration writer layer.

## Residual limitations and cross-lane integration

Lane 1 must update lifecycle status classification and bounded launchd status/mutation operations in its assigned files. After those changes land, the setup callers should be manually re-read for signature compatibility and fail-closed behavior; this lane intentionally does not edit them speculatively.

Lane 3 must enforce expected-byte ownership for managed integration writes and guarded catalog/cache deletion in its assigned files. Setup should continue treating integration failure as the primary setup failure and rely on the integration layer's compensation; no setup change is required by the reviewed API.

The review remains source-level. It establishes neither live launchd behavior nor installed-app, account, connector approval, or external authorization state.
