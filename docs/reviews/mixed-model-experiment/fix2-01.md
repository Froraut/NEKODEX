# NEKODEX correction wave 2, Sol medium lane 1

- Repository: `/Users/alex/Dev/nekodex`
- Review input: `docs/reviews/mixed-model-experiment/review3-01.json`, `review3-01.md`, `fix1-01.json`, `fix1-01.md`, and `coordinator-notes.md`
- Scope: `src/service.ts`, `src/tunnel-service.ts`, `src/process.ts`
- Verification mode: manual source review only, as requested; no tests, typechecks, scripts, broad audits, runtime actions, commits, pushes, or delegation.

## Result

Applied one residual correction to the fix1 implementation for L01. No changes were needed in `src/service.ts` or `src/tunnel-service.ts` during this wave.

### L01 — exact absent-service classification

`isMissingLaunchdService` in `src/process.ts` used a multiline regular expression. That allowed a nonzero `launchctl print` result containing a matching absence line plus unrelated diagnostics to be accepted as an absent service. The classifier now requires the complete trimmed combined output to match the recognized absence form. Unknown nonzero results continue to throw from both service status readers, preserving fail-closed lifecycle behavior.

## Compatibility and scope

The existing synchronous and asynchronous caller contracts remain unchanged. Public service status fields, connector names and schemas, manual cancellation behavior, bootstrap/bootout timeout arguments, and the existing primary-error/compensation-error paths were preserved. Only `src/process.ts` and this wave's review artifacts changed in this lane.

The coordinator's symlink rollback, initially absent managed-file, and removal-compensation concerns were reviewed through the direct setup callers. Their implementation belongs to setup's snapshot/restore ownership and was not changed from this lane, which owns only the three listed service/process files.

## Integration requirements

Parent integration should retain the tightened exact classifier together with fix1's shared helper import and timeout changes. Review the setup merge as one unit for atomic replacement of symlink targets, restoration of an initially absent target, and preservation of external edits after service or tunnel removal. Parent-owned focused verification should cover the recognized absent response, a matching line with extra diagnostics, an unknown nonzero response, and the existing timeout/error propagation paths. No verification was run in this lane.
