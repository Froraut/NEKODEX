# NEKODEX correction wave3, lane 14

- Accepted root: `L14-manual-cancel-release`
- Changed source: `launcher/electron/browser-host.cjs`

## Correction

The periodic manual orphan reaper now preserves an exact-owner tab while launcher-owned runtime cancellation is `pending` or `failed`. Previously, a helper-process exit or reaper tick could call `removeTurnTab(tab, true)` during either state, releasing the browser surface before cancellation settled or while the failed cancellation still needed an explicit retry. The tab remains available for acknowledgement, retry, and recovery; normal running-tab orphan cleanup remains unchanged.

## Evidence and limits

Manual review covered the current `reapExpiredTurnTabs` path, `startManualCancellation`, `cancelManualTab`, `cancelManualTurn`, `endManualTurn`, `removeTurnTab`, the runtime supervisor cancellation contract, and the authenticated control-server caller. No tests, typechecks, scripts, broad audits, runtime actions, app starts, commits, pushes, or delegation were run, per lane instructions. This is source-level evidence only; it does not prove live runtime acknowledgement or browser DOM state.
