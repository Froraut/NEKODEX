# NEKODEX correction wave2 Sol medium lane 4

- Lane: 4 (diagnostics)
- Repository: `/Users/alex/Dev/nekodex`
- Baseline review: `review3-04.{md,json}`
- Prior correction: `fix1-04.{md,json}`
- Review mode: manual source and direct-caller review only
- Scope: `src/doctor.ts`, `src/cli.ts`, and `src/route-diagnostics.ts`

## Correction

`L04-profile-probe` had one residual identity-loss case after fix1. When the selected profile passed the existence check but `readBoundedUtf8File()` failed, the report emitted `profile-unreadable` but left `profilePath` as `null`, because the path was derived only from successfully read profile bytes. `diagnoseCodexConfiguration()` now retains the safe selected-profile path when `profileReadError` is true. A profile that was simply absent still keeps `profilePath: null`, and unsafe profile names still resolve no file.

This preserves the selected target identity without exposing profile contents or changing the public diagnostics shape. `src/doctor.ts` and `src/cli.ts` required no additional source change: fix1 already isolates the independent login and Codex probes, and `doctorCommand()` already serializes the aggregate report returned by `runDoctor()`.

## Actual changes since wave start

- Changed `src/route-diagnostics.ts` only: preserve `profilePath` for a safe selected profile whose bounded read failed.
- Added this wave report and its machine-readable companion.
- No tests, typechecks, scripts, broad audits, runtime actions, commits, pushes, or delegation were performed.
- The pre-existing fix1 edits in `src/doctor.ts` and `src/route-diagnostics.ts` were reviewed and left intact except for the path-preservation correction above.

## Integration requirements

- Parent must reconcile the concurrent source tree before integration.
- The launcher route-diagnostics validator and UI consumer must accept and present fix1's `profile-invalid` and `profile-unreadable` issue codes; that consumer is outside this lane's owned source scope.
- Parent must review the coordinator's cross-lane warnings about symlink compensation after owned atomic inode replacement, initially absent managed-write targets, external edits, manual cancellation feedback loops, and runtime proof publication reachability. No direct actionable caller in the three owned files justified changing those paths here.
- The result remains source-level evidence only; no runtime or account-side behavior is established.
