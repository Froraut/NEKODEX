# NEKODEX correction wave1 Luna medium lane 4

- Lane: 4 (diagnostics)
- Repository: `/Users/alex/Dev/nekodex`
- UTC start: `2026-09-15T20:51:31Z`
- UTC end: `2026-09-15T20:52:20Z`
- Verification: manual diff and direct-caller review only, as requested. No tests, typechecks, scripts, benchmarks, runtime actions, app starts, commits, pushes, or delegation.

## Fixed roots

- `L04-codex-probe`: wrapped `inspectCodexIntegration()` in its own doctor probe boundary. A journal read or validation exception now produces a structured `codex` error check and the service, proxy, tunnel, and connector checks continue. `src/cli.ts` already serializes the returned `DoctorReport`, so no CLI fallback change was necessary.
- `L04-login-probe`: isolated the complete managed-Chrome login evidence and permission probe. A `secureFile()` or related filesystem exception now produces a structured `login` error check while later doctor checks continue. Chrome executable reporting remains an independent check.
- `L04-profile-probe`: separated root config and selected-profile reads. A valid root remains available when a selected profile read fails, which now reports `profile-unreadable`; malformed or non-table selected profile TOML reports `profile-invalid`; an absent or invalidly named selection remains `profile-unavailable`.

## Compatibility review

The `DoctorReport`, `DoctorCheck`, and `CodexRouteDiagnostics` public shapes remain unchanged. The route diagnostics command and its JSON serialization remain unchanged. Connector names and schemas (`Native4`, `Native4 DEV`, and `ZeroRisk4`) and external authorization behavior were not edited. No files outside the exclusive source scope and this lane report were changed by this correction pass.

## Residual limitations

This pass does not prove runtime behavior or account-side connector attachment. Error detail uses the existing doctor detail path; the new boundaries classify the failure and retain the aggregate report, but do not make a failing filesystem or journal state recoverable. The existing early return for invalid main configuration remains a deliberate design boundary because later checks require validated configuration.

## Required cross-lane integration

Parent integration should review the two new route issue codes (`profile-invalid`, `profile-unreadable`) against any consumers that compare issue-code lists, and reconcile this lane with concurrent edits before merging. The parent owns the reserved focused verification pass; this lane performed no executable verification.
