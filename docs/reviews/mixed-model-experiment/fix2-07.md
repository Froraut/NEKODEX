# NEKODEX correction wave2 — Sol medium lane 7 supervisor

- Repository: `/Users/alex/Dev/nekodex`
- Scope: `launcher/electron/runtime-supervisor.cjs` and its direct callers
- Inputs: `review3-07.{md,json}`, `fix1-07.{md,json}`, `coordinator-notes.md`
- Lane: 7, Sol medium replacement after Terra capacity failure

## Result

No additional source correction was justified in this lane. The three accepted
review3 roots are already represented by the shared fix1 edits in
`launcher/electron/runtime-supervisor.cjs`; no new root was confirmed during
the wave2 review.

## Review outcome

- `L07-queue-reservation`: remains corrected. `runTunnelCommand` reserves its
  successor before the first await, preserves a cancelled waiter's slot until
  predecessor release, and releases an owned slot on provider failure or
  cancellation.
- `L07-post-settlement-kill`: remains corrected. `stopChild` no longer sends
  its unconditional group `SIGKILL` after exit/close settlement. The bounded
  pre-settlement graceful/forced termination remains, including the
  tunnel-control exit path needed for descendant cleanup before close.
- `L07-force-control-cancel`: remains corrected. Every tunnel-control child
  has a supervisor-owned controller; forced shutdown aborts tracked children,
  waits within the bounded policy, records non-settlement, and does not enqueue
  a replacement stop while an earlier control child remains unsettled.
- No residual defect was confirmed in the direct callers for local inventory,
  health discovery, managed startup, graceful stop, or forced shutdown.

## Actual changes since wave2 start

No source files were changed by this lane. Only this wave2 report and its JSON
record were added. The pre-existing shared working-tree changes in
`runtime-supervisor.cjs` are Luna fix1 edits and remain subject to parent-lane
reconciliation.

## Integration requirements

The parent lane must reconcile the shared diff before focused verification.
Preserve synchronous queue-slot reservation before proxy-environment awaits,
the cancellation-controller removal on `close`, bounded forced settlement and
partial-cleanup reporting, and the distinction between post-settlement
supervisor cleanup and pre-settlement descendant cleanup. Do not restore the
removed unconditional post-settlement group signal, and do not replace the
bounded force path with overlapping tunnel-stop commands.

Manual source review only. No tests, typechecks, scripts, broad audits,
runtime actions, commits, push, or delegation were performed by this lane.
