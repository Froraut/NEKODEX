# NEKODEX correction wave1 Luna medium lane 16

- Lane: 16
- UTC start: 2026-09-15T20:50:07Z
- UTC end: 2026-09-15T20:51:23Z
- Root: `L16-padded-identity` (accepted, P2)

## Result

Fixed the accepted padded connector identity root in the two authorized source files.

- `src/config.ts` now trims connector fields at the config boundary before legacy detection, migration, active-mode equality checks, and returned provider configuration. The exported legacy predicate also trims its input so the existing setup migration caller cannot miss a padded retired name.
- `launcher/electron/connector-identity.cjs` now trims input in the legacy-name predicate, covering raw values observed by Electron dev-profile and helper verification callers.
- Existing public connector names and schemas remain unchanged: `Codex Native4`, `Codex Native4 DEV`, and `Codex Zero Risk4`.
- A persisted value such as `"Codex Native3 "` is now recognized as retired and follows the existing migration/error path. A padded current or custom name is returned in canonical trimmed form, keeping Electron and browser-worker exact comparisons aligned.

## Manual review and limits

Re-read direct callers in `src/setup.ts`, `src/adapters/chatgpt-web/browser-worker.ts`, `launcher/electron/runtime.cjs`, `launcher/electron/main.cjs`, and `launcher/electron/browser-helper-verifier.cjs`. The accepted defect was confirmed; the three rejected review claims were not forced into changes. No tests, typechecks, scripts, benchmarks, runtime actions, app starts, commits, pushes, or delegation were performed, per lane instruction. This is source-level evidence only and does not establish ChatGPT-side connector creation, approval, cache state, live DOM state, or production behavior.

## Residual limitations and cross-lane integration

The fix does not edit the browser-worker or runtime lanes. Their existing exact matching and runtime guards must consume the canonical names produced by `parseConfig`/`loadConfigForSetup`; integration should preserve that data flow. A caller that manually constructs an `AppConfig` without passing through config parsing can still provide a non-canonical custom name; that is outside the persisted config boundary fixed here. External authorization behavior is unchanged.
