# NEKODEX correction wave2, lane 14

- UTC start: `2026-09-16`
- Accepted root: `L14-manual-cancel-release`
- Changed source: `launcher/electron/browser-host.cjs`

## Correction

The fix1 cancellation state could become permanently non-recoverable after a rejected runtime cancellation: a later explicit cancel saw `status: "failed"` and immediately returned `manual_cancel_failed` without attempting the launcher-owned cancellation again. The failed tab remains owned, so a later explicit attempt now starts a fresh pending cancellation and can succeed.

The synchronous `/v1/manual/cancel` path is also idempotent after acknowledgement. If a delayed or retried request arrives after the cancellation callback has already signalled the exact terminal owner and removed the tab, it returns the existing cancelled result for the matching helper PID instead of reporting an ownership mismatch. Exact owner and terminal status checks remain required.

No normal completion, runtime proof publication, connector identity, connector schema, symlink/atomic-file path, absent-file guard, or external-edit path was changed. The source-owned fix1 behavior remains: a running tab is retained until targeted runtime cancellation acknowledges, and rejection keeps the tab available.

## Evidence and limits

Manual review covered the fix1 diff, `BrowserHost.closeTab`, `startManualCancellation`, `cancelManualTurn`, `endManualTurn`, `AccountBrowserPool.ownerForTrace`, the authenticated control-server caller, the launcher runtime callback wiring, and the adapter's async cancellation/cleanup caller. `git diff --check` was the only focused mechanical check; no tests, typechecks, scripts, broad audits, runtime actions, app starts, commits, pushes, or delegation were run.

The correction is source-level evidence only. It does not prove a live runtime acknowledgement, browser DOM state, or account-side connector behavior. Parent integration must preserve the shared fix1 edits and cover the existing acknowledgement/rejection paths plus the new retry and post-acknowledgement idempotence cases within the parent's reserved fast verification.
