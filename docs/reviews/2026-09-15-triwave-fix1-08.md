# NEKODEX triwave fix 1 of 3, lane 8

Baseline: `3740505b0107af9a83053fd523ae1ad30be36465` (`HEAD3740505`).

## Handoff

Implemented the source-proven T3-14 status-read correction in the two authorized source files:

- `src/codex-integration-journal.ts`
- `src/codex-integration.ts`

## T3-14-1 fixed

`readJournal()` now accepts an internal `repair?: boolean` option, defaulting to the existing repairing behavior for lifecycle callers. When `repair: false`, it preserves journal parsing, validation, pending-state selection, and error behavior while suppressing:

- v2 marker copy compensation;
- pending JSON hook recovery writes;
- one-sided journal-copy creation;
- divergent journal-copy compensation.

`inspectCodexIntegration()` now requests `repair: false`, so route status, top-level status, and doctor inspection no longer repair Codex integration journal or hook files. `readCodexSubagentProtocol()` also uses `repair: false`, keeping model/protocol discovery observational.

Explicit recovery remains available only through the existing lifecycle callers: disconnect and uninstall continue to call `readJournal({ reconcileInactiveHook: true })`, and that path retains the pending active-primary/inactive-recovery proof before writing.

## ABI and scope

Native4, Native4DEV, ZeroRisk4, connector names, public schemas, tool names, and generation-4 ABI pins were not changed. No optional compensation redesign was included; the previously reported T2-10-2 concurrent-write rollback limitation remains outside this fix.

## Verification

No tests, typechecks, scripts, runtime/account actions, production actions, or commits were run. Manual review and `git diff --check` covered the changed paths only. The parent owns the final focused verification budget.

## Changed paths

- `src/codex-integration-journal.ts`
- `src/codex-integration.ts`
- `docs/reviews/2026-09-15-triwave-fix1-08.md`
