# NEKODEX — triwave fix 1, lane 15: DEV tool boundary handoff

- **Baseline:** `3740505b0107af9a83053fd523ae1ad30be36465` (`HEAD`)
- **Implementation scope:** `src/dev-chat/cli.ts` only for source code; this handoff file is the requested coordination artifact.
- **Source path:** `executeMessage` now wraps the driver event callback with a DEV protocol guard. The guard derives the declared function/custom surface from `DEV_CHAT_TOOLS`, including the namespace-qualified `mcp__dev_simulator__echo` entry.
- **T2-11-1 fixed at the CLI boundary:** undeclared names, browser-only outer tool calls, malformed function arguments, invalid custom input, and invalid large-context arguments fail before the driver can append the corresponding simulated output to retained history.
- **T3-11-1 fixed at the CLI boundary:** the guard seeds call IDs from retained chat history, rejects empty or reused receipt `call_id` values, and rejects a receipt whose tool name does not match the preceding call event. A duplicate in one response is rejected before its second output is appended.
- **Receipt contract:** only declared tool events may reach the renderer; accepted receipts must retain `simulated: true` and `side_effects_performed: false`.
- **ABI:** Native4, Native4 DEV, ZeroRisk4 identities and public tool schemas were preserved. `driver.ts`, parser code, and server code were not changed.
- **Verification:** no tests, typechecks, scripts, runtime, production, or Git operations were run. Manual review was limited to the changed CLI path and the cited wave reports.
- **Known limitation:** `DevChatEvent.tool_call` does not expose `call_id`; the duplicate check observes the receipt immediately before the driver appends its output. This still prevents the duplicate output from entering retained history, while the underlying driver/parser remains unchanged by the exclusive source scope.
