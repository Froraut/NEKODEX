# Upstream lane 06 — compaction and checkpoints

Reviewed 2026-09-15 against NEKODEX source at `359d8fc` and [coding iterations](2026-09-15-coding-iterations.md). Live GitHub issue bodies and PR file diffs were fetched with `gh api`; this is a focused source review, with no local runtime test. Counts across six assigned numbers: **2 already addressed (one issue/PR defect), 3 insufficient evidence for a current defect, 1 useful optional feature; 0 confirmed still-applicable concrete defects**. Thus there are five distinct topics after deduplicating #447/#448. #426's conditional pre-ACK risk is not counted as a confirmed defect.

| Upstream item | Assessment and current evidence | Recommendation |
| --- | --- | --- |
| [#447](https://github.com/miuuyy/codex-chatgpt-web/issues/447) + [#448](https://github.com/miuuyy/codex-chatgpt-web/pull/448) | **Already addressed at source level; one defect.** The issue supplies an isolated synthetic v5.0.6 compiler reproduction: front-trimming drops the older cumulative summary yet retains bulky tool output. The open PR changes `prompt.ts` to keep the newest readable summary, omit expendable history, disclose trimming, and error when required material will not fit; its diff changes only prompt compiler and focused tests. NEKODEX already has the same essential protections in `src/adapters/chatgpt-web/prompt.ts:663-669,690-716`, including `plainMessageText` for text parts, newest checkpoint, ordering, omission notice, and explicit budget failure. Multipart exits before legacy inline trimming at `prompt.ts:679-684`. Neither the upstream installed health check nor source parity proves a real long-task continuation. | Do not cherry-pick the PR. Preserve these invariants if the prompt compiler changes; parent may select one focused compiler case within its shared verification budget if this code is touched. |
| [#474](https://github.com/miuuyy/codex-chatgpt-web/pull/474) | **Useful optional feature.** Open PR offers a separate model/family/effort for eligible automatic Pro compaction, sampled when a logical compaction starts. Live diff adds `src/chatgpt-web-compaction-policy.ts`, `src/compaction-model-config.ts`, selector/controller/daemon/helper changes and tests; it explicitly retains original request identity for lookup, cancellation and retirement. NEKODEX currently has only ordinary Pro family preference (`src/adapters/chatgpt-web/index.ts:196-211`) and compaction ownership keyed to source request (`index.ts:889-918`); no `compactionModel` setting or separate execution policy appeared in current config/UI/adapter search. PR claims earlier installed successful compactions, but its later selector follow-up did not run a new paid end-to-end flow on that branch. | Consider as a product choice only if users need independent summary quality/cost control. If adopted later, keep source identity and execution choice separate and require exact visible family/effort before submission; avoid broad patch adoption. |
| [#444](https://github.com/miuuyy/codex-chatgpt-web/issues/444) | **Insufficient evidence for a current defect.** Windows Plus/Luna report combines forgetting the previous prompt, a Codex context-window error, and an absent Astra picker entry, without a discriminating trace or complete fresh-task comparison. NEKODEX uses a Luna checkpoint on eligible turns (`src/adapters/chatgpt-web/index.ts:427-432`), commits it only after a browser answer (`index.ts:474-478`), and disables separate Luna compaction (`src/server.ts:777`). This is a different route from #447's Pro fallback compiler. Source behavior alone cannot establish whether the reported Luna answer/checkpoint was captured, replayed, or omitted. The picker statement is an account/catalog observation, not evidence of compaction failure. | Ask for one privacy-safe two-turn trace showing checkpoint capture/apply and exact final error before attributing root cause. Keep catalog investigation separate. |
| [#426](https://github.com/miuuyy/codex-chatgpt-web/issues/426) | **Insufficient evidence for a current defect; conditional pre-ACK risk.** Reporter observed a 120-second `browser_surface_bootstrap_timeout` and a pending Codex turn with no terminal error under Bigger Context. Current source advertises multiplied context/auto-compact limits (`src/chatgpt-web-models.ts:164-168`) while each browser message keeps its base budget by design (`chatgpt-web-models.ts:206-224`); multipart records are partitioned and can fail explicitly (`src/adapters/chatgpt-web/prompt.ts:380-403,629-650`), and browser preflight has `context_length_exceeded` (`src/adapters/chatgpt-web/browser-worker.ts:1015-1021`). This does **not** prove the reporter's proposed flag mismatch. The automatic reaper closes the view (`launcher/electron/browser-host.cjs:1453-1467,1591-1613`), but that is not proof of an unhandled pending request: after Playwright acquires the page, a closed page can fail a DOM operation or be detected by `page.isClosed()` (`src/adapters/chatgpt-web/browser-worker.ts:4623-4646,5054-5056`). `runExclusive` then ends the launcher turn (`browser-worker.ts:4486-4519`); the helper emits `error` and `turn_settled` (`src/adapters/chatgpt-web/browser-helper-main.ts:312-344`); the client removes and rejects pending (`src/adapters/chatgpt-web/launcher-helper-client.ts:625-641,697-715`). A possible gap is earlier: after lease, `onPreparedSelected` awaits `promptSelection.wait()` before `runBrowserTurn` and its `browser_page` stage (`browser-worker.ts:4430-4486`; `browser-helper-main.ts:216-218,265-269`). Client preparation sends `prepared_selected_ack` or handles an actual rejection (`launcher-helper-client.ts:578-606`), but the report does not show which frame or await failed. | Inspect one exact trace for `prepared_selected`, its ACK, `browser_page started`, helper `error`/`turn_settled`, and client pending settlement. Only if the pre-ACK wait is shown to remain unresolved should the owner add a bounded failure at that precise await. Do not patch the reaper or label it `context_length_exceeded` on the current evidence. |
| [#424](https://github.com/miuuyy/codex-chatgpt-web/issues/424) | **Insufficient evidence of a current defect.** v5.0.6 report says a Bigger Context fallback browser turn was visible/completed, then Codex failed to continue with `trusted environment unavailable`; the stated error also says the stream disconnected before handoff completion, so browser completion does not establish accepted remote compaction. Current code gives compaction a bounded handoff and explicit failure (`src/adapters/chatgpt-web/index.ts:920-946,1105-1128`), stores exact completed-summary/source hashes for continuation (`src/adapters/chatgpt-web/compaction-continuation.ts:29-66`), and parses a current native environment claim from sibling content parts (`src/adapters/chatgpt-web/environment.ts:301-320`). It still logs trusted-environment rejection when `environmentStore.resolve` fails (`index.ts:860-870`), correctly refusing unproven authority. The [iteration review](2026-09-15-coding-iterations.md#final-implementation-map) reports persistence and cached-compaction interruption fixes (E13/E21), but those source repairs do not prove this exact live symptom is gone. | Capture one redacted canonical continuation request and handoff completion status if this recurs; compare native turn ID, summary format/hash, current environment claim and store result. Do not weaken the trusted-environment gate to hide the symptom. |

Priority: #447/#448 need no duplicate implementation. #426, #444 and #424 require exact current traces before new repairs; #426's pre-ACK wait is a conditional inspection target. #474 is an optional scoped design decision.
