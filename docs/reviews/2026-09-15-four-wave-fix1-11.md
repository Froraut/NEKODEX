# Implementation wave 1, writer 11 — D07 caller policy

Baseline `3a66157a8943a80bbbe299699cc96bec82721ba1`. Changed `src/codex-integration.ts` only as source; this report is the requested handoff. No tests, typechecks, benchmarks, installation, release, commit, or public schema changes. Reviewed the targeted source diff manually.

- Ordinary subagent/model reads, inspection (`route status` and `doctor`), and setup preflight pass `readJournal({ reconcileInactiveHook: false })`. Inactive v11 JSON hook reappearance is reported by inspection as an error, without hook deletion. Setup preparation also refuses the occupied inactive slot before attempting a new installation.
- Explicit disconnect and uninstall pass `readJournal({ reconcileInactiveHook: true })`, relying on writer 10 to interpret this solely as a pending inactive recovery when an old active primary and the corresponding inactive recovery copy prove intent. Equal inactive copies do not prove ownership. Both operations refuse an exact reappeared inactive entry; an occupied foreign slot or duplicate command is rejected by `restoredInactiveJsonHook`. Reconnect and install also refuse this inactive ambiguity. Uninstall no longer includes the inactive entry in its hook-write plan, and rechecks before journal deletion.
- Cross-lane API: `readJournal({ reconcileInactiveHook: false })` for ordinary callers; `readJournal({ reconcileInactiveHook: true })` for explicit disconnect/uninstall only. `restoredInactiveJsonHook(journal)` is detection only in this file. Writer 10 owns the pending-intent predicate, its byte/config guards, and all journal implementation.

**Writer 10 follow-up:** current `recoverPendingJsonHookWrite()` first requires `journalConfigMatches(recovery)`, which includes `journalExternalHooksMatch(recovery)`. An inactive recovery copy with the exact hook still present therefore fails the prerequisite before the pending disconnect can remove it. Resolve this in `src/codex-integration-journal.ts` by separating restored-config proof from hook-state proof; retain exact journal-pair intent and expected hook bytes. This writer cannot edit that file. An exact hook reinserted after the final uninstall hook check is a remaining cross-file race without a transaction spanning hook bytes and journal deletion. Parent's five focused cases should include identical inactive copies plus exact reappeared hook, and the pending active/inactive pair.
