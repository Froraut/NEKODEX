# Implementation wave 2, lane 05 — DEV CLI

Baseline `3a66157a8943a80bbbe299699cc96bec82721ba1` plus uncommitted wave 1. Changed `src/dev-chat/cli.ts` and this report only. Read the adjudication D11/D18 rows, wave 1 lane report, and current setup/CLI diff; manually reviewed the direct close declarations. No tests, typecheck, runtime, benchmark, suite, production, or commit.

## D18 — remaining cleanup edge corrected

Wave 1 retained the original thrown operation value, even for a non-`Error` throw, and placed it before close failures in `AggregateError.errors`. The remaining edge was synchronous failure while *calling* `driver.close()`: constructing the old `Promise.allSettled` input array could throw before invoking `transport.close()` or collecting the primary exception. Both closes are now invoked independently through promise callbacks, so synchronous throws become settled rejections and the second close is still attempted. The original operation error is unchanged if both closes succeed. With cleanup failures, the original is first, followed by driver and transport failures in call order.

Interactive command failures handled and printed inside `interactive()` remain handled; only failures escaping that command loop are primary exceptions for outer cleanup. This preserves the existing interactive behavior.

## D11 — no CLI source change

The direct `dev setup` caller awaits `setupDevProfile` before printing success. The setup owner has added a pre-mutation, fail-closed check for a changed existing Full DEV profile: it requires an explicitly stopped old tunnel alias and rejects an unreadable, running, or indeterminate observation. The CLI has no verified owner/idle-drain contract and must not independently stop a foreign or busy tunnel. No `setup.ts` edit belongs to this lane. Parent should reconcile the setup guard's exact runtime-status proof and any live-owner races with writer 01; source inspection alone cannot prove an atomic launcher handshake.

Public Native4/Zero Risk4 identities and tool ABI were untouched. Parent retains the shared final five focused cases and 60-second budget.
