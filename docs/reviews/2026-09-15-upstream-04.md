# OPEN upstream lane 4 — Setup/catalog

Reviewed 2026-09-15 against checkout `359d8fc` and [coding iterations](2026-09-15-coding-iterations.md). Fetched the live GitHub bodies/comments for all nine numbers and the file patches for all three PRs with `gh api`. This is source review only: no account-bound reproduction, Windows run, code edit, or test. Existing working-tree edits in `tunnel.ts` / `tunnel-service.ts` were outside this lane. Issue/PR pairs are grouped below so an upstream patch is not counted as a separate defect.

| Upstream item | Current evidence and judgment | Recommendation |
| --- | --- | --- |
| [#483](https://github.com/miuuyy/codex-chatgpt-web/issues/483), Windows Free smoke answer visible but `Browser helper smoke timed out` | **Insufficient evidence; still applicable failure class.** The current helper waits up to 90 s for an ID-matched final `result`, and emits this exact timeout when none arrives ([browser-helper-verifier.cjs:50–142](../../launcher/electron/browser-helper-verifier.cjs)). Smoke only passes after `runBrowserTurn` returns the exact expected answer, then sends helper evidence ([browser-worker.ts:3865–3890](../../src/adapters/chatgpt-web/browser-worker.ts), [browser-host.cjs:3004–3027](../../launcher/electron/browser-host.cjs)). A visible answer does not prove that final turn completion or the helper result frame occurred. | For one failing trace, correlate helper operation ID, browser-turn terminal event, helper stdout frame, and response confirmation. Fix the observed missing transition only; do not lengthen the timeout from the issue alone. |
| [#452](https://github.com/miuuyy/codex-chatgpt-web/issues/452), Windows Plus: healthy install, absent picker entries, forced route 503 | **Insufficient evidence for one defect.** Current setup distinguishes installed route, served catalog and confirmed picker ([App.tsx:1329–1338](../../launcher/src/App.tsx), [main.cjs:150–173](../../launcher/electron/main.cjs)). Successful `/v1/models` increments a counter ([server.ts:1073–1105](../../src/server.ts)); routing diagnostics compare config/provider/override and current daemon identity before interpreting that counter ([route-diagnostics.cjs:12–55](../../launcher/electron/route-diagnostics.cjs)). Issue reports no fresh browser-turn directory for the 503; that suggests pre-browser failure, but does not localize it. | On affected host, capture exact `/v1/models` counter and routing report after picker open, then correlate a single forced `/v1/responses` request with bridge error and launcher-owned daemon state. Healthy doctor/smoke is not native model-list or turn proof. |
| [#443](https://github.com/miuuyy/codex-chatgpt-web/issues/443), Interrupt hook changed after setup | **Protection already present; report cause insufficient.** Exact refusal remains when a managed TOML hook command/order/markers differ ([codex-interrupt-hook.ts:318–449](../../src/codex-interrupt-hook.ts)). Existing JSON storage is now supported separately ([codex-integration.ts:110–142](../../src/codex-integration.ts)). The issue body provides the final error but no hook before/after evidence. | Compare redacted hook storage, command hash and journal ownership on the failing host. Preserve external edits; no blanket overwrite or assumption that #435 fixes this exact error. |
| [#438](https://github.com/miuuyy/codex-chatgpt-web/issues/438) + [PR #473](https://github.com/miuuyy/codex-chatgpt-web/pull/473) + [PR #478](https://github.com/miuuyy/codex-chatgpt-web/pull/478) | **Already addressed at source/UI level; host outcome unproven.** #438 comments distinguish repeated successful route setup from unobserved catalog verification, with a separate tunnel `EACCES` earlier ([discussion](https://github.com/miuuyy/codex-chatgpt-web/issues/438#issuecomment-5635453933)). #473 changes the renderer's pending/reinstall row; #478 changes troubleshooting and three locale strings. NEKODEX already routes the pending row to diagnostics and keeps Repair in details ([App.tsx:1463–1487](../../launcher/src/App.tsx)); copy names the ChatGPT desktop host, picker opening and routing check ([i18n.ts:305–306](../../launcher/src/i18n.ts)); health and monitor require a served catalog ([server.ts:943–944](../../src/server.ts), [main.cjs:150–173](../../launcher/electron/main.cjs)). [Coding iterations:49–60](2026-09-15-coding-iterations.md) are focused local checks, not Windows proof. | Retain the more explicit installed/catalog/picker states. For a recurring host report, read the catalog counter and routing report before reinstall; separate tunnel launch failure from Browser-only catalog. Do not reapply either PR mechanically. |
| [#437](https://github.com/miuuyy/codex-chatgpt-web/issues/437), intermittent `Codex Native2` absent from mention menu | **External/account-specific availability; current exact-identity path exists.** This reported legacy name is retired in NEKODEX; `Codex Native3` is the current public connector ABI ([architecture.md:73–82](../../docs/architecture.md)). The worker uses bounded mention attempts, checks exact row, detects legacy/DEV-only mismatches, and may refresh a stale catalog ([browser-worker.ts:3310–3358](../../src/adapters/chatgpt-web/browser-worker.ts)). Account-installed connector availability cannot be inferred from source. | For a current-name failure, collect actual menu rows, account/organization identity, selected pill and one bounded refresh trace. Avoid reverting to `Native2` or auto-selecting a near-name row. |
| [#418](https://github.com/miuuyy/codex-chatgpt-web/issues/418), no native tool call | **Insufficient evidence; real tool invocation still applicable acceptance gate.** The maintainer's [comment](https://github.com/miuuyy/codex-chatgpt-web/issues/418#issuecomment-5635459071) says completed turns had no local calls and asks for correlated connector/tool evidence. NEKODEX's setup can show model, tunnel and connector states, while smoke deliberately disables local tools ([browser-worker.ts:3865–3871](../../src/adapters/chatgpt-web/browser-worker.ts)); [coding iterations:74–80](2026-09-15-coding-iterations.md) do not establish live connector/model combinations. | Reproduce one task that truly requires a local read, then correlate selected connector, visible tool call or absence, MCP broker and native result. Do not mark this fixed from catalog verification or a no-tool answer alone. |
| [PR #435](https://github.com/miuuyy/codex-chatgpt-web/pull/435), existing `hooks.json` Interrupt storage | **Already adapted; residual repeated-disconnect guard now implemented at source level.** Live PR file diff changes 13 paths, including JSON parser, journal/recovery, launcher rollback and focused tests. NEKODEX credits and documents the integrated patch ([upstream-pr-435-integration.md:3–31](../../docs/upstream-pr-435-integration.md)); direct source selects existing JSON, tracks ownership and checkpoints symlinks ([codex-integration.ts:110–142](../../src/codex-integration.ts), [runtime.cjs:542–575](../../launcher/electron/runtime.cjs)). The prior repeat `disconnect` could return after checking only TOML and `readJournal()` could delete a matching inactive JSON hook first. The new route guard skips that read-time reconciliation for `disconnect`, checks the restored TOML route and journal-owned JSON slot, and rejects a present recorded hook, occupied external slot, or missing JSON file ([codex-integration.ts:511–534](../../src/codex-integration.ts), [codex-integration-journal.ts:311–380](../../src/codex-integration-journal.ts)). Other journal callers keep their previous reconciliation behavior. This is manual source/diff review only; no runtime or test proof. | Keep the integrated JSON behavior. The implemented repeat-disconnect check leaves foreign JSON content intact and refuses an inconsistent inactive journal. Follow account-bound reproduction separately if the original #443 Windows error recurs. |

**Count:** 9 numbered items reviewed, grouped into **6 independent report/PR topics** (#438/#473/#478 and #443/#435 deduplicated). Source-level behavior already addressed in 2 groups (#438 pair, #435 integration); 1 concrete residual source edge case implemented (inactive v11 JSON hook check); 4 report groups need host/trace evidence (#483, #452, #443, #418), while #437 depends on account connector availability. No blanket upstream adoption recommended.

Implementation follow-up (same task): only `src/codex-integration.ts`, `src/codex-integration-journal.ts` and this lane report changed. No tests, added cases, or commit; the parent owns the shared verification budget.
