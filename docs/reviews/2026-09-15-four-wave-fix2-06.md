# Four-wave implementation wave 2, lane 06

Frozen baseline `3a66157` plus wave 1. Exclusive source writer: `launcher/electron/main.cjs`. Manual review of the changed paths and direct state/supervisor contracts; no tests, typecheck, runtime launch, packaging, production action, or commit.

## Residual correction

- **D03/D13:** A failed, external, needs-setup, or unconfigured production startup previously awaited `restoreCodexRouteAfterRuntimeFailure()` before retiring an older catalog probe and invalidating current core/catalog/MCP evidence. During that await, an old positive probe could still publish a verified picker. Main now retires the monitor and publishes conservative invalidation as soon as the unsuccessful startup result is known, before the route-recovery await. The unconfigured branch also clears the Full runtime-installed flag and guide step immediately. Route recovery still runs and can publish its separate restart-required flag.
- **D04/D06:** Wave 1's synchronous `catalogHealthIsCurrent(config, health)` call uses the same received health payload and checks owned runtime identity. Smoke snapshot/setup gate still derive from persisted current-version evidence; no additional change was justified in this lane.

Changed paths: `launcher/electron/main.cjs`, this report. No new helper or test file. `git diff --check -- launcher/electron/main.cjs` passed. The parent owns the shared five focused cases and live proof. Material limit: this source review does not prove a current ChatGPT account-side connector or installed-app behavior.
