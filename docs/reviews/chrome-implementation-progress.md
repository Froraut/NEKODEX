**Chrome/Web implementation — active scope**

Requested: implement the six offerings and all eight issues in the review handoff. Baseline: `b90f4e4`. Source/development work; no distributable release or installed-app replacement is implied.

This checklist is an evidence ledger, not a completion claim. A checkbox requires final affected behavior, not only source or mocked checks.

- [x] F1 Existing Chrome profile connection: searchable choice, actual ChatGPT identity binding, deterministic supported capture, remembered successful connection, cancellation/rollback.
- [x] F2 Task center: exact submission phases, retained ambiguous incidents, targeted cancel and truthful global cancel, state-derived recovery actions.
- [x] F3 Account/model readiness: capability projection, separate tools readiness and actual provider quotas, pacing-aware balanced admission.
- [x] F4 Admission queue: stable ownership, no duplicate submission, queued cancellation, pause new work, resume revalidation, explicit restart state.
- [x] F5 Files in both directions: actual PDF/CSV/image bytes, unresolved-reference failure, upload acknowledgement, generated artifacts with provenance and usable local results.
- [x] F6 Browser workspaces: correct navigation, usable account/window/tab switching, identity mutation protection and supported workspace restoration.
- [x] I1 Selected Chrome profile must be causally bound to session capture.
- [x] I2 Google profile metadata must not substitute for ChatGPT identity.
- [x] I3 Workspace navigation must use workspace policy rather than auth-only policy.
- [x] I4 Balanced routing must avoid accounts blocked by local pacing before assignment.
- [x] I5 Browser-only account selection must not require a connector.
- [x] I6 Singular cancellation must not silently cancel all work.
- [x] I7 File references must not masquerade as delivered contents.
- [x] I8 Ambiguous submissions must remain inspectable without unsafe resend.

Verification plan: focused logic contracts per implementation slice; isolated DEV UI checks for connected controls and states; actual Chrome/account and file-transfer outcomes before those features are marked verified. Preserve production sessions and active user tasks. No broad test suite, installer, signing or publication.

**Progress**

- Baseline source and review re-read. Implementation started with shared account availability needed by routing and queue.

- Slice 1 source: added pure pacing availability shared with final admission; Balanced skips blocked unbound accounts without moving pinned/selected ownership. Scheduled breaks have fixed deadlines and expire without a probe extending them. Added account capability projection/cards, removed connector requirement from browser-only Select, exposed provider allowed/exhausted quota states in six languages.
- Slice 1 checks: three focused admission tests and two focused readiness/quota tests passed; two existing quota presentation cases passed; launcher typecheck passed. First admission fixture run exposed a stale test fixture missing accountOperations, corrected without weakening production gates. Real DEV interaction remains pending; F3/I4/I5 are not marked fully verified.

- Slice 2 source: workspace pages now use a full ChatGPT navigation allowlist; embedded home/auth surfaces reject foreign main-frame navigation through the existing gesture-bound external-link broker. Global cancellation is explicitly labelled all tasks in six languages and requires a native confirmation; results show distinct HTTP/browser/compaction receipts, with unknown preserved for older runtimes.
- Slice 2 checks: two focused navigation tests and two cancellation receipt tests passed; launcher typecheck passed. Real navigation/confirmation interaction, session-mutation isolation and workspace restore are still pending.

- Slice 3 source: unresolved input_file and file_id-only input_image now fail explicitly instead of becoming inert text placeholders. Two focused parser checks passed (nine assertions). This is the first file-contract correction, not completion of F5: authorized byte resolution, PDF/CSV upload, delivery verification and output artifacts remain required.
- Next implementation: task phase protocol/retained incidents and Task Center; admission queue; Chrome profile/identity connection; complete bidirectional attachment pipeline; workspace identity isolation and restoration. Keep all six offerings and all eight issues open until their actual DEV/live outcomes are verified.

- Slice 4 source: added the dedicated Task Center screen (six languages), exact-surface authenticated progress receipts, and a private bounded task metadata journal. The browser worker now reports context staging, Send activation, accepted submission, responding and tool waiting. New helpers require the matching receipt-capable launcher before sending. Interrupted active records become explicit incidents at startup; unknown legacy send state never becomes safe-to-retry. Failed documents remain inspectable and are not reclaimed as completed tabs. History dismissal preserves a healthy completed continuation; task cancellation is targeted and distinguishes local observation from provider work.
- Slice 4 checks: four focused cases cover restart persistence, pre-send versus uncertain/accepted outcomes, authenticated HTTP progress ownership/replay and retained failed tabs, and completed-history dismissal. The HTTP fixture initially used an invalid mutation UUID; corrected the fixture and reran only the affected case. Root and launcher typechecks passed for the new protocol/UI.
- Slice 4 manual DEV evidence: launched current source with isolated `/tmp/nekodex-task-center-Lvwsqr`, selected Browser, inspected the initial panel, then moved the center to a dedicated sidebar surface to preserve browser space. Opened the final Task Center, inspected the three distinct synthetic outcomes, and dismissed one owned fixture through the actual control; its disk journal retained precisely the other two fixtures. This establishes renderer/IPC/durable dismissal, not actual provider submission or live cancellation. DEV runner and Electron PIDs 44863/44871/44875 were gracefully stopped and confirmed absent. Production was untouched. Queue, Chrome profile capture, full files and workspace recovery remain required.

- Slice 5 source: introduced a bounded automatic Web admission queue (64 waiting owners in addition to the physical tab limit). The helper polls 202 receipts for the same execution, explicitly acknowledges the acquired lease, and cancels its queued admission on abort. The Task Center supports global/per-account pause, resume, priority, queued cancellation and owner reconnect state. Restart restores waiting entries paused. Cancellation tombstones prevent late polls from resurrecting cancelled/dismissed entries. Current/pinned account ownership is preserved.
- Slice 5 review corrections: a counter-review found stale lease replay and unacknowledged acquisition rollback gaps. Start acknowledgement, current-surface validation and turn retirement now bound the receipt lifetime. Affinity is tentative until first sending progress. Exact unsent release restores new-session allowance and pacing clocks only when no newer mutation superseded them; newer provider cooldowns remain intact. Acquisition close, late host reuse, and cancel during acquisition have focused regression evidence. Cancelling has its own visible settling state.
- Slice 5 checks: focused queue/rollback cases cover capacity release, FIFO/priority bookkeeping, restart/reconnect, durable cancellation, late-start/dismiss races, lease acknowledgement/retirement, shutdown during acquisition, reuse rollback and preserved provider cooldown. Real local HTTP control + helper client tests confirm waiting beyond request retry limits and abort cleanup. No provider request is part of those tests. Four preexisting routing test fixtures lacked constructor state; restored that state and reran only the four affected lifecycle cases successfully.
- Slice 5 manual DEV evidence: isolated current-source app `/tmp/nekodex-queue-ui-JFjpSA`, main PID 59382, runner 59373, Vite 59380; synthetic driver PID 59726 submitted two scheduling-only requests to the actual private DEV control server while signed out. Used the actual Task Center to pause globally, prioritize the second item, cancel that item, resume globally and pause only the selected account. Found and fixed a UI defect where position numbers changed but rows did not move; the final view reordered correctly. Disk evidence showed exactly one item cancelled while its sibling remained waiting, no task ledger/browser lease created, and independent global/per-account pause flags. The bounded driver then cleaned up both synthetic requests. All four PIDs were confirmed absent after graceful stop; the disposable profile was moved to Trash. This verifies queue UI/IPC/storage and not-sent cancellation, not a live provider task waiting behind 16 submitted tasks.
- F3 counter-review correction: preserve unknown capability fields as null rather than treating them as false; Luna now requires explicit solAvailable=false. Account cards expose Instant/Medium/High independently, with unknown distinct from unavailable. The focused presentation case passed. Codex quota remains separate from Web admission.
- Delegation authorized by the user during implementation: Sol/high owns profile capture, file transfer and workspaces in disjoint sections; Sol/medium performed bounded queue and readiness reviews. Parent coordinates shared integration, all app lifecycle, final review and real DEV acceptance. These lanes do not authorize release work.

- F1 integrated source: searchable Stable Chrome picker, private one-use profile target claim, actual ChatGPT principal preflight separated from Google metadata, installed-principal verification, compare-and-swap binding commit after capture cleanup, and shared Chrome/Firefox/direct-import identity handling. Parent added public safe-code projection and actionable six-language error guidance so specific failures survive the renderer boundary. Profile lane added in-memory first-party cookie/local-storage rollback; failed import restores the old authentication state only after fresh matching evidence, otherwise reports verification unavailable. Offline caches/IndexedDB are not claimed to be preserved byte-for-byte.
- F1 picker manual evidence: actual isolated Electron picker with 25 synthetic Russian profiles. Search `research17` reduced the list to one item; Continue returned exactly `Profile 17`. A second owned run confirmed Escape returns `{ kind: cancel }`. Found actual horizontal clipping in the initial grid/absolute metadata layout, corrected CSS and visually rechecked full name/email/profile ID/saved badge. An initial temporary harness used an absolute npm electron import rather than Electron's built-in module; fixed the harness, stopped its failed process, and reran. Successful picker PIDs 68836 and 69649 exited; the temporary profile was moved to Trash. No real Chrome profile was opened by this harness, so selected-profile capture remains unverified against live Chrome.
- F5 source and IPC: real inline document/image bytes, explicit authorized file-ID resolver boundary, prepared.files negotiation/validation across the separate helper process, Manual attachment materialization, and generated artifact validation. Parent review required further transfer-deadline, ZIP/office, complete-write, origin, symlink and manifest hardening. File lane implemented those and a task-owned native DownloadItem size guard; final guard wiring and live provider acceptance are still being completed.
- F6 integrated source: account-scoped mutation coordinator, guarded workspace navigation and auth requests, stable generation-bound session observation, lazy window/tab restoration, and principal-bound manifests. Saved locations for principal A cannot restore under principal B. Temporary Chats remain explicitly non-restorable. Real Electron windows/restart and actual provider auth mutation still need parent verification.
- Prepared `scripts/verify-dev-file-flow.ts` for two small real provider acceptance scenarios (attachment byte-only readback and verified generated artifact). Runtime imports occur only after explicit DEV environment activation; default per-turn timeout is 180 seconds. The harness has not yet made a provider submission.

- F1 live failure (2026-09-22): selected real Stable Profile 5 (Fronk) in standard DEV; capture failed twice with chrome-profile-claim-missing before session installation. Chromium startup URL validation rejects about:blank with a fragment, which the initial claim used. Replacing it with an inert one-use loopback HTTP marker and waiting for its actual request; final live verification pending. Also corrected mismatched progress protocols: discovering/waiting-for-chrome/reading-session must keep passkey import active and cancellable. Its focused public-state regression passes; Retry copy now accurately offers an existing profile or isolated sign-in. Old DEV runner/main 71460/71469 stopped after failed import settled; production untouched.
- F1 corrected live result: restarted current DEV (main PID 83595, exec session 42750), selected real Profile 5/Fronk, then Verify account. Actual embedded ChatGPT loaded, launcher reported Signed in to ChatGPT, and durable binding recorded Profile 5 plus matching actual ChatGPT email at 2026-09-21T21:58:51.866Z. Marker/CDP focused checks passed (39 Node + 35 Bun); progress boundary case passed. No production replacement, signing or release. The standard DEV remains open with this imported session for remaining file/workspace acceptance.
- F6 additional source boundary: retained automatic task documents now require the same authentication epoch and principal fingerprint as when created; old documents remain inspectable but cannot continue under a switched principal. Five focused cases passed.

- Integrated DEV acceptance (2026-09-22): isolated browser-only dev-harness setup succeeded. Real Medium Responses input test trace 254872c9ad83 returned all three byte-only identifiers from PDF, CSV and PNG. Task Center visibly reported Receiving response and then Completed. The generated-CSV test exposed an inline-preview/download mismatch, and its failed-after-send record remained inspectable through Open conversation without resubmission. Response card Download file was observed to open a real native save panel; that manual diagnostic was cancelled. Automatic artifact acceptance remains pending the exact card/action/native-receipt fix.
- Native workspace DEV evidence: created a separate authenticated window, Cmd-T created a native two-tab group, Control-Tab switched the selected tab, and New chat navigated to normal ChatGPT home. Cmd-N created a third independent workspace; manager Close removed only that window and its manifest row. Supported home + explicitly nonrestorable Temporary Chat entries remained. Found missing Cmd-W; implementation and 13 focused cases now pass, runtime/restart restoration check pending final reload.
- Account capability DEV evidence: actual account card showed checked Instant/Medium/High/Extra High/Pro, unavailable Luna, no local hold, and separate unverified Codex allowance/Tools setup; the actual Medium file request ran without a tools connector. Existing focused routing/pacing tests cover held-account selection and pinned-owner refusal.
- Removed speculative 48-hour reauthentication reminder (eede089, pushed); live HMR removed the notice while the same account remained Verified. Manual logout remains in Settings. Fixed stale logout fixture to include real identity retirement; its focused case passes and checks cleared principal/session fingerprints and advanced epoch without live logout.
- Final file output boundary: after the exact-card hover/noWaitAfter/native-receipt correction, real DEV trace d5938db59363 completed. Native telemetry showed registered, claimed (ChatGPT authority), completed (44 bytes), and lease-completed; the CSV and versioned manifest were saved. The acceptance harness then rejected the new provenance suffix with its stale row regex. Corrected that verifier and verified the retained file size, SHA-256, CSV schema, manifest and production Markdown formatter offline, without another provider request. The original output envelope was not saved, so replay of the original expected nonce is not claimed; future synthetic receipts are persisted before assertions.
- Final DEV restart (main PID95735): same imported session remained signed in, no age-based reminder appeared. Explicit restore reopened the supported ChatGPT home and skipped the Temporary Chat with separate truthful counts. Cmd-W then closed the restored workspace and removed its durable record; verification workspace count returned to zero.
- Exact requested model metadata now crosses the worker/control/queue boundary rather than being inferred from effort. Legacy task rows remain Model unknown. The final live completed task showed chatgpt-web/medium in both the durable ledger and Task Center; old failed records survived restart with no stale Open action or resend. Root and launcher typechecks passed after integrated changes.

**Completion evidence for the six implementation groups and eight selected issues**

Checks above apply to current source plus the isolated DEV target, not the installed production app or a distributable release. Entry-point changes, adverse identity/ownership cases and queue scheduling use focused behavioral fixtures; the main happy paths use actual Electron/Chrome/ChatGPT evidence. No claim is made that all models, accounts or operating systems were live-tested.

| Requirement | Final evidence |
|---|---|
| F1 / I1 / I2 | Exact nonce page in chosen Chrome profile, independently verified ChatGPT principal, successful actual import, durable binding; focused cancellation/rollback/corruption and explicit Chrome route tests. |
| F2 / I6 / I8 | Actual accepted/responding/completed task, preserved failed-after-send document opened from Task Center; restart retains truthful incidents; exact model metadata; focused submission/cancel ownership tests and real targeted queue cancellation. |
| F3 / I4 / I5 | Actual verified account capabilities and successful Medium request without connector; focused tri-state capability, pacing, Balanced and pinned-owner cases. |
| F4 | Actual queue controls and durable selected-item cancellation; real helper/control admission for provider requests; focused capacity/ack/cancel/restart/rollback cases. |
| F5 / I7 | Real PDF/CSV/PNG byte readback; real native generated CSV download, provenance manifest and offline hash/schema validation; unresolved-reference and transfer-boundary cases. Harness formatting assertion was corrected offline as detailed above. |
| F6 / I3 | Actual windows, macOS native tabs, navigation, tab switch, targeted close, same-profile restart restore and Cmd-W; focused principal/generation isolation and unload-veto cases. |

Additional user-requested account tools onboarding is tracked separately below while its final UI handoff is verified. External tunnel creation and changes to workspace permissions are not implied by the DEV acceptance.
- Additional account-tools handoff completed: after verified authentication, Browser offers Continue account setup for that exact account. Its card distinguishes missing runtime, unverified connector and checked connector; shared wizard returns to the same account and account-scoped connector verification. Live DEV route Browser → account card → shared tunnel guide → Return preserved the account and existing settings. Twelve focused UI/ownership cases and launcher typecheck passed. No tunnel/key/permission was created or changed; independent multi-tunnel runtimes are not claimed. Official OpenAI guidance allows authorized organization/workspace associations to reuse one tunnel, so the flow does not require a new tunnel per account.
- Learned-skills update completed and reviewed: maintain-learned-skills now reconciles confirmed current-task lessons before handoff; NEKODEX recurring failures records Chrome marker/progress, native file actions, unsupported age reminders and auth-versus-tunnel readiness. The skill-preservation --base instruction was corrected to preserve existing baselines by omission. Scoped adaptations were validated and pushed to the private skill-preservation repository.
- Final targeted failure reconciliation: one real double-cancellation defect in native-turn retirement was fixed; exact-identity cancellation occurs once, synchronously before acknowledgement, and preserves another thread. Four older test expectations/fixtures were brought to existing invalidation, retained-release, structured deadline and bounded admission-registry contracts. All five targeted scenarios passed after correction; no full-suite claim.
