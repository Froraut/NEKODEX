# v6 setup and platform review — 2026-09-24

Scope: source checkout `afd9e5a`, upstream baseline `eaf4f09`, tag `v6.0.0` (peeled commit `212ceef2acac9d6ee0f3c9037abfaf4ad8ff9827`), and `upstream/main` `7579422`. This lane owns the setup, route, hook, tunnel, CLI, Linux packaging helper, installer guidance, `scripts/install-launcher.ps1` for #659, and corresponding focused tests. It did not package, download, install, restart, write a real Codex route, or change release trust.

| Upstream record | Disposition | Source evidence and reason |
| --- | --- | --- |
| [PR #600](https://github.com/miuuyy/codex-chatgpt-web/pull/600), head `7a8d13b` — Windows tunnel install locks | **Already adapted** | `src/tunnel.ts` uses bounded `EBUSY`/`EPERM` staged-file removal, exact before/owned installation snapshots, an installation lock, and compare-before-rollback (`removeTunnelInstallFile`, `installTunnelClient`, `restoreTunnelClientInstallation`). `tests/tunnel.test.ts` covers transient locks; `tests/tunnel-install-lock-focused.test.ts` covers lock ownership. The upstream untracked-binary reuse and stale-file deletion were not copied: an untracked executable has no signed manifest/ownership evidence, and deleting another attempt's staged files risks concurrent work. The current installer deliberately preserves incomplete installations for manual recovery. |
| [PR #613](https://github.com/miuuyy/codex-chatgpt-web/pull/613), head `49d3383` / [issue #612](https://github.com/miuuyy/codex-chatgpt-web/issues/612) — reordered Interrupt hook state | **Already adapted** | `src/codex-interrupt-hook.ts` locates command and trust definitions separately, validates parsed values and journal hash, permits disjoint reordered ranges and unrelated tables, and rejects changed/ambiguous ownership. `tests/codex-interrupt-hook.test.ts` covers a state table moved before its hook, normalized line endings, foreign tables, and changed command/hash. |
| [PR #627](https://github.com/miuuyy/codex-chatgpt-web/pull/627), head `eaeb42a` — CLI route restart guidance | **Adapted** | `src/cli.ts` now prints a full Codex restart reminder to stderr only after a successful route action with `changed: true`; JSON remains on stdout. `TROUBLESHOOTING.md` explains route persistence when NEKODEX is quit and exact connect/disconnect recovery. Existing CLI setup restart guidance remains. |
| [issue #614](https://github.com/miuuyy/codex-chatgpt-web/issues/614) — duplicate `[agents]` aliases | **Already present** | `TROUBLESHOOTING.md` already says to replace `max_threads` rather than adding `max_concurrent_threads_per_session` beside it, and not to create another `[agents]` table. No config mutation was needed. |
| [issue #620](https://github.com/miuuyy/codex-chatgpt-web/issues/620) — legacy `--app-name` | **Inapplicable to current schema** | `src/config-policy.ts` classifies `Codex Native2` as a legacy connector requiring migration; current identities are Native4/Native6 and Zero Risk4, selected by interaction/transport mode. Accepting the issue's Native2 argument would bypass the connector schema migration. `tests/cli.test.ts` keeps unknown connector names rejected. DEV CLI is owned by another lane. |
| [issue #626](https://github.com/miuuyy/codex-chatgpt-web/issues/626) — psmux seen as tmux on Windows | **Guidance adapted; runtime fix outside this lane** | The reported fault is tunnel-client supervision selected from the launcher's child `PATH`. `launcher/electron/runtime-tunnel-policy.cjs` builds connect arguments and diagnostics; `launcher/electron/runtime-supervisor.cjs` owns the spawned tunnel child, with `runtime.cjs` also launching setup/doctor commands. Those files belong to the parent/runtime lane. `TROUBLESHOOTING.md` gives the distinctive diagnostic and a scoped workaround without replacing credentials or tunnel state. |
| [issue #652](https://github.com/miuuyy/codex-chatgpt-web/issues/652) — Linux update needs stable wrapper | **Already present, guidance adapted** | `scripts/install-launcher.sh` creates a stable `codex-web-gpt` wrapper, bundled runner, and desktop entry after authenticated release metadata verification. `TROUBLESHOOTING.md` explains one-time reinstall for older direct AppImage installations. An already installed direct AppImage is not changed by these source edits. |
| [issue #659](https://github.com/miuuyy/codex-chatgpt-web/issues/659) — Windows installer API 403 | **Adapted after scope extension** | `scripts/install-launcher.ps1` now identifies release-lookup 403/429 rate limiting from response headers or error details, fails before the 2/4-second retry, and reports `Retry-After` or `X-RateLimit-Reset` when available. The message names `CODEX_WEB_GPT_VERSION` as the explicit-version path. That path already bypasses API lookup; the downloaded asset still must pass SHA-256 and pinned Authenticode publisher checks. The current fork has no provisioned Windows publisher thumbprint, so its standalone installer exits at that existing trust gate before lookup. A verified temporary PowerShell 7.6.6 runtime parsed the script and exercised primary/secondary rate limits, unrelated 403 and one-attempt behavior through extracted helper definitions. No installer or live API path was executed. |
| v6 Linux packaging (`8bd72e5`, `29e748b`) | **Adapted where compatible** | `scripts/prepare-linux-libnotify.sh` now selects Meson's built SONAME link instead of a recursive match that can select `.symbols`. `launcher/scripts/prepare-linux-appimage-tools.cjs` rejects wrong-architecture or non-shared ELF libnotify before copying it into the owned toolset. `launcher/scripts/smoke-linux-appimage-symbols.sh` checks native x64 ELF artifacts and treats a failed `ldd -r` as failure. The existing `beginDirectoryBuild` transaction and packaged output/signing safeguards remain. The fork's Linux installer currently publishes x64 only; upstream arm64 expansion was not copied without a matching authenticated asset and runner path. |
| v6 route/provider behavior | **Adapted** | `src/codex-integration-route.ts` rejects a selected custom `model_provider` before installing or reconnecting the bridge, including with `--replace-codex-route`; it does not rewrite that provider. Existing active-route removal still preserves a provider changed later. `tests/codex-integration.test.ts` covers both boundaries with fixture Codex homes. |
| v6 tunnel stop outcome | **Adapted** | `src/tunnel.ts` accepts a v0.0.12 SIGTERM-timeout stop receipt only when it names the expected alias and an OS probe confirms that exact PID has exited. A stopped inventory alone cannot prove exit. `tests/tunnel.test.ts` covers exited, running, wrong-alias and malformed receipts. |
| v6 `--saved-chats` / `--temporary-chats` | **Adapted in this lane's setup boundary** | `src/cli.ts` makes the flags exclusive and forwards an optional `useSavedChats` choice. `src/setup-policy.ts` defaults missing state to false, preserves an existing choice when omitted, and includes it in runtime-change detection. `tests/cli.test.ts` and `tests/setup-policy-focused.test.ts` cover those transitions. Config/provider fields and DEV setup belong to the other lanes. Existing `experimentalFreshConversationPerTurn` remains independent. |
| `upstream/main` `7579422` — later semantic hook followup | **Adapted** | `src/codex-interrupt-hook.ts` now uses pinned `toml-eslint-parser` source ranges to identify the exact parsed Interrupt element and trust table after native serialization, including inline arrays, literal TOML command quoting and moved comments. It keeps the fork's separate `hooks.json` trust helpers, journal hash and fail-closed ownership checks. A local separator correction preserves a surviving foreign table when the original config had no final newline. `tests/codex-interrupt-hook.test.ts` covers inline reserialization/reinstall, unchanged foreign settings, changed command/hash rejection, reordered state and all existing JSON trust cases. The parent owns the parser package/lock and generated architecture map. |

Focused verification: targeted Bun cases for saved-chat setup flags and transition, route provider conflict and later provider edit/removal, tunnel stop and Windows cleanup retry passed. The focused hook file passed all 23 cases after AST adaptation. One Node fixture rejected an ARM library at the x64 AppImage tool boundary. `sh -n` and `node --check` passed for changed packaging helpers; `git diff --check` passed. Root typecheck was attempted once before the parent's integrated pass and reported only concurrently edited `ResolvedBrowserConfig` fixtures missing `useSavedChats` in `tests/backend-review-regressions.test.ts`, `tests/browser-helper-protocol-cleanup.test.ts`, and `tests/launcher-helper-client.test.ts`; those type/fixture files belong to the saved-chat lane. For #659, the official `PowerShell/PowerShell` v7.6.6 macOS arm64 tarball matched the published SHA-256 `6df833d094ebac1c1a74340d7b3437f4aaf5e03ce640484a1c4359f3ce8b3db1` from `hashes.sha256`; its `pwsh` parsed the installer AST and the isolated classifier fixture passed for `X-RateLimit-Reset`, `Retry-After`, unrelated 403 and fail-fast one-attempt behavior. No Linux or Windows binary installation was executed on this macOS host.
