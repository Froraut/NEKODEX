# v6 browser lane review — 2026-09-24

Scope: compare local `afd9e5a` (including the architecture extraction) with reviewed upstream `eaf4f09`, v6.0.0 `212ceef`, and follow-up `7579422`. GitHub issue/PR snapshots under `/Users/alex/Dev/nekodex-upstream-v6-evidence-20260924/items/` are untrusted leads. Current checkout source and focused behavior checks determine the dispositions. This lane did not change production, install/restart, release, push, or the review ledger.

| Upstream browser change | Disposition | Local evidence and decision |
| --- | --- | --- |
| Explicit model family and effort confirmation; GPT-6 Latest can describe lower effort as 5.6 | **Adapted** | The extracted owner is `src/adapters/chatgpt-web/browser-model-selection.ts:72-80,98-122,413-433,451`. The checked family radio, live slider description, effort position and ready composer must all agree before Send. `src/adapters/chatgpt-web/browser-worker.ts:513,2583-2592` accepts the parent route's `modelFamily` and rejects a conflicting compaction pin or GPT-6 Luna. The helper protocol carries it at `browser-helper-main.ts:43,259` and `launcher-helper-client.ts:190`. Legacy unpinned Pro still uses its existing `proModelVersion`; no picker rows were removed. |
| Locked effort tick and cooldown while waiting for picker | **Adapted / already present** | `src/chatgpt-session.ts:457` reads complete tick lock evidence when the UI exposes it; `browser-model-selection.ts:369` fails before Send for a locked target. Older picker variants retain the existing modal-gate checks. The model controller's slider wait already races a rate-limit dialog (`browser-model-selection.ts:317-340`); `browser-worker.ts:2247` checks the dialog before the next multipart composer mutation. |
| Oversize submission diagnostics and accepted-generation observation retries | **Already present** | `src/adapters/chatgpt-web/browser-worker.ts:293-333` binds a 413 JSON response to this page's current POST after Send. `browser-worker.ts:1935-1969` recovers submission observation on the same owned surface; `browser-worker.ts:3548` bounds internal observation faults after acceptance. The focused accepted-send case passed without a second Send. These checks do not prove live ChatGPT behavior. |
| Fresh saved chats for automatic and Manual turns | **Adapted; source wiring confirmed** | `src/chatgpt-session.ts:5-9,518` defines and checks the selected blank URL. `browser-worker.ts:714,1128-1156,2854,3004,3074` keeps maintenance inspection temporary, navigates automatic turns according to config, and reselects effort after a saved multipart chat changes URL. `launcher-helper-client.ts:183` and `browser-helper-main.ts:36,234` preserve the flag through the helper. Manual `src/adapters/chatgpt-web/turn-runtime.ts:397-403` now passes `provider.chatgptWeb?.useSavedChats === true` through `src/launcher-browser-control.ts:133` → `launcher/electron/control-server.cjs:305-315` → `browser-manual-turns.cjs:107,163,193,220` → `browser-host.cjs:933,968,1000`. The per-turn policy prevents a retry/retained continuation from changing it. `launcher/src/SettingsSurface.tsx:249-253` exposes the switch in Manual mode. Source wiring was read; a live Manual submission was not run by this lane. |
| Exact connector selected-pill check, async accepted callback | **Adapted / already present** | `browser-worker.ts:1544-1548` now matches the connector's exact `data-keyword`, avoiding a partial text match. `browser-worker.ts:2033-2044` already awaits `onSubmitted`, preserving its acceptance boundary. Temporary-only personalization preflight stays scoped by the chat URL (`browser-worker.ts:1668`). |
| Auth probe single-flight | **Adapted** | `launcher/electron/browser-host.cjs:2997-3012` coalesces ordinary concurrent probes, while setup, observation-only and caller-cancelled probes keep their own semantics and the existing generation/principal checks in `runAuthenticationProbe`. |
| Turn-start lease timeout and disconnected caller (#623) | **Adapted to extracted lifecycle** | `src/launcher-browser-control.ts:119` allows 30 seconds for direct start and reports phase timeout. `launcher/electron/control-server.cjs:426-466` aborts the direct acquisition on response close. `browser-turn-lifecycle.cjs:56-130,369-459,494-510` aborts initialization and releases only a matching trace/helper/surface/acquisition signal, with ledger settlement before removal. The current `taskProgressVersion: 1` queue retains its separate cancel/ack protocol at `launcher-browser-control.ts:373-470` and `control-server.cjs:416-422`. |
| Closing a turn tab with no active turn or while cancellation settles | **Already present** | `launcher/electron/browser-turn-lifecycle.cjs:330-365` validates the expected trace, waits for running-turn cancellation and tolerates an end receipt that removed the same tab first. `browser-host.cjs:913-928` hides the idle host instead of revealing a gray blank document after the last tab closes. No broader removal fallback was imported because it would bypass the one-owner cancellation boundary. |
| Markdown plan markers, paths, file-reference labels, hydrated links | **Already present / adapted** | `src/adapters/chatgpt-web/markdown.ts:17-34` already restores standalone plan markers outside code/quotes; `markdown.ts:35-66` keeps file buttons inert. `markdown.ts:72-79` now escapes local path link labels. `browser-response-dom.ts:250-261,361-362` withholds a pending link until `href` arrives and records targets; `markdown.ts:214,230,304,391,460` rejects a changed destination after a block is committed. |
| Upstream Limits plan inspector, usage endpoint, helper verifier and UI/store | **Inapplicable by parent decision** | Local per-account durable receipt and Native provider quota ownership already cover the main need; dated estimates do not prove remaining allowance. No upstream Limits module, browser-helper-verifier operation, usage hook or dependency was added. Existing local usage observations remain owned by `browser-worker.ts:2533-2547` and `control-server.cjs:395-414`. |

## Explicit PR dispositions

| PR | Disposition | Evidence limit |
| --- | --- | --- |
| [#589](https://github.com/miuuyy/codex-chatgpt-web/pull/589) GFM email autolink renderer patch | **Rejected by parent review** | The proposed `autolink-render-compat.ts` uses a CDP debugger breakpoint and private ChatGPT bundle signature. That injection is outside the reviewed `eaf4f09..7579422` source diff and is not an acceptable runtime dependency for this checkout. No dependency bump or breakpoint was added. |
| [#602](https://github.com/miuuyy/codex-chatgpt-web/pull/602) missing Pro classification | **Already present, with additional locked-tick handling adapted** | `browser-model-selection.ts:49-64,354-382` returns a structured non-fallback Pro error for an absent step; locked present steps are now rejected before Send. Account allowance was not checked live. |
| [#603](https://github.com/miuuyy/codex-chatgpt-web/pull/603) multipart cooldown dialog | **Already present** | `browser-worker.ts:2247,2883` checks the dialog between accepted stages before editing the next composer. |
| [#611](https://github.com/miuuyy/codex-chatgpt-web/pull/611) broad personalization locale list | **Existing structural fallback; broad locale table rejected** | `browser-personalization.ts:331-355` first looks for the known exact labels, then proves access to the configured connector when those controls are absent. The current structural fallback preserves the browser boundary without a speculative table of translations. The broad locale table was not copied. |
| [#623](https://github.com/miuuyy/codex-chatgpt-web/pull/623) lease acquisition | **Adapted** | See the direct-start owner/abort row above. The queue flow remains distinct. |
| [#649](https://github.com/miuuyy/codex-chatgpt-web/pull/649) French and Korean Latest labels | **Adapted narrowly** | `browser-model-selection.ts:72` recognizes observed `Le plus récent` and `최신`, with the same checked radio and independent slider/version proof. Launcher-wide locale expansion belongs to the parent/UI owner and was not changed here. |

## Verification and handoff

- Passed: focused Bun cases for explicit Latest lower effort through Send, French/Korean family labels, saved versus temporary URL, locked effort tick, Markdown path escaping and late link destination; the accepted Full-mode send case recovered one stalled DOM probe without a second Send. The launcher control lifecycle case passed after its old fixture was updated to expect the existing mutation ID. Commands used `bun test ... --test-name-pattern=... --timeout=4000` on the named files, not the package suite.
- Passed: targeted Node cases for direct start authentication/abort, tab initialization cleanup, replacement-owner protection, Manual saved navigation and retry policy, Manual control payload, and ordinary auth-probe coalescing, using `node --test --test-name-pattern=...`.
- `git diff --check` passed. One initial `browser-worker-contract.test.ts` selection exposed an outdated fake dialog locator; that fixture was corrected and the accepted-send case passed. Another source-text assertion in that file still refers to code moved out of the monolith; it was excluded rather than used as behavior evidence. An initial broad selection of three test files was stopped and replaced with the focused names above.
- No integrated build, whole suite, live account/ChatGPT submission, installed-app check, or source publication was run by this lane. Parent owns those gates, architecture/extension-guide updates and the review ledger. Manual runtime and Settings source wiring were read after the parent changes; the integrated typecheck and any `ResolvedBrowserConfig` fixture results belong to the parent pass.
